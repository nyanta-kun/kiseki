# G04: money-flow 検証ハーネス＋初期観察

## 目的
朝→直前のオッズ変動が「賢い金の流れ」を示すか——公開オッズを超える唯一の
市場内シグナル候補——を検証するハーネスを作り、現有データで初期観察を出す。

## データの現実（過大主張禁止）
- `wt_odds_snapshot`: morning 5日/345R・evening 92,709行のみ（2026-06-09〜）。
  **統計的結論を出すには薄い**。本タスクの主目的は「データが溜まり次第すぐ回せる
  再実行可能なハーネス」と「初期の記述統計」。
- 確定オッズ = `wt_odds`（上限値）。結果 = `wt_entries.finish_order between(1,3)`。

## 成果物
`scripts/exp_moneyflow_wt.py`（`--from/--to` 期間指定で再実行可能）:

1. **記述統計（現有データで実行）**:
   - bet_type別・オッズ帯別の morning→確定ドリフト分布（比率の中央値・四分位・短縮/伸長率）
   - 現行ガミ3段階（<3 / 3-5 / ≥5）の判定が morning→確定で反転する率
   - **的中目 vs 非的中目のオッズ短縮差**: 「結果的に当たった組み合わせは朝→確定で
     オッズが短縮していたか」（smart money 仮説の最初の検定・符号とeffect size・CI）
2. **エッジ検定の枠組み（doc18セマンティクス・データが薄い間は枠組み＋現時点値）**:
   - リーク無しモデル（`lgbm_wt_eval` 等、評価期間を学習していないもの。なければ
     TRAIN期間限定で学習。`exp_leakfree_rescore_wt.py` 参照）
   - ランキングは全エントリー・≤6車は出走表基準・欠車void
   - 検定セル（**事前登録・これ以外を追わない**）:
     a) 「モデル上位目のうちオッズ短縮した目」vs「伸長した目」の確定ROI差
     b) 現行C0戦略 × 「推奨目が短縮」ゲートの有無
     c) fav_mismatch × 「市場本命が朝→直前で交代」の有無
3. レポート `docs/analysis/23-moneyflow-initial.md`:
   観察結果・現時点の標本数・「結論を出せる最小標本数」の見積もり・再実行手順。

## 受け入れ基準
- 現有データで end-to-end 実行が通り、レポートが生成されること。
- 多重比較への防衛: 上記事前登録セル以外の探索的数字は「参考・追試しない」と明記。

## 触ってよいファイル
- `scripts/exp_moneyflow_wt.py`（新規）
- `docs/analysis/23-moneyflow-initial.md`（新規）

## 禁止事項
- 本番（wave-picks-wt/cron）への反映・git commit・スキーマ変更
