# 事前登録: p3 モデルの目的関数を Plackett-Luce 直接最尤へ替える（2026-09-14）

**この文書は 2026Q4 以降のデータを一度も見ない状態で書いた。** 実行後に本文を書き換えない
（結果は「結果」節に追記するだけ）。

## 背景

`docs/type_lab/objective_pl_2026_09_14.md`。本番は**選手1人1行の二値分類2本**
（`top3_flag` = `lgbm_wt_eval`/`lgbm_wt` と `win_flag` = `lgbm_wt_win`）だが、
消費側の `strategy_wt.rank_7t3_blend_probs` は **既に PL（Plackett-Luce）を仮定**して
三連単確率を組んでいる。＝ 学習は「その1車が3着以内か」、消費は「レース内の順序と同時確率」。

VAL（焼けた窓 w1 2026-04-13〜07-15 / w2 2026-01-01〜04-12・5seed）での観測:

| 腕 | ΔAUC(3着内) | Δ1位勝率 | Δ上位2車そろい |
|---|--:|--:|--:|
| **PL-K3**（ListMLE 上位3段） | **+0.00163 / +0.00237** | **+0.30 / +0.54pt** | **+0.08 / +0.38pt** |
| PL-K6 | −0.00529 / −0.00408 | +0.11 / −0.01 | −0.22 / +0.05 |
| reg_rank（中央 v27 型） | −0.00621 / −0.00593 | +0.33 / +0.11 | −0.07 / −0.01 |

物差し（同じハーネスで**採用された** `MEETING_FORM`＝節内成績）は
ΔAUC +0.00058 / +0.00068・Δ1位勝率 +0.33 / +0.14pt。**PL-K3 はその 2〜3倍。**

🔴 **ただし商品KPI には届いていない**（最良形でも Δ表示的中 +0.27 / +0.08pt で
4本とも CI が 0 を跨ぐ・ROI は窓で符号反転・10万+/日 は両窓一貫で −14〜26%）。
＝ **VAL では「モデルは良くなるが商品は判定不能」。** これを TEST で決める。

## 🔴 凍結した腕（結果を見る前に確定）

| 項目 | 値 |
|---|---|
| 腕 | **PL-K3 の p3 × 現行 `lgbm_wt_win` の pw** の1本だけ |
| 却下済み（再検討しない） | ①p3 も pw も PL（表示的中 −1.10pt）②温度較正 T=1.4（−3.25pt）③PL-K6 ④reg_rank |
| 目的関数 | ListMLE（上位3段）・行はレース単位のリスト |
| 特徴 | `FEATURE_COLS_WT`（そのとき本番にある列をそのまま） |
| 学習窓 | expanding（`TRAIN_FROM=2024-04-01` 〜 その TEST 窓の直前） |
| 較正 | **腕の一部として凍結する**（下記） |

### 🔴 較正の引き直しを腕に含める

PL は確率が上に寄る（十分位の実測/予測 q9 0.942 ↔ q0 1.146）。実測で
`axis_sum` 平均 1.484 → 1.546・`>= 1.44` が 57% → 67%。
`RANK_7C_P3_SUM_MIN`（= `AXIS_SUM_FIRM` 1.44）も `AXIS_GATE_MIN`（4プランの p20）も
**絶対閾値**なので、モデルだけ差し替えると**別の商品を売ることになる**。

→ **TEST に入る前に、探索窓（2024-07-01〜2025-12-31）の PL-K3 出力で分位を取り直し、
値を本文へ書き込んで凍結する。** TEST 期間の分布で取り直すことはしない（それをすると
TEST を見たことになる）。

    凍結値（TEST 開始前にここへ追記する）:
      AXIS_SUM_FIRM      = （探索窓の p__ ＝ ______）
      AXIS_GATE_MIN      = （4プラン × 探索窓のプラン内 p20 ＝ ______）

## 窓

`keirin_protocol.clean_test_start()` は **2026-07-16**（w1 の焼けが 07-15 まで伸びている）。
四半期で揃えると最初の綺麗な窓は **2026Q4**。`MIN_TEST_WINDOWS = 4` なので:

| 窓 | 期間 |
|---|---|
| t1 | 2026-10-01 〜 2026-12-31 |
| t2 | 2027-01-01 〜 2027-03-31 |
| t3 | 2027-04-01 〜 2027-06-30 |
| t4 | 2027-07-01 〜 2027-09-30 |

**判定は 2027-10-01 以降。** それより前に採否を宣言しない。
🔴 4窓で **SE ≒ 0.15/√4 = 0.075pt**。VAL の観測 +0.08〜0.38pt を判定できる大きさ。

## 採用ライン（両方を満たしたときだけ採用）

1. **主指標: 上位2車そろい率**（＝軸崩壊の裏返し）が 4窓の平均で **+0.15pt 以上**
   かつ 4窓のうち **3窓以上でプラス**
2. **商品KPI: 表示的中**が 4窓通算（≒27,000R）の対レース bootstrap で
   **95%CI の下限が 0 を上回る**

## 🔴 事前に認めておく代償（これが出ても「想定外」としない）

**10万+/日 は VAL の両窓で一貫して −14〜26%。** これは測定のばらつきではなく
構造（PL が上に寄る＝堅い側へ寄る）なので、TEST でも出る前提。
**「看板より表示的中を採る」という店頭KPIの判断を、TEST の結果を見る前に決めておくこと。**
決められないなら、この TEST は走らせても採否が決まらない。

## 記録

- 各窓の評価を行ったら `keirin_protocol.record_test_usage()` を呼び、
  `docs/KEIRIN_TEST_USAGE_LEDGER.md` へ追記する
- 実装: `scripts/exp_objective_pl_{prep,ab,product,product2,product3}.py`
- 🔴 **本番コードはまだ変更していない。** 採用が決まってから差し替える

## 結果

（2027-10-01 以降にここへ追記する。本文は書き換えない）
