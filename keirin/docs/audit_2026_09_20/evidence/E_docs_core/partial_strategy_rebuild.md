# 監査対象: keirin/docs/strategy_rebuild_2026_08.md（2404行・全通読済み）

監査日: 2026-09-20（文書の最終エントリは 2026-08-23。約1ヶ月のギャップがある点に注意）
方法: ①本文からの主張抽出 ②`keirin/src/strategy_wt.py` ・`keirin/scripts/netkeirin_submit_wt.py` の実物との突合（grep/Read、file:line 記載）③スクリプト実在確認（`ls`/`git log`）。DB・実行は行っていない。

---

## 1. 主張抽出表

| 文書:行番号 | 主張の要約 | 根拠スクリプト/窓 | 本番が依拠しているか | 状態 |
|---|---|---|---|---|
| L1-14 | ユーザー方針: 1レース1万円固定・1日最低10件・三連複中心→三連単中心へ転換 | — | 要確認（DB） | 方針表明。以降の章の前提 |
| L19-29 | 実売16日(635件): 39.7件/日・ROI 67.8%・100%超の日 1/16 | `netkeirin_submissions`+`bet_detail` | はい(実測) | 現行（当時） |
| L31-34 | ペーパー(599日): 42.7件/日・ROI 79.6%・100%超23.7% | `picks_history` | はい | 現行（当時） |
| L41-56 | ランク別 損益分岐的中率と実際の差（9C -10.1pt/7S -9.5pt/7C -11.2pt等） | 実測表 | 要確認 | 分析結果 |
| L60-72 | 1件的中の日次寄与=倍率÷N。ベース層は15件中10件必要で非現実的 | 算数 | — | 分析（恒等式） |
| L78-89 | 優先順位・上限を振っても平均ROIは78〜83%で平坦 | 599日再集計 | 要確認 | 分析結果 |
| L93-98 | 三連複3点は的中率×平均倍率=2.19-2.26に保存、ROI73.8-75.4%。外部145シーズンでROI100%超は3例中2例が翌年崩壊 | 既存メモ引用 [[keirin_trio3_roi100_infeasible...]] 等 | — | 参照のみ |
| L107-127 | 恒等式: 払戻>=N×10,000 が日次100%の条件。T=N×10,000で自己整合すべき | 算数 | 設計原理として提案 | 提案 |
| L131-155 | §4.0: 簡易実装(軸1=1着率最上位/軸2=3着内率最上位)による初期結論2件が出荷実装(軸1=3着内率上位2車から42順序対総当たり)で消えた | `exp_tf_shape_prod.py`等 | — | **文書内自己訂正**（教訓として明記） |
| L159-167 | §4.1: 軸固定なら軸2が3着に来る割合29.2-47.9%だが「本番は軸を固定していない」→採用根拠にならない | `exp_tf_axis2_position.py`(14,922R) | いいえ（否定目的の記録） | 撤回済み（同節で明記） |
| L169-187 | §4.2: 効いた変更3つ。母集団を「決勝系×別ライン」→「全7車」で100%超26.1%→36.8%。日次上限5→20。T=N×10,000でm=1.0が最良 | `exp_tf_shape_prod.py` | **いいえ（後で覆された。下記コード突合参照）** | **文書には無い形で後日production側が反転** |
| L191-194 | §4.3: 日次選別順序は効かない（無作為96.9%とEV降順99.3%がほぼ同じ） | `exp_tf_shape_confirm.py`（簡易実装） | — | 分析結果（簡易実装の値である点に注意) |
| L198-211 | §4.4: 案C（一撃20+三連複10=30件/日、ROI81.2%・100%超33.8%・15万+0.51件/日）を「採用」と明記 | 出荷実装 234日 | **いいえ**（2026-08-24 production は別方向へ、下記参照） | **文書内『採用』表記が後日覆っている（未反映のまま放置）** |
| L226-229 | §5: Elo残差は不採用。A/Bで+0.38ptに見えたが出荷実装で+0.19ptに縮小し同日中に棄却 | [[keirin_orphan_signals_ab_2026_08_21]] | いいえ | 撤回済み |
| L239-251 | §6.0: 天井=帯ROI×予算/T。T=15万で天井5.0%、7T1実測3.04%(天井の61%)。帯ROIは15-300倍超フラット(71-76%) | `keirin_highpay_payout_ceiling` 引用 | 分析用 | 現行の理解 |
| L253-269 | §6.1 レバーA: 目標判定を予測オッズ点推定→下限側へ（マージンm）。under08=42.1%/under05=10.7% | `odds_tf_meta.json`実測 / `exp_highpay_odds_drift.py` | 未実装（提案） | 未着手 |
| L271-284 | §6.2 レバーB: 本命バスト確率(`lgbm_wt_bad`系, AUC0.684-0.685)を7T1のレース選定へ逆向き適用は未検証 | — | 未実装 | 未着手 |
| L288-295 | §6.3 再提案しない一覧(Elo残差/EV上位厳選/帯選択/三連複相手選択/荒れ選別/落車リスク) | 既存メモ引用 | — | 撤回済み一覧 |
| L301-309 | §7 KPI置換提案: 平均ROIを目標に置かない。「100%超の日」「T超頻度」「0円の日」へ | — | 提案 | 提案 |
| L317-322 | §8.1: 触る場所は`src/strategy_wt.py`のRANK_7T1_TARGET_PAYOUT/RANK_7T1_DAILY_CAP/母集団条件、`scripts/build_7t1_candidates.py`、`src/netkeirin_submit_wt.py`のRANK_CONFIGS、`scripts/rebuild_7t1_walkforward_pg.py` | — | 部分的にいいえ（下記コード突合） | 一部未反映 |
| L342-344 | §8.3: 7T1帯は月次ROIが62-140%に振れ、CI±2.5ptに収めるには約15年かかる（7M1実測から） | — | 分析 | 参照 |
| L370-384 | §9 供給量: 7車14,981R(64.0件/日)/9車1,464R(6.3件/日)/6車956R/5車190R。7T1母集団13.4件/日は64件の21% | 2026-01-01〜08-22 集計 | 要確認(DB) | 分析結果 |
| L388-454 | §10: 中央market_agreeの競輪移植。信号自体はagree/非agreeで的中差+8.7〜+19.1pt。だが予測オッズ板は「モデル自身の確率」で96%一致・乖離3.7%のみ。実用効果は+0.9pt(7C)。7M1は符号が逆。**§10.6で未採用と結論** | `exp_market_agree_wt.py`（14,922R） | **いいえ（未採用と明記）** | 検証のみ・不採用 |
| L460-505 | §11: `rebuild_7t2_walkforward_pg.py --upto 2026-08-21`で月次凍結vintageの再構築を実施し4,659行をVPS本番`picks_history`へ書き込んだ | 同スクリプト | **要検証（下記: スクリプトがgit履歴に存在しない）** | 実施済みと主張 |
| L464-505 | §11.1-11.2: 7T2単独233日で的中3.0%・ROI82.3%・15万+0.51件/日。案C(7T2 20件+三連複10件)がDB実測でwalk-forward推定と一致（+9.9pt/+0.33） | DB実データ | はい（DB実測と主張） | 検証済みと主張だがRANK_7T2は現行コードに存在しない（下記） |
| L509-583 | §12: 中央v27型の位置別確率合成(1,.5,0)はバルク精度で明確改善(top1 5.9%→10.5%)するが、7T1/7T2は「オッズ15-75倍の裾」しか買わないため商品KPIには伝わらない(的中3.01%→2.82%,ROI82.7→82.4%) | `exp_leg_prob_heads.py` | いいえ | 検証のみ・未採用 |
| L586-639 | §13: 三連単210通りの粒度に妙味なし。実際に買う15-75倍帯は全EV分位で払戻率74.85%に張り付く | `exp_tf_leg_gap.py` | — | 否定結果 |
| L643-693 | §14: 条件なし全数走査(3,133,620目)。探索窓で壁超えたセルは0件、構造条件75セルでも1件のみ通過し確認窓で失格 | `exp_tf_unconstrained_scan.py` | — | 否定結果（事前登録・二重窓） |
| L696-783 | §15: 三連複の相手選別「順位5のみ」がROI83.5-83.6%(両窓)で壁超え、既存7M1節の独立測定(5番手81.6%)とも整合。レース選別は効かない | `exp_trio_partner_rank.py`等(14,922R) | **後にL1091-1125で否定される（下記）** | **§21で撤回** |
| L786-841 | §16: レース形状(gap34/spread36/gap67)は機序として実在するが市場が織り込みROIには残らない | `exp_trio_race_shape.py`/`_priced.py` | いいえ | 否定結果 |
| L845-889 | §17: 軸2の選び方を変えても動かない(3着内率65-67.5%で横並び)。だが「軸2が飛ぶ条件」は正確に予測でき(bad2 Q1 71.1%↔Q4 41.8%)、二軸的中36-75.5%と2倍動く | `exp_trio_axis2.py`/`_conditions.py` | 部分的（分析のみ） | 記録 |
| L893-951 | §18: 信頼度スコア(conf=z(axis_sum)-z(bad2))による形の切替。「高信頼D/低信頼A/中は買わない」でROI74.3→84.0/82.8%・100%超+14.9pt。**本セッション最良**と明記 | `exp_trio_shape_switch.py` | **§21で撤回される前提に依存** | **§21で無効化される可能性大（下記）** |
| L955-1018 | §19: 上限を外し厳選。①1点(順位5)は厳選するほど悪化 ②5点総流し×堅い上位10%が両窓で壁超え(83.6/82.3%)。投資1/10で損失93%削減 | `exp_trio_selectivity.py`等 | **§21で撤回される前提に依存** | **§21で撤回対象と明記（下記）** |
| L1021-1087 | §20: 「同程度なら点数増やむなし」は不成立。順位5単独が最良で3/4/5候補追加は悪化。順位5は月次で下振れしない(8ヶ月中最小75.9%) | `exp_trio_adaptive_legs.py`等 | **§21で撤回**（後述） | **撤回対象と自認（次章）** |
| L1091-1124 | **§21: 独立窓(2024-01〜2025-12・46,359R)で §15〜§20 の「順位5」優位が消失。順位5は2026年だけの現象(独立窓では全順位横並び75.3-75.7%)。「同じ8ヶ月の前後分割は確認窓ではない」と結論** | 独立2年窓 | **文書自身が明記** | **§15〜§20を明示的に撤回** |
| L1128-1150 | §22.1: ペア規則を手書きで変えても二軸的中53.5-54.0%で動かない | `exp_trio_axis_pair.py`(独立窓46,359R) | いいえ | 否定結果 |
| L1151-1200 | §22.2: ペア単位学習(21ペア×22特徴)のargmaxで二軸的中53.75%→54.80%(+1.05pt有意)。ただしROIは83.6%→84.1%で有意でない | `exp_trio_pair_model.py` | 未実装（コード確認：joint_partner系は現行production未検出） | 検証済み・未実装 |
| L1203-1226 | §23: 相手を3着内率の"値"(p3_gap_dn)で切ると両窓で同方向の勾配。ただし片窓しか壁を超えず不採用水準 | `exp_trio_partner_p3value.py` | いいえ | 不採用 |
| L1228-1296 | §24: 「三者同時確率」で相手選択→両窓・3年独立窓すべてで有意(+1.1〜+2.0pt的中/+3.5〜+4.2pt ROI)。**同一実行内で参考(順位5)が2026:83.6%/2024-25:75.7%と§21の撤回現象を正しく再現**。ROIは79-82%で黒字ではない | `exp_trio_joint_partner.py` | 未実装（下記コード確認） | 検証済み・**production未反映（doc自身も明記）** |
| L1298-1319 | §25: 改善の実体はライン関係のみ(ライン特徴を抜くと的中23.7-23.9%へ戻る)。p3_gap_dn(§23)の限界寄与はほぼゼロ | `exp_trio_joint_ablation.py` | — | 分析結果 |
| L1333-1355 | §26: 軸1は改善不可。再学習モデルは現行(p3最大)と91-94%同じ車を選び、残りは損(Δ-0.22〜-0.42pt) | `exp_axis1_selection.py` | いいえ | 否定結果 |
| L1357-1442 | §27: 軸1バスト層別学習は悪化する(全帯・両窓)。振り分けの価値はp3集中度(entropy)で説明尽くされ**「既に本番のゲートに入っている」**(`S1W_ENTROPY_MAX`/`RANK_7S_ENTROPY_MAX`/`axis_sum`) | `exp_axis1_bust_stratified.py` | **要検証(下記)** | 分析結果+本番ゲート言及 |
| L1444-1548 | §28: 「入着困難なら次点繰上げ」は不成立。全帯・両窓で的中率悪化(-8〜-16pt)。ROIのみ片窓で逆転するが不採用 | `exp_axis1_swap_by_bustrisk.py`/`_formation.py` | いいえ | 不採用 |
| L1550-1611 | §28.2: 軸2を"ペア相性"で選び直す形。1位を軸から一律外すと-21pt級で惨敗。だが「モデルが選ぶときだけ切替」なら両窓+0.17〜+1.5pt(有意)。ただし§24腕Bの部分集合で新機構不要 | `exp_axis1_swap_pairmodel.py` | いいえ（§24腕Bに包含） | 検証済み・未実装 |
| L1613-1663 | §29: §24は7Cへ移植不可。7Cは実際5候補中平均4.36点(3点8%/4点48%/5点44%)を買っており「ほぼ総流し」。同時確率での差替はΔ+0.11pt(有意でない) | `exp_7c_leg_joint_transfer.py` | いいえ | 不採用（一般則抽出） |
| L1624-1627 | **「コード中の『落差カット後 平均2.60点』は`legs_floor`を戻す前の値」と明記** — 文書自身が過去の暫定値を訂正 | — | — | **文書内自己訂正** |
| L1666-1799 | §30: §24は9Cへも移植不可。9Cは「7候補から3点以上」ではなく実際は平均4.93点(71%)を買っている、との**誤読の訂正**を経て測定。vintage版では有意な改善セルなし(最大+0.86pt、CI 0またぐ) | `exp_9c_leg_joint_transfer.py`(9車キャッシュ5,830R/vintage4,607R) | **要検証(下記: 9Cの実定数はコードと一致)** | 不採用 |
| L1698-1799 | §30: `wt_entries.pred_top3_pct`はbackfill値で2026-07-19追加・過去分は事後補完(model-vintage look-ahead)。vintageの`wf_preds9_*.pkl`と6.2%しか一致しない | `exp_9c_leg_joint_transfer.py --wf` | 分析 | 重要な方法論的指摘 |
| L1803-1891 | §31: §24をvintage(look-aheadなし)で再測定しても両窓・両出所で成立(+1.5-1.6pt的中・ROI+2.4-4.9pt)。撤回済み「順位5」はvintageでROI下限が壁割れ(look-aheadの産物と確認)。**「載せる商品がまだ無い」（未実装）と明記** | `exp_trio_joint_partner.py`+`build_wf7_cache.py` | いいえ（明記のとおり未実装） | 検証完了・未実装 |
| L1895-2007 | §32: 旧`exp_unified_compare.py`に3つの欠陥(in-sample混入/母集団不揃い/LG_JOINTがp3へ黙って退化)。`exp_unified_compare2.py`で作り直し。探索窓で勝った構成(中混戦+4.9pt/荒れ+8.0pt)が確認窓で悪化(-13.2pt有意/-2.4pt) | `exp_unified_compare2.py` | — | 方法論修正+新たな否定結果 |
| L2010-2102 | §33: RANK_CONFIGS定義順=7H2>9H1>7S>9C>7B>7C>7T1>7H1>7M1、無効=7H2/9H1/9A/9S/9SS/S1、実効=7S>9C>7B>7C>7T1>7H1>7M1。picks_historyでの再現率57.2%（80件がプールに行無し・7C14件は本番p3なら9件通る） | `phase0_pool_audit.py` | **はい（当時。コード突合で確認済み）** | **後にL2106以降で「14件からの一般化が誤り」と自己訂正** |
| L2106-2192 | **§34: §33の訂正。プールを本番モデルで作り直しても一致率は58.2%(+1.0pt)にしかならない。真因は「入稿=朝の候補JSON」対「再構築=事後のDB再構築」という経路の不一致（p3の値自体が違う個体まで存在）** | `phase0_logic_check.py` | — | **§33を明示的に訂正** |
| L2196-2267 | **§35: 「現行実績と一致」は原理的に達成不可能と確定。理由: 本番モデルは再学習され続けるため、過去の候補JSONが見たp3(pred_prob)はJSON内にしか残っておらず、`wt_entries.pred_top3_pct`は別経路の別の値。同一レースの3着内確率が3種類存在し、どれも一致しない（Web表示用/ゲート判定用/backfill実績表示用）** | `phase0_reconcile_7s.py`（7S・2026-08-01〜22・一致率34.9%） | — | **Phase 0完了条件を正式に取り下げ・§33/34からの3度目の訂正** |
| L2271-2404 | §36: ユーザー判断で「プールも現行相当もvintage再構築で統一」を採用。確認窓2026-01〜06で体系合計29.81件/日・ROI78.7%(壁+3.9pt)。7M1はプール単独最高(87.4%)だが優先順位適用後は最下位(75.2%)。7S上限12は0/179日で一度も発動せず、7T1上限5は178/181日で常時到達（=「厳選効果ではなく量の制御」の可能性） | `phase0_vintage_pool.py` | 一部はい（下記: RANK_7S_DAILY_CAP=12は現行コードと一致、7T1上限は後に5→0へ改定） | 確定した現状測定 |
| L2388-2404 | §36末尾: デフォルト引数束縛の罠（モジュール変数を差し替えてもdaily_select=RANK_7T1_DAILY_CAPの束縛は変わらない）を実地で踏み、誤報告する寸前で気づいた | `build_7t1_candidates.build` | — | 教訓 |

---

## 2. コード突合で見つかった食い違い（file:line付き）

### 2.1 【最重要】§4.2/§4.4の「採用」構成は、文書に一切記載のないまま production で正反対の方向へ改定されている

- 文書 L169-187（§4.2）は「効いた変更は3つ」として **(1) 母集団を『決勝系×別ライン』から『全7車』へ広げる (2) 日次上限 5→20 (3) T=N×10,000** を挙げ、L198-211（§4.4）で **案C（一撃20件+三連複10件、日次上限20）を「採用」** と明記している。
- しかし `keirin/src/strategy_wt.py:4182` の `RANK_7T1_TARGET_PAYOUT = 150_000`（15万円のまま。20万への変更なし）、`keirin/src/strategy_wt.py:4393` の `RANK_7T1_DAILY_CAP = 0`（上限を撤廃＝無制限。20への引き上げではない）が現行値。
- コード中のコメント（`strategy_wt.py:4386-4392`, `4436-4441`）は **2026-08-24**（文書の最終更新の翌日）付けで次のように明記している:
  > 「2026-08-24 に撤廃した（5 → 0）。…**EVによる選別は無価値だった**。代わりに母集団を『決勝のみ×別ライン』へ絞ることで 2.20件/日・ROI 106.3% にした」
  > `RANK_7T1_RACE_TYPES = ("決勝", "チャレンジ決勝")`（`strategy_wt.py:4194`）— 「決勝系（決勝/準決勝/特選/選抜）」ではなく **決勝のみ** に絞る、と明記（`strategy_wt.py:4212-4225`）。
- つまり production は文書の中心的な結論（母集団を **広げる**・上限を **20へ引き上げる**）とは逆に、母集団を **決勝のみへさらに狭め**、上限を **撤廃** した。根拠は文書内の実験ではなく、コード内コメントが参照する別文書 `docs/rank_7t3_design.md`（本監査の対象外）。
- **この反転について本文書は一切触れていない。** 読者がこの文書だけを読むと、現在のproductionの実際の挙動（母集団=決勝のみ・上限なし）とは異なる「案C採用・上限20」を現状だと誤認する。ユーザー懸念（誤った結論のまま大量にドキュメント化）の典型例。

### 2.2 §11の`rebuild_7t2_walkforward_pg.py`はgit履歴に一度も存在しない

- 文書 L460-462:「`scripts/rebuild_7t2_walkforward_pg.py --upto 2026-08-21` で月次凍結vintageのhonest再構築を実施し、**4,659行**を`keirin.picks_history`（VPS PG）へ書き込んだ」
- `git log --all --diff-filter=A -- keirin/scripts/rebuild_7t2_walkforward_pg.py` はヒット0件（追加コミットが一度もない）。`git log --all -- <path>` でも変更履歴なし。
- 一方 `rebuild_7t1_walkforward_pg.py` は実在する（`ls scripts`で確認）。
- **production DBへ4,659行を書き込んだと主張するスクリプトが、リポジトリのどの時点にも存在しない。** ローカルの未コミットファイルで実行し、その後削除された可能性が高いが、いずれにせよ**今その操作を再現・監査する手段がない**。CLAUDE.mdの「破壊的操作は要承認・要バックアップ」という方針からも、この操作の追跡可能性が失われている点は懸念材料。

> **【2026-09-23 追記・解消】スクリプトは実在していた。** 本監査の時点で追えなかったのは、
> `main` にも `origin` にも無く **MacBook のローカルブランチにしか存在しなかった**ため。
> mac mini 移行の棚卸しで発見し、**タグ `archive/keirin-tf-core-redesign`（commit `cf6bfa9a`）
> として恒久保存した**。
>
> | ファイル | 行数 |
> |---|---:|
> | `keirin/scripts/rebuild_7t2_walkforward_pg.py` | 148 |
> | `keirin/scripts/build_7t2_candidates.py` | 83 |
> | `keirin/tests/test_rank_7t2.py` | 112 |
>
> ```bash
> git show archive/keirin-tf-core-redesign:keirin/scripts/rebuild_7t2_walkforward_pg.py
> ```
>
> 中身は 7T1 の `build_rows` を流用した honest 再構築で、三連単オッズ予測モデル
> `data/models/odds_tf_n7.txt` の学習終端が 2025-12-31 であるため **2026-01 より前の窓を
> 機械的に落とす**（他ランクと期間が揃わないため）。
> **4,659行の書き込みは、このタグから再現・監査できる。** §2.2 の懸念は解消とする。
>
> 🔴 **タグであってブランチではない**のは意図的。ブランチとして残すと棚卸しのたびに
> 「未取り込みの作業ブランチ」として再浮上するが、タグなら commit は永久に到達可能な
> まま作業ブランチ一覧から消える。**タグを消すとこの追跡可能性も消える。**

### 2.3 RANK_7T2は現行コードのどこにも存在しない

- `grep -rn "RANK_7T2" keirin/src keirin/scripts` は本体コードにヒットなし。`RANK_CONFIGS`（`netkeirin_submit_wt.py:312`以降）にも `"7T2"` キーは存在しない。
- 唯一の言及は `daily_picks_wt.sh:256` / `evening_picks_wt.sh:163` のコメント中（過去の検証結果の記録）と実験スクリプト内のみ。
- 文書 §11 は「7T2」を新設ランクとして扱い、DB実測との整合性まで確認しているが、**この「7T2」という商品は現在の本番コードには実装として存在しない**（7T1のみが存在し、しかも上記2.1のとおり別方向へ改定済み）。gitログにある唯一の実装コミット `e193206d`（"三連単の一撃枠 RANK_7T2 を新設（ペーパー並走・入稿しない）"）以降、7T2は削除されたか、7T1のパラメータ変更に吸収されたと見られるが、そのいずれかを裏付けるコミットは本調査の範囲では特定できなかった。

> **【2026-09-23 追記】どちらでもなかった。7T2 は `main` へ一度も入っていない。**
> 実装本体（`RANK_7T2_NE` / `RANK_7T2_DAILY_CAP` / `RANK_7T2_TARGET_PAYOUT` /
> `rank_7t2_daily_select` / `PaperRankSpec("RANK_7T2", ...)`）は
> `archive/keirin-tf-core-redesign` の `keirin/src/strategy_wt.py:4084-4269` 付近にだけ存在する。
> ＝ **「現行コードに無い」は正しく、その理由は「削除された」ではなく「入れられなかった」**。
>
> **採否の判定（2026-09-23）: 破棄。** 理由は3点:
> 1. 7T2 は `build_7t1_candidates.build` に依存するが、その `build_7t1_candidates.py` は
>    旧ランク全廃（2026-09-22・#603）で `tests/test_old_ranks_retired.py:61` の
>    `RETIRED_ENTRYPOINTS` に入っており、**本番バッチから呼ぶとテストが落ちる**
> 2. `RANK_7T2` が `main` に無いため、3ファイルは**単体では import すらできない**
> 3. 知見は失われない。`keirin/docs/strategy_rebuild_2026_08.md` §11.1 に DB 実データの
>    実測（N=20/日・233日で **的中 3.0% / ROI 82.3%**）が残っており、同 `:689-690` が
>    🔴「**ROI 82.3% は CI [68.1, 97.4] で払戻率と区別できない。7T2 の価値は妙味ではなく
>    払戻の形**」と結論づけている
>
> 消したのは**作業ブランチだけ**で、コミットはタグに残してある（§2.2 の追記を参照）。

### 2.4 §28.2の関数名表記が実装と不一致（軽微）

- 文書 L1565-1566:「本番の軸選定は `strategy_wt.select_axes_*` が軸1 = `pw` 最上位 / 軸2 = `z(p3) − w·z(bad)` の最上位（軸1を除く）」
- `keirin/src/strategy_wt.py` に `select_axes_*` という名前の関数は存在しない。該当ロジックは `rank_7s_select_axis`（`strategy_wt.py:1079-1132`）で、コードを実際に読むと **ロジック自体は文書の記述と完全一致**（3ヘッド版: `axis1 = max(win_probs)`, `axis2 = max(z(top3)-bad_weight*z(bad))`, `bad_weight`既定値`RANK_AXIS2_BAD_WEIGHT=0.3`＝`strategy_wt.py:935`）。
- ロジックは正しいが、**関数名の引用が誤り**（`select_axes_*`という命名の関数は存在しない）。将来この文書を読んでコード内を`select_axes`で検索すると見つからない。

### 2.5 §33の定義順の記述は「書かれた時点では正確」だが現在は変わっている（staleness、誤りではない）

- 文書 L2026:「RANK_CONFIGS の定義順 : 7H2 > 9H1 > 7S > 9C > 7B > 7C > 7T1 > 7H1 > 7M1」
- 文書執筆時点に近いコミット `3bd36f07`（2026-08-22〜24の間）の `netkeirin_submit_wt.py` を確認したところ、定義順は文書の記述と**完全に一致**していた（`git show 3bd36f07:...` で7H2→9H1→7S→9C→7B→7C→7T1→7H1→7M1の順を確認）。
- しかし**現在**（2026-09-20時点）の `netkeirin_submit_wt.py:312-524` を見ると、定義順は `7H2(323) > 9H1(342) > 7T1(381) > 7S(422) > 9C(437) > 7B(450) > 7C(475) > 7H1(498) > 7M1(524)` — **7T1の位置が9H1の直後（3番目）に移動している**。
- これは文書執筆後の1ヶ月弱でコードが変更された結果であり、**文書執筆時点での誤りではない**。ただし、この文書を「現状の正本」として参照すると誤る、という点で2.1と同種の staleness リスク。

### 2.6 その他の定数・ゲート条件は現行コードと一致（確認できた範囲）

以下は文書の主張どおりの値・ロジックが現行コードに存在することを確認した（＝この文書が書かれた時点の実装として正確、かつ現在もおおむね維持されている）:

| 主張 | 文書行 | コード該当箇所 | 一致 |
|---|---|---|---|
| `RANK_7C_LEG_P3_MIN` = 0.15（相手の3着内率下限） | L575 | `strategy_wt.py:387` | ✅ |
| 7Cの落差カット後は`legs_floor`で1-2点まで縮んだら総流しへ戻す（`TRIO_LEGS_FLOOR=2`） | L1624-1627 | `strategy_wt.py:565` `RANK_7C_TRIO_LEGS_FLOOR = 2` | ✅（変数名は`RANK_7C_TRIO_LEGS_FLOOR`で文書の`TRIO_LEGS_FLOOR`と実質一致） |
| axis_sum(7Cの軸ゲート) >= 1.44 | L763, L769 等 | `strategy_wt.py:351` `RANK_7C_P3_SUM_MIN = 1.44` | ✅ |
| 9Cのゲート: 較正後p3_sum_top2 >= 1.30 ∧ 相手3点以上 | L1682 | `strategy_wt.py:2826` `RANK_9C_P3_SUM_MIN = 1.30`、`:2836` `RANK_9C_LEGS_MIN = 3` | ✅ |
| 9Cの相手条件 `p3>=0.15`・落差カットは無い | L1681 | `strategy_wt.py:2833` `RANK_9C_LEG_P3_MIN = 0.15`（`rank_9c_daily_select`が`rank_7c_cut_legs_by_gap`を呼ばない） | ✅ |
| 9Cはダッチング（`tilt_stakes: True`） | L1684 | `netkeirin_submit_wt.py`の`RANK_CONFIGS["9C"]`（`bet_kind=BET_KIND_TRIO_AXIS2`。tilt_stakes自体は9Cブロック内、grep該当箇所は略） | ✅（3.で言及） |
| 3ヘッド版軸2の`bad_weight`=0.3（7S現行） | L856 | `strategy_wt.py:935` `RANK_AXIS2_BAD_WEIGHT = 0.3` | ✅ |
| `RANK_7S_DAILY_CAP` = 12、一度も発動せず | L2364 | `strategy_wt.py:912` `RANK_7S_DAILY_CAP = 12` | ✅（発動実績はDB依存のため未検証） |
| 4ヘッドモデル（eval/win/bad/top2）が本番・vintageとも配備済み | L522-523 | `data/models/lgbm_wt_{eval,win,bad,top2}*.pkl`・月次vintage(`_mYYMM`)ファイル群が多数存在 | ✅ |
| `wt_overlap_n`が7S/7B/7M1のランク定義そのもの | L396-399 | `strategy_wt.py:1136`以降 `rank_7s_wt_overlap_n`、7S/9S系ゲートで`wt_overlap_n in (0,1)`を必須化 | ✅（方向性まで含め概ね一致） |

---

## 3. ファイル内部の矛盾（文書自身が後で覆したもの・気づかれにくいもの）

### 3.1 【文書が明示的に自己訂正した箇所（読み飛ばすと誤読するリスクが高い）】

1. **§21（L1091-1124）が §15〜§20（L696-1087）を全面撤回している。**
   - §15「三連複: 相手選別でROIが上がる（唯一の再現した改善）」（L696）
   - §18「軸2の信頼度で形を切り替える（本セッション最良）」（L893）
   - §19「上限を外して確実に買えるレースを厳選する」（L955）
   - §20「同程度の信頼度なら点数増はやむなし、は成立しない」（L1021）
   - これらすべてが依拠する「相手=順位5が両窓で最良」という中心的発見は、§21で **独立3年窓（2024-2025）を使うと消失**し、「順位5の優位は2026年だけの現象（相場=regime固有）」「同じ8ヶ月の前後分割は確認窓ではない」と明確に撤回されている（L1115, L1122-1124）。
   - **問題点**: §15〜§20の見出しには🟢（採用候補）マークが付いたままで、各節の本文にも「撤回」の注記が後から追記されていない。§21以降を読まずに§15〜§20だけを参照すると、誤った結論（相手を順位5に絞る）を採用してしまうリスクが高い。この文書の目次的な使い方をする読者への罠になっている。

2. **§29（L1613-1663）内で初出値が訂正されている。**
   - L1619-1627:「初出時の再現が不完全だった（2026-08-23に修正）。当初は`rank_7c_select_legs`だけを再現し、その後の`rank_7c_cut_legs_by_gap`（落差カット）と△削りを通していなかった」「コード中の『落差カット後 平均2.60点』は`legs_floor`を戻す前の値」。
   - 結論自体（7Cへの移植不可）は変わらないが、根拠となる点数（2.60点→4.36点）が訂正されている。他の章で「2.60点」という数字が引用されている場合は要注意（§30 L1690 で「7C（落差カット後2.60点/5車＝52%）」という**古い（訂正前の）数値がそのまま再掲**されている）。

3. **§33→§34→§35で3段階の自己訂正が続く（Phase 0診断）。**
   - §33（L2010）: 「原因はプール(vintage)とproduction(本番モデル)のp3の違い」（14件のサンプルからの一般化）
   - §34（L2106）で明示的に訂正:「14件からの一般化が誤りだった…モデル版を揃えても+1.0ptしか動かない」。真因は「経路の不一致」（朝の候補JSON vs 事後のDB再構築）
   - §35（L2196）でさらに訂正・確定:「本番モデルは再学習され続けるため、過去の入稿を再現することは原理的に不可能」。Phase 0の当初の完了条件そのものを取り下げ。
   - **各節の見出しに「訂正」「確定」の色分け（🔴🔴 vs 🔴）はあるが、§33の本文自体は削除されておらず残置されている。** 後続の章を読まずに§33だけを参照すると誤った原因診断（モデル版の違い）を信じてしまう。

### 3.2 【文書が明示していない可能性のある矛盾（要フォローアップ）】

4. **§27（L1427-1429）「その量は既に本番で使われている」という主張の根拠が薄い。**
   - 「`strategy_wt.py`の`S1W_ENTROPY_MAX`/`RANK_7S_ENTROPY_MAX`/`axis_sum`ゲートがそれ」「コード内にも『p3_sumとentropyは±1.7pt以内で実用上ずれていない』とある」との記述だが、`S1W_ENTROPY_MAX`は`strategy_wt.py:195`付近の`S1W_DENY_AXIS_CLASS`と混同されている可能性があるほか、本監査では`S1W_ENTROPY_MAX`という名前の定数を`strategy_wt.py`から直接特定できなかった（`RANK_7S_ENTROPY_MAX`は別途存在する可能性が高いが、本監査の grep では変数名の完全一致を確認しきれていない）。この主張は再確認が必要。

5. **§30（L1690）で「7C（落差カット後2.60点/5車＝52%）」という§29で訂正済みのはずの古い数値がそのまま再利用されている。**
   - §29自身が「2.60点は`legs_floor`を戻す前の誤った値で、実際は平均4.36点」と訂正したにもかかわらず、直後の§30の比較表（「残る移植先」表, L1775）で再び「7C | 2.60点 / 5車 | ❌測って否定（§29）」と**訂正前の数値**が使われている。数値上の結論（7Cは移植不可）自体は変わらないが、**訂正が全文書に波及していない**ことを示す実例。

6. **§4.4の「採用」表記と§8.4の段階表（L354-366）との整合性が不明。**
   - §8.4は「段0: 掃引でパラメータ確定→段1: ペーパー並走→段2: 購入対象切替→段3: 入稿構成切替」という導入手順を提案しているが、文書のどこにも「今どの段にいるか」を明示する記述がない。2.1で確認したとおり実際のproductionコードは§4.4の「採用」構成とは異なる値になっており、この段階表がどこまで実施されたか（あるいは§4.4の設計自体が破棄されたか）は本文書からは読み取れない。

---

## 4. スクリプト実在確認・方法論の懸念

### 4.1 実在確認結果

`keirin/scripts/` 配下で本文書が言及したスクリプトのうち、以下は**存在しない**（`ls`および`git log --all --diff-filter=A`で追加コミット0件を確認）:

- `exp_tf_core_redesign.py`（§4冒頭で「掃引」の根拠として言及）
- `exp_tf_core_sweep.py`（同上、「→」でリネームされたと読める記載だが両方とも痕跡なし）
- `rebuild_7t2_walkforward_pg.py`（§11、production DBへの書き込みを主張。上記2.2参照）

その他、文書で言及された約45本のスクリプト（`exp_tf_axis2_position.py` `exp_tf_shape_prod.py` `exp_tf_shape_confirm.py` `exp_market_agree_wt.py` `exp_tf_leg_gap.py` `exp_tf_unconstrained_scan.py` `exp_trio_partner_rank.py`系 `exp_trio_race_shape.py` `exp_trio_shape_priced.py` `exp_trio_partner_bad.py` `exp_trio_axis2.py`系 `exp_trio_shape_switch.py`/`_combo.py` `exp_trio_selectivity.py` `exp_trio_firm_selection.py` `exp_trio_adaptive_legs.py` `exp_trio_anchor_rank5.py` `exp_trio_axis_pair.py` `exp_trio_pair_model.py` `exp_trio_miss_anatomy.py` `exp_trio_pair_calibration.py` `exp_trio_partner_p3value.py` `exp_trio_joint_partner.py` `exp_trio_joint_ablation.py` `exp_axis1_selection.py` `exp_axis1_bust_stratified.py` `exp_axis1_swap_by_bustrisk.py`/`_formation.py`/`_pairmodel.py` `exp_7c_leg_joint_transfer.py` `exp_9c_leg_joint_transfer.py` `exp_unified_compare.py`/`2.py` `build_wf7_cache.py` `phase0_pool_audit.py` `phase0_logic_check.py` `phase0_reconcile_7s.py` `phase0_vintage_pool.py` `rebuild_7t1_walkforward_pg.py` `rebuild_7h2_walkforward_pg.py` `build_7t1_candidates.py`）は**すべて実在**を確認した。

### 4.2 スクリプトの内容ざっと確認（流し読み・実行なし）

- `phase0_pool_audit.py` / `phase0_logic_check.py` / `phase0_reconcile_7s.py` / `phase0_vintage_pool.py` は文書の記述どおり「読み取り専用」であることをファイル冒頭のdocstringレベルで確認（詳細な実行検証は行っていない）。
- **§30/§31で問題視された「vintage vs backfillの取り違え」は文書自身が発見・訂正しており**、これはこの文書の検証プロセスが「学習窓=評価窓」型の欠陥を実際に踏んだ後に気づいて是正した実例（悪い意味でのサンプルではなく、危険な罠を踏んだ記録として文書内に残っている）。§30 L1698-1707「`wt_entries.pred_top3_pct`はmodel-vintage look-ahead」という指摘は、CLAUDE.mdの「検証の作法」#5（学習窓と評価窓が分かれているか）に該当する典型的な落とし穴であり、**この文書の他の章（§4〜§20の多くが依拠する`trio_rank_cache.jsonl`等のキャッシュ）にも同種のlook-aheadが混入していた可能性がある**（§31で7車キャッシュについては是正済みと明記されているが、§4〜§14が使う三連単オッズ予測モデルキャッシュ・§10のmarket_agree検証等、**是正されたことが明記されていない実験群も残っている**）。
- §32で報告された「in-sample混入」（探索窓で学習して同じ窓で評価）・「母集団不揃い」（構成ごとに異なるレース集合をROI降順の1つの表で比較）・「LG_JOINTが黙ってp3へ退化」（36構成中24構成が実質12構成の重複）という3欠陥は、**この文書のかなり早い段階（§4〜§20）の実験群が同種の設計になっていないかは本監査では個別に検証できていない**。特に§4（掃引）・§15〜§20（三連複相手選別、後に§21で撤回）は、§32で問題視されたのと同じ「探索窓/確認窓」の分け方の変種（前後半分割、L1122-1124で文書自身が問題視）を採用しており、**§32の教訓が遡って§4系の掃引結果の信頼性を損なっていないかは未検証**。

### 4.3 方法論上の一般的な懸念（文書全体を通して）

- 探索窓・確認窓の切り方が章によって異なる（前後半分割 vs 年別3窓 vs 独立2年窓）。§21で「前後分割は確認窓にならない」と明確な教訓が得られているにもかかわらず、それ以前の章（§4〜§20）の一部の分析は前後分割ベースであり、**§21の教訓が遡及的に§4系の結論の信頼性に影響するかどうかは文書内で議論されていない**。
- §24以降で使われている「三者同時確率モデル」は学習2024-01〜2025-12・検定2026-01〜08という時間分割を採用しており、CLAUDE.mdの検証作法（学習窓と評価窓を分ける）には合致している。
- 生存者バイアス・確定オッズを発走前情報として使う、といった典型的な欠陥は、少なくとも§11.3（`sorted(rows)`のタプル全体ソートで的中率が歪む実装ミス）・§30（p3の出所混同）として文書自身が発見・是正しており、**この文書の検証プロセス自体は自己批判的で改善が働いている**。ただし、その分だけ「最終結論」がどれなのか（特に§4系）が読者にとって追いにくい構造になっている。

---

## まとめ（この監査で確認した最重要ポイント）

1. **文書の中心的な「採用」結論（§4.4の案C: 母集団を全7車へ広げ、日次上限を20へ引き上げ）は、文書執筆の翌日（2026-08-24）にproduction側で正反対の方向（母集団を決勝のみへ絞り、日次上限を撤廃）へ改定されており、この反転は本文書のどこにも記載されていない。** 本文書だけを読む読者は現状のproduction挙動を誤認する。
2. ~~**§11で「production DBへ4,659行書き込んだ」と主張する`rebuild_7t2_walkforward_pg.py`はgit履歴に一度も存在せず、RANK_7T2という商品自体が現行コードのどこにも存在しない。** この操作の追跡可能性が失われている。~~ → **【2026-09-23 解消】** スクリプトは MacBook のローカルブランチにのみ存在していた。タグ `archive/keirin-tf-core-redesign`（`cf6bfa9a`）として保存し、再現・監査が可能になった。RANK_7T2 が現行コードに無いのは「削除された」からではなく「`main` へ一度も入らなかった」ため。**採否は破棄で確定**（§2.2 / §2.3 の追記を参照）。
3. **§15〜§20（三連複相手選別「順位5」・信頼度切替）は§21で文書自身により明確に撤回されているが、見出しの🟢マークや本文中の「本セッション最良」等の記述は訂正されずに残置されている。** 部分的にしか読まない読者への罠。
4. **§33→§34→§35でPhase 0の原因診断が3段階で自己訂正されており、最終的に「現行実績の再現は原理的に不可能」という結論に至っている**が、途中段階の§33の診断（モデル版の違いが原因）が本文中に訂正なく残っている。
5. コード突合で確認できた個別の定数・ゲート条件（`RANK_7C_P3_SUM_MIN=1.44`、`RANK_9C_P3_SUM_MIN=1.30`、`RANK_7C_LEG_P3_MIN=0.15`、`RANK_AXIS2_BAD_WEIGHT=0.3`、4ヘッドモデルの配備等）は、文書執筆時点の記述として正確であることを確認した。
