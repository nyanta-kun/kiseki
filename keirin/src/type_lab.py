"""型ラボ — レースの型判定と、型ごとの買い目・配分の**唯一の正本**（2026-08-27 新設）。

## なにをするモジュールか

レースを A〜F の6型に分け、型ごとに決めた構成で三連単／三連複の買い目と賭け金を作る。
**ペーパー検証（過去）と実地検証（当日）の両方がこの1ファイルを呼ぶ**ので、
「検証した商品と違うものを売る」ことが構造的に起きない。

設計と全実測: `keirin/docs/type_lab/SUMMARY.md`（+ `type_a.md`〜`type_f.md`）。
🔴 **既存ランク（7C/7S/7M1/7T1…）には一切触らない。** 別テーブル
`keirin.type_lab_picks` へ書き、既存の一覧・統計・入稿には出さない。

## 6層 → 型

    ① 軸の堅さ  axis_sum = 3着内率 上位2車の合計（境界 1.44 = RANK_7C_P3_SUM_MIN）→ 的中率
    ②〜⑥ 荒れ度 arare（下記の加算）→ 配当

      arare = +1 if 指数1位のライン人数==2 else (-1 if >=4 else 0)      # ③ ライン構成
            + (-1 if 先頭の遅れ率 >= 11% else +1)                       # ④ ラインの維持
            + (+2 if 先頭の脚質 == '追' else 0)                          # ⑥a 並びの妥当性
            + (開催日目 - 2)                                            # ⑤ 開催日目
            + (+1 if 番手の競走得点 > 先頭の競走得点 else 0)              # ⑥c 並びの妥当性

    型 = (堅い: A arare<=-1 / B ==0 / C >=1) / (混戦: D <=-1 / E ==0 / F >=1)

## 型ごとの構成（`PLANS`）

🔴 **順序の入れ替えを何点買うかは型で逆になる**（SUMMARY 追補 B）。
   鉄板(A)は着順まで読めるので1順序だけ、大混戦(F)は3車が当たっても順序が読めないので
   6順列すべてが最良（確認窓 ROI 66.8% → 79.2%）。
🔴 **配分はダッチ（∝1/予測オッズ）か信頼度傾斜**。均等は全型でガミ 12〜66%。
   信頼度傾斜の床 m は「当たったら最低 m 倍」の安全余裕で、**予測が中央 0.87 倍に下振れる
   （勝者の呪い）ので m=1.0 では実ガミが消えない**。既定は 1.3。

## 車数（2026-08-28 に 9車を追加）

**型判定は車数で分けない**（同じ型なら車数が違ってもそろい率が揃い、プールしても
A 67.16% → F 38.37% と単調。境界 1.44 も据え置き）。分かれるのは**売るもの**だけで、
それは `plans_for()` が車数と種別を見て決める。7車の挙動は変えていない。
"""
from __future__ import annotations

import itertools
import math
from dataclasses import dataclass, replace
from typing import Mapping, Sequence
from .unit_distribution import FLOOR_THEN_LARGEST_REMAINDER, distribute_units
from .stake_allocation import MIN_MEAN_PAYOUT

BUDGET = 10_000
UNIT = 100

#: 軸の堅さの境界。**値は 7C/7M1 の `RANK_7C_P3_SUM_MIN` と同じ**
#: （テストで一致を固定してある）。
#:
#: 🔴 **ただし「同じ量」ではない。** 7C は `_gate_p3_sum`＝**較正後**の
#:    `p3_sum_top2_cal` を 1.44 と比べるが、ここは `lgbm_wt_eval` の**生の p3** の
#:    上位2合計を比べている。`keirin_p3_calibration` は決勝 +2.84pt・GII +2.10pt の
#:    過大評価を直すもので、まさに「p3 の絶対値を見るゲート」のために作られている。
#:
#: 🟢 **測ったうえで生のままにしてある**（2026-08-28）。較正へ切り替えると
#:    動くのは全体の **2.4%（845R）** だけで、その帯の二軸そろい率は 49.52%
#:    ＝堅い側 65.00% と混戦側 40.08% の間。移しても**分離度は +24.25pt →
#:    +24.43pt（+0.18pt）**しか変わらない:
#:
#:      セグメント別の境界帯そろい率  決勝 46.58% / 準決勝 48.96% / その他 50.60%
#:      堅い−混戦の分離              決勝 +23.17pt / 準決勝 +19.32pt / その他 +25.28pt
#:
#:    ＝ 7C で見つかった病理（「決勝が最も通るのに最も効いていない」）は
#:    **型ラボでは再現しない**。境界は決勝でもほぼ同じだけ効いている。
#:    20か月の検証はすべて生の p3 で出しているので、+0.18pt のために作り直さない。
#: ⚠️ 較正へ切り替えるなら**この定数だけでなく検証窓ごと引き直すこと**。
AXIS_SUM_FIRM = 1.44
#: 先頭の遅れ率の中央値（2025-01〜2026-08 の 7車 実測）。
BEHIND_MID = 11.0
#: 信頼度傾斜の既定の床（＝当たったら最低この倍率を予測ベースで確保する）。
DEFAULT_FLOOR_MULT = 1.3

#: **当たれば最低これだけは返す**という床の倍率（2026-09-05・ユーザー要望
#: 「最低2倍を担保しながら、買い目に対しての信頼度を得点化し、信頼度が高いところから
#: 払い戻しが多くなる様に傾斜配分する」）。
#:
#: 🟢 **入稿ゲートと同じ制約なので、通る買い目なら必ず組める。** 床を全点へ置くには
#:    `予算 × mult / オッズ` の合計が予算以下、すなわち **Σ(1/オッズ) <= 1/mult**。
#:    mult=2.0 は `Σ <= 0.5` で、入稿ゲート（平均想定払戻 2万円超）とまったく同じ式。
#:
#: 🔴 **これは配分の話であって買い目の話ではない。** 買う目は1点も変えない。
#:    変わるのは「どの点にいくら置くか」だけ。
#:
#: 実測（7車・hit系6プラン・入稿ゲート通過分・本番のモデル確率と予測オッズ・
#: 探索2025 / 確認2026）:
#:
#:      案                    ROI          2万円未満の点があるレース
#:      現行                 76.0 / 80.6   22.1% / 21.8%
#:      **conf 最低2.0倍**   **76.1 / 80.7**   **8.5% / 8.3%**
#:
#:    ＝ **収支は動かさずに「当たったのに2万円に届かない」を 1/3 以下へ**。
#:    的中率・表示的中・10万+ はいずれもほぼ不変。プラン別の ΔROI は
#:    −1.0〜+1.7pt でばらつくが大勢は不変（最大の改善は `F_hit` の
#:    61.4/60.8% → 22.5/22.4%、次いで `A_hit` 21.8/24.0% → 9.9/10.9%）。
#:
#: 🟢 **傾斜も実際に付く。** 1レース内の想定払戻のひらき（最高÷最低・確認窓）は
#:    `B_hit` 1.18→1.83 / `C_hit` 1.19→1.59 / `E_hit` 1.20→1.87 / `D_hit` 1.03→1.21。
#:    床を置いたぶん残りが確率比例で乗るので、**確信のある点ほど払戻が厚い**。
#:    逆に元から `conf` の2つは床が予算を食うのでひらきは縮む
#:    （`A_hit` 1.45→1.22 / `F_hit` 2.17→1.60）。狙いは「最低額の底上げ」なので想定内。
#:
#: ⚠️ **穴狙い・看板（`A_ana` / `F_sign` / `F_pay`）には掛けない。** `A_ana` は
#:    実測で **ROI 78.6 → 65.7（−12.9pt）** と大きく悪化する。想定払戻25万円級の
#:    商品に全点2万円の床を置くと、高配当の目への傾斜が床に食われて崩れる。
#:    そもそもこの3つは「2万円未満の点」が元から 0.0〜0.3% でこの手当てが要らない。
#: ⚠️ `A_trio`（三連複2点）も対象外（2万未満 1.6%・元から dutch で均等）。
MIN_PAYOUT_MULT = 2.0

#: 型A を「穴狙い（軸1が飛ぶ側）」で売る境界。**1着率エントロピーの上位10%**
#: （探索窓 2025-01-01〜12-31 の 7車 型A・3,338R の p90）。
#:
#: 🔴 **なぜ pw_ent か**（`keirin/docs/type_lab/type_a_upset_2026_08_31.md` §7・§12）:
#:    「軸1が3着にも入らない」の事前検出は `pw_ent` 単一で **評価窓 AUC 0.680**。
#:    11特徴の LightGBM は 0.656 で単一量に負ける＝学習すべき構造が無い。
#:    上位10% で軸崩壊率は 9.6% → 19.7%（**2.04倍**）。
#: 🔴 **上位20% 以上へ広げないこと。** 確認窓 ROI が 80.8% → 76.1% → 71.3%
#:    （上位10% → 20% → 33%）と落ち始める。上位10% なら
#:    「②のみ」比で表示的中 −1.7〜−3.0pt を払って看板が型A比 2.8〜4.2倍、
#:    **ROI は両窓とも動かない**（探索 75.0 / 確認 83.5）。
#: 🔴 **7車の分位なので 7車にだけ掛ける**（`AXIS_GATE_N_ENTRIES` と同じ理由）。
#:    エントロピーの上限は車数で変わる（ln7=1.946 / ln9=2.197）ので、
#:    9車に当てると「上位10%」ではなく**絶対値で切る**ことになる。
ANA_PW_ENT_MIN = 1.4076
#: ── 看板枠（高額払戻を作る買い方）─────────────────────────────────────
#:
#: 🔴 **看板の本数は「点の選び方」では動かない。** 市場効率下では
#:    P(払戻 >= X) = 帯ROI × 予算/X という恒等式に支配されるので、券種・点数・配分・
#:    並べ方に依存しない（`keirin_highpay_payout_ceiling_2026_08_06`）。
#:    本番検証台（`/tmp/tf20_board_vint.npz`・全210点・vintage）で 2026-08-31 に
#:    選び方を7通り比べたが、10万+/日 は **1.67〜1.94（探索）/ 1.71〜1.86（確認）** で
#:    ほぼ一定・ROI 差は**全案とも 95%CI が 0 を跨ぐ**（探索で有意に見えた「確率順」は
#:    確認窓で符号が反転する）。
#:
#: 🟢 **動くのは3つだけ**: ①看板枠を置く型の数（＝枠/日）②計画払戻 `SIGNBOARD_TARGET`
#:    ③1レースの予算。①②は下の定数、③は `BUDGET`。
#:
#: 実測（7車・確定払戻で採点・現行 = 表示的中 20.74% / ROI 76.7% / 10万+ 0.39件/日）:
#:
#:      看板枠を置く型   枠/日   表示的中   ROI    10万+/日   30万+/日
#:      なし（現行）      0.0   20.74%   76.7%    0.39      0.02
#:      F                14.2   19.84%   76.8%    0.55      0.02
#:      F+C              23.1   16.53%   74.8%    0.85      0.03
#:      F+C+E            27.5   15.26%   73.8%    0.98      0.05
#:      F+C+E+B          33.5   12.40%   72.1%    1.18      0.06
#:      全型              48.2    5.14%   70.7%    1.67      0.07
#:
#: 🔴 **1レース1万円では看板 5件/日 は作れない**（上限 1.7件/日・全レースを看板枠に
#:    しても）。恒等式どおり本数は予算に比例するので、5件/日 には 1レース約3万円
#:    （投資 約145万円/日）が要る。目標を動かすときはここを読むこと。
#: 🔴 **7車でしか測っていない。** 9車（型F が母集団の55〜59%・ROI 65%）は
#:    有望だが未測定なので `SIGNBOARD_N_ENTRIES` で 7車に限定してある。
#: 🔴🔴 **2026-08-31 夕に空へ戻した（ユーザー判断）。** 同日朝に `("F",)` で入れたが、
#:    実売3日の内訳を見ると**優先すべきは看板ではなく的中体験**だった:
#:
#:      型ラボ実売 8/29-31  98件 ROI 44.3% ＝ 参照分布の 2.9%点（34回に1回）
#:      内訳  F_pay 37件で**表示的中 0.00%** ↔ A_hit 81.9% / B_hit 92.0% / A_trio 91.1%
#:
#:    型F は母集団の約30%を占めるのに `F_pay` の表示的中は設計上 8.4%（12件に1件）で、
#:    「毎日当たらない」体感の主因になっていた。**看板枠はそれを 4.5% へさらに下げる**
#:    ので、いま入れる変更ではない。機構と実測は残してあるので、的中体験が戻ってから
#:    `("F",)` などへ戻せる（`docs/type_lab/signboard_slot_2026_08_31.md`）。
#: 🔴🔴 **2026-09-01 に `("F",)` ＋ 種別の絞りで再投入した（ユーザー判断）。**
#:    2026-08-31 夕に空へ戻したのは「的中体験を優先する」ためだったが、翌日
#:    「型F はたまにでも大きく当たることが宣伝効果につながるので現状維持」と方針が示された。
#:    そこで**「大きい当たり」を F_pay ではなく看板枠で作る**。両者は同じものを買うのに
#:    値段が違う（確認窓 2026・ラインナップ全体）:
#:
#:      構成                              件/日   表示的中   10万+/日  30万+/日   ROI
#:      型F 全部 F_pay（〜2026-08-31）      37.84  20.03%    0.338    0.037   78.9
#:      F_hit＋決勝F_pay（看板枠なし）        35.21  25.52%    0.139    0.023   82.8
#:      ＋決勝系＋特選に看板枠（2026-09-01〜09-02）  36.30  22.13%  0.338  0.023  81.7
#:      **＋決勝系＋準決勝系に看板枠（現行）**  35.35  23.10%    0.306    —      82.2
#:      ＋型F 全部に看板枠                  37.84  18.90%    0.556    0.042   81.5
#:
#:    ＝**現行は「今日までと同じ 10万+ 0.338件/日」を保ったまま 表示的中 +2.1pt・ROI +2.8pt**。
#:    探索窓も同じ向き（21.14% / 0.299件 / 75.1 ↔ 18.82% / 0.326件 / 71.3）。
#: 🟢 **看板枠は ROI をほとんど食わない**。枠の量を 0 → 1.2 → 3.0 → 6.1 → 12.1枠/日 と
#:    広げても ROI は 82.8 → 82.0 → 82.1 → 81.7 → 81.5（探索は 74.9〜75.1 でほぼ平ら）。
#:    動くのは 表示的中（25.52 → 25.30 → 24.00 → 22.13 → 18.90%）と 10万+ だけ＝
#:    **純粋な店頭KPIのダイヤル**。量を変えるときは `SIGNBOARD_RACE_TYPES` を動かす。
#: 🔴 対象レースでは看板枠は **100% 組める**（両窓 2,265R / 1,306R で失敗ゼロ）ので、
#:    組めなかったときの代替は用意していない。種別を広げるなら測り直すこと。
SIGNBOARD_TYPES: tuple[str, ...] = ("F",)
#: 看板枠を掛ける**種別**。空なら種別で絞らない（＝その型の全レース）。
#:
#: 🔴 **これが「たまに」の量を決めるダイヤル**（上の表）。決勝系だけなら 1.2枠/日、
#:    決勝系＋特選で 6.1枠/日、外せば 12.1枠/日。
#: 🔴 **「決勝」は完全一致で持つこと。** 部分一致にすると「準決勝」を巻き込む
#:    （CLAUDE.md の既知の罠）。だから列挙する。
#:
#: 🔴🔴 **2026-09-02 に「特選」系を外した（ユーザー判断）。**
#:    発端はユーザー観察「09-01 と 09-02 で的中率に差がある。今日は波乱が多い」。
#:    分解すると、開催日目で型F率が 初日 8.4% → 2日目 28.2% → **最終日 52.6%** と動き、
#:    その最終日に集中する種別が**特選**だった。看板枠 `F_sign` は表示的中が
#:    **設計上 5%**（`F_hit` は 27〜28%）なので、最終日だけ商品の見え方が別物になる。
#:
#:    実データ 2026-09-02: 入稿 43件のうち **12件（28%）が `F_sign`・うち 10件が「特選」**
#:    （採点済 7件は全ハズレ）。09-01 は 8件（準決勝5・決勝2・特選1）。
#:
#:    ラインナップ全体・確認窓 2026-01〜08（探索 2024-07〜2025-12 は括弧）:
#:
#:      看板枠の種別          件/日   全体表示的中   **最終日**   10万+/日      ROI
#:      決勝系+準決勝系+特選   35.35   23.10 (22.79)  19.14 (18.67)  0.306 (0.281)  82.2 (76.8)
#:      **決勝系+準決勝系**    34.70   25.04 (24.62)  24.89 (23.81)  0.194 (0.179)  82.7 (77.3)
#:      決勝系のみ            34.27   26.38 (25.98)  24.89 (23.83)  0.125 (0.104)  82.5 (77.3)
#:      看板枠なし            34.27   26.61 (26.06)  25.64 (24.08)  0.106 (0.080)  83.4 (76.4)
#:
#:    ＝**最終日 +5.8pt（探索 +5.1pt）・全体 +1.9pt・ROI +0.5pt と引き換えに
#:    10万+ を 37% 手放す**。両窓で向きも大きさも一致するが、これは発見ではなく
#:    「表示的中5%の商品を何件並べるか」の**会計恒等式**なので窓に依存しない。
#:    不確かなのは 10万+ の件数の側だけ。
#:
#: 🔴 **最終日を動かすのは「特選」だけ。** 最終日に決勝は1R・準決勝は無いので、
#:    準決勝系を外しても最終日は 1ミリも動かない（動くのは2日目 22.95 → 26.85%）。
#:    準決勝系を残しているのはそのため——2日目の看板を保ったまま最終日だけ戻せる。
#: 🔴 **「看板枠を別の種別へ移す」は効かない**（一般系へ移す案は 確認 20.53% ↔
#:    探索 18.24% で**窓をまたいで符号が反転**する）。`F_sign` の表示的中は
#:    どの種別群でも 4〜6% で変わらないので、置き場所に情報は無く**量だけ**が効く。
#: 🔴 **型F を種別で「買い分ける」根拠は無い**。13腕 × 5種別群 × 両窓で測って
#:    `F_hit` がどの群でも最良で、勝者が入れ替わることは一度もなかった。
#:    ここで種別を使うのは買い方の選択ではなく**枠の量の調整**である。
#: ⚠️ 実測・再現手順: `docs/type_lab/typef_racetype_2026_09_02.md` /
#:    `scripts/exp_type_lab/typef_racetype.py`
SIGNBOARD_RACE_TYPES: frozenset[str] = frozenset({
    "決勝", "チャレンジ決勝",
    "準決勝", "チャレンジ準決勝",
})
#: 計画払戻（ダッチなのでどの点が当たっても最低この額を狙う）。
#:
#: 🔴 **10万ちょうどで組んではいけない。** 予測オッズは確定より高めに出るため
#:    計画10万だと的中の **55%** しか10万に届かない。掃引実測（的中→10万の到達率）:
#:    計画8万 35% / 10万 55% / 12万 71% / **15万 84%** / 20万 94%。
#:    15万を超えると点数が減りすぎて 10万+ の本数自体が減る（1.67 → 1.42件/日）。
#:
#: 🔴 **2026-09-02 に掃き直して「動かさない」と決めた**（特選を外して 10万+ が
#:    0.306 → 0.194件/日 に減ったぶんを、種別を広げずに取り戻せるかの検討）。
#:    ラインナップ全体・看板枠は決勝系＋準決勝系で固定・括弧は件数:
#:
#:      計画払戻   枠の点数  全体表示的中   10万+/日（件）        30万+/日（件）
#:      80,000     4.03    24.90 / 25.60   0.091 (50) / 0.171 (37)  0.004 (2) / 0.014 (3)
#:      100,000    3.51    24.83 / 25.36   0.164 (90) / **0.199 (43)**  0.004 (2) / 0.014 (3)
#:      **150,000**  2.89  24.62 / 25.04   **0.179 (98)** / 0.194 (42)  0.011 (6) / 0.019 (4)
#:      250,000    2.30    24.38 / 24.88   0.138 (76) / 0.171 (37)  0.020 (11) / 0.051 (11)
#:                                          （探索 549日 / 確認 216日）
#:
#:    🔴 **10万+ の最良値が窓で入れ替わる**（探索 150,000 / 確認 100,000）うえ、
#:       件数の差は Poisson の SE（√n ≈ 6〜10）に収まる＝ノイズ。
#:    🔴 **そもそも幅が足りない。** 掃引の全域でも 0.091〜0.199件/日 で、
#:       特選を外して失った 0.112件/日 はどこにも無い。**看板の本数は
#:       「何レースに枠を置くか × 1レースの予算」でしか動かない**（上の節の恒等式）。
#: 🟢 **両窓で一貫するのは2つだけ**: ①表示的中は T に単調減少（24.90→24.38 /
#:    25.60→24.88%）②30万+ は T に単調増加（2→11件 / 3→11件）。
#:    ＝ **10万+ と 30万+ は逆を向く。** KPI をどちらに置くかを先に決めないと回せない。
#:    30万+ へ移すなら 250,000 が候補だが、**11件の観測で決めないこと**。
#: ⚠️ 実測: `docs/type_lab/typef_racetype_2026_09_02.md` 追補 /
#:    `scripts/exp_type_lab/typef_racetype.py target`
SIGNBOARD_TARGET = 150_000
#: 予測オッズの上限。**帯ROI が崩れる超高配当を買わないための蓋**
#: （三連単の帯ROI は 600倍まで 71〜76% だが 600-1200倍 64.5% / 1200倍- 51.6%）。
SIGNBOARD_MAX_ODDS = 600.0
#: 看板枠を掛ける車数。**これ以外には掛けない**（`ANA_N_ENTRIES` と同じ理由で、
#: 9車では測っていない）。
SIGNBOARD_N_ENTRIES = 7

#: 上の境界を作った窓。数字の出どころを行に残す。
ANA_SOURCE_WINDOW = ("2025-01-01", "2025-12-31")
#: 穴狙いを掛ける車数。**これ以外には掛けない**。
ANA_N_ENTRIES = 7


# ─────────────────────────── 上帯（多穴の重ね買い）───────────────────────────
#
# 🔴🔴 **2026-09-04 新設。予算 10,000円のうち 2割を「上帯」へ回す。**
#    商品は増やさない・投資は増やさない・1レース1商品のまま。**下帯（従来の買い目）
#    の賭け金を 8割へ縮め、空いた 2割で高オッズの目を買い足す**（バーベル）。
#
# 発端（ユーザー観察 2026-09-04）:
#   > 100倍を超える配当の的中が無い。100倍を超えるようなレースは、指数上位を軸に
#   > 取る買い目だと外れている。現在の帯の上に多穴狙いと下帯を重ねられないか。
#
#    実地の裏づけ（08-29〜09-03 の入稿 329件）: **28.6% が 100倍以上で決着し的中は
#    3件（3.2%）**。その 94件の **48% は指数1位を含まず**、3車とも指数5位以内なのは
#    32% だけ ＝ **上から点数を増やしても構造的に届かない層**。
#
# 実測（`docs/type_lab/overlay_upper_band_2026_09_04.md` /
#      `scripts/exp_type_lab/overlay_ana.py`・探索 2024-07〜2025-12 / 確認 2026-01〜08）:
#
#     腕                     表示的中(探/確)   10万+/日      100倍的中/日   ROI
#     現行                   24.68 / 24.76%   0.215/0.222   0.240/0.231   78.2/81.7
#     ＋帯100-600倍8点 1割    26.74 / 27.04%   0.208/0.213   0.956/1.000   78.2/81.2
#     ＋看板15万 1割          24.65 / 24.94%   0.313/0.333   0.355/0.343   77.9/81.1
#     **＋両方（下8割）**     26.42 / 26.71%   0.290/0.338   1.100/1.139   77.5/80.4
#
#     Δ表示的中の対応ブートストラップ（探索 / 確認）は **[+1.49,+2.02] / [+1.53,+2.33]pt
#     で両窓とも 0 を上回る**。ΔROI は両窓とも 0 を跨ぐ（点推定 −0.5〜−1.3pt）。
#
# 🔴🔴 **効いているのは「帯」であって「点数」ではない。** 同じ 1,000円・8点を
#    帯なしの次点8点へ置く対照では **的中が 25→46% に跳ねるのに表示的中は +0.3pt**
#    （ガミ 46%）。薄いスライスの払戻は `予算 ÷ Σ(1/オッズ)` なので、
#    **投資 10,000円を上回れるのは高オッズの目だけ**。帯30-100倍でも +0.3〜0.6pt 止まり。
#
# 🔴 **1日数レースに絞らない**（全商品へ薄く重ねるのが最効率）。レース選別には情報が無く
#    （`pw_ent` 上位で選んでも 10万+ で無作為対照に勝てない・両窓 3〜12/20）、
#    これは `P(払戻>=X) = 帯ROI × 予算/X` の恒等式の帰結
#    （`docs/type_lab/axis_bust_conditional_2026_09_03.md` §4）。
#    **表示的中 1pt あたりに買える 10万+/日**: 薄く重ねる **0.65** ↔ 別商品として
#    重ねる 0.071 ↔ 差し替え 0.062 ↔ `SIGNBOARD_RACE_TYPES` に特選を足す 0.058。
#
# 🔴 **入稿ゲートは下帯だけに掛ける。** 混成商品の平均想定払戻は 100-600倍の目に
#    引き上げられるので、素で掛けると**いま落ちているレースが静かに通り母集団が変わる**。
#    `gate_mean_payout_floor` が閾値を下帯の予算比で割り戻す（＝今日と同じ母集団）。
# 🔴 **三連複プラン（`D_hit` / `A_trio`）には掛けない。** 1商品に2券種は混ぜられない。
# 🔴 **7車だけ。** 9車では測っていない（`ANA_N_ENTRIES` / `SIGNBOARD_N_ENTRIES` と同じ理由）。
# ⚠️ 代償は **払戻中央 −20%（26,000 → 20,910円）とガミ率 1.8 → 8.2%**。


@dataclass(frozen=True)
class UpperBand:
    """上帯ひと切れ。`kind` が行の `legs[].role` に出る。

    structure='prob_top' … 予測オッズ [min_odds, max_odds] から確率上位 max_legs 点
    structure='signboard' … Σ(1/予測オッズ) <= 予算/target の枠へ確率順に詰める
    structure='perm_fill' … **下帯で同じ3車の並びを `min_bought` 以上買っている**
                            トリオの、まだ買っていない順列を確率上位 max_legs 点
    """
    kind: str
    budget: int
    structure: str
    min_odds: float = 0.0
    max_odds: float = 0.0
    max_legs: int = 0
    target: int = 0
    #: `perm_fill` 用。下帯に何通りの並びがあるトリオを対象にするか。
    min_bought: int = 2
    note: str = ""


#: 上帯の設計と実測の記録（**いまは無効**・下の `UPPER_BANDS` が空）。
#: 合計は必ず `BUDGET` 未満（残りが下帯の予算）。
#:
#: 🔴🔴 **2026-09-04 に中身を「並び違いの補完」へ替えた**（ユーザー判断）。
#:    発端の指摘:
#:      > 100倍ちょっとに100円ずつ置くのは目が増えて的中率は上がるかもだが、
#:      > **ガミも多く当たった感が少ない**。通常買い目のうち**2つ以上同じ組み合わせの
#:      > 並び違いがあるもの**を補完してダッチ配分するほうが良さそう。
#:      > **惜しく外してしまっているものを拾えれば**。
#:
#:    指摘は算数として正しかった。旧構成は 1,000円を8点＝**実質100円均等**
#:    （100円が最小単位なのでダッチが効かない）で、120倍が当たっても 12,000円。
#:    実測でも上帯の的中の **55.9 / 59.5% が2万円未満**だった。
#:
#: 実測（`E_hit`+`F_hit` に適用・探索 2024-07〜2025-12 / 確認 2026-01〜08・
#: `docs/type_lab/overlay_upper_band_2026_09_04.md` §9.5）:
#:
#:   上帯2,000円の使い方        表示的中        ガミ%      上帯払戻中央   2万未満     10万+/日
#:   無し（下帯だけ）           24.68 / 24.76%  3.0 / 1.8  —             —          0.215/0.222
#:   旧 band8点1割+看板1割      25.04 / 25.19%  5.5 / 4.4  18,090/16,060 55.9/59.5% 0.246/0.259
#:   **perm(>=2) 上位4点 2割**  25.49 / 25.64%  6.4 / 5.1  15,500/16,255 59.8/60.4% 0.209/0.222
#:   perm(>=2) 上位6点 2割      25.63 / 25.81%  7.5 / 6.1  12,600/13,200 72.2/68.9% 0.209/0.222
#:   perm(>=2) 全部 2割         25.65 / 25.92%  9.2 / 7.5  10,230/10,610 82.4/76.9% 0.209/0.222
#:
#: 🟢 **並び違いは「当たる回数」が段違い**（上帯の的中 0.71件/日 ↔ 旧 band 0.51件/日、
#:    全部なら 1.12件/日）。モデルがその3車を推している証拠がある目だけを拾うので、
#:    「3車は合っていたのに順序で外した」を回収できる。
#: 🔴 **`max_legs` は「拾う数」と「1回の大きさ」のダイヤル**。増やすほど当たるが
#:    払戻は小さくなり、ガミが増える（上表）。4点にしたのは、ガミを旧構成と同水準
#:    （+0.7pt）に保ったまま表示的中の上がり分を大半（+0.88 / 最大 +1.16pt）取れる点。
#:    広げるならここだけを動かす。
#: 🔴 **看板枠（`sign`）は外した。** perm へ2割を集中させるため。10万+ は
#:    0.259 → 0.222件/日（＝上帯なしと同水準）に戻る。看板が要るなら
#:    `sign` を戻すか `F_sign` の種別を広げること（両方を同じ2,000円では買えない）。
#: 🔴 **上帯は「的中回数」と「1回の大きさ」を同時には買えない。** 2,000円をどう割るかの
#:    一次元のダイヤルで、点数を増やす＝当たるが小さい／減らす＝当たらないが大きい。
#: 🔴🔴 **2026-09-04 夕方に切り戻した（前向き実測・ユーザー判断「戻して様子見」）。**
#:    9/1〜9/4 の `E_hit`+`F_hit` 95行を、**焼き付いた買い目そのまま**で予算だけ
#:    8,000 へ縮めて採点し直した反実仮想（DB は変更していない・上帯候補の予測オッズと
#:    確率は盤面を作り直して焼き付き1,092点と比 1.0000 で一致することを確認）:
#:
#:      全行95     的中 31.6→32.6% / 表示的中 30.5→29.5% / 払戻中央 20,780→17,500円
#:                 払戻合計 760,710→633,160円 / **ROI 80.1→66.6%**
#:      入稿36     的中 22.2→25.0% / 表示的中 19.4→22.2% / **ROI 51.2→45.9%**
#:
#:    上帯で拾えたのは **1件だけ**（弥彦2R 17,100円）で、元から当たっていた30件が
#:    すべて2割減（合計 −14.5万円）。**削る側は確実・拾う側は稀**という構造がそのまま出た。
#: 🔴 これは長期の測定（20か月・上帯の的中 0.71件/日・表示的中 +0.88pt）を
#:    否定する証拠ではない。**4日95行では当たり目が並び違いの中にあったレース自体が
#:    5件（5.3%）しかなく**、そのうち上位4点で拾えたのが1件。5件では推定は動かない。
#: 🟢 **戻すのは1行**（`UPPER_BANDS = (_PERM_BAND,)`）。仕組み・文面・画面・テストは
#:    そのまま残してある。拾う側へ寄せるなら `_PERM_BAND` の `max_legs` を
#:    6 か 0（全部）にする（上帯の的中 0.71 → 1.12件/日・ただしガミ 5.1 → 7.5%）。
_PERM_BAND = UpperBand("perm", 2_000, "perm_fill", max_legs=4, min_bought=2,
                       note="下帯で2通り以上買っている3車の、残りの並びを確率上位4点")

#: 重ねる上帯。**空なら重ね買いをしない**（下帯が予算全額を使う＝2026-09-04 夕方以降）。
UPPER_BANDS: tuple[UpperBand, ...] = ()
#: 上帯を掛ける車数。**これ以外には掛けない**。
UPPER_BAND_N_ENTRIES = 7
#: 上帯を掛ける**プラン**（ユーザー決定 2026-09-04）。空なら全プラン。
#:
#: 🔴 **測ったのは全プラン**。絞ると per-plan の効き（下記）は変わらないが、
#:    掛ける母集団が減るぶん**ラインナップ全体の平均は薄まる**。
#:    `E_hit` + `F_hit` は確認窓で全商品の 26.5%（807 + 1,476 ÷ 8,610）。
#:
#:    プラン単体の Δ表示的中（探索 / 確認・`overlay_upper_band_2026_09_04.md` §5）:
#:      **E_hit +2.54 / +2.73pt   F_hit +1.76 / +1.83pt**
#:      （掛けないもの: A_hit +1.03/+1.38・B_hit +2.63/+2.75・C_hit +2.47/+2.30・
#:        F_sign +3.48/+3.70・A_ana +2.00/+5.69）
#:
#: 🟢 **ねらいは2つ**:
#:    ① **当たりにくい型にだけ押さえを置く**。型E/F は軸崩壊が 27.8 / 28.8% と
#:       最も高く（型A は 12.7%）、100倍以上での決着も 64.9 / 69.2% と最も多い
#:       （`docs/type_lab/axis_bust_conditional_2026_09_03.md` §1）。
#:    ② **点数を絞っている商品には押さえを入れない**。`F_pay`（1着固定4点）・
#:       `F_sign`（看板枠2〜3点）・`A_hit`（3点）・`A_trio`（2点）・`D_hit`（3点）は
#:       「少ない点に厚く置いて払戻を作る」設計なので、薄い押さえを足すと
#:       その集中を自分で薄める。**押さえは面で買う商品（`E_hit` 14点 /
#:       `F_hit` 12点）にだけ乗せる**。
#: ⚠️ 収支では決められない（どのプランでも ΔROI の CI は 0 を跨ぐ）。
#: 🔴 `F_hit` がゲートに落ちて帯15倍へ切り替わったときも `key` は `F_hit` のままなので
#:    ここに掛かる（`GATE_FALLBACK`）。**そちらも12点の面買い**なので狙いは同じ。
UPPER_BAND_PLANS: frozenset[str] = frozenset({"E_hit", "F_hit"})
#: 9車型F の三連複（`line_axis2_flow`）で積める Σ(1/予測オッズ) の上限。
#: **ダッチ配分では計画払戻 = 予算 ÷ Σ なので、これは「計画払戻が
#: `MIN_MEAN_PAYOUT` を割らない最大点数まで積む」と同じこと**。
#: 入稿ゲート（平均想定払戻 > 2万円）と同じ値なので、通る限界まで積んで止まる。
TRIO_LINE_SIGMA_MAX = BUDGET / MIN_MEAN_PAYOUT
#: 3点未満しか積めないなら**商品にしない**。
#: 🔴 「1万円以下は当たりと扱わない」（2026-09-06 ユーザー方針）ので、
#:    ここで落ちたレースは入稿しない。実測では落ちる 5〜8% のレースは
#:    現行の三連単12点でも当たっていない（8〜9月の実地3件はすべて不的中）。
TRIO_LINE_MIN_LEGS = 3

#: 上帯の合計予算。下帯の予算は `BUDGET - UPPER_BAND_TOTAL`。
UPPER_BAND_TOTAL = sum(b.budget for b in UPPER_BANDS)
#: 行の `legs[].role`。上帯を入れる前の行には無いので、読む側は既定を `base` にする。
ROLE_BASE = "base"


def win_entropy(win_probs: Mapping[int, float] | None) -> float:
    """1着率のエントロピー。大きいほど1着が読めない。

    渡された値の合計で正規化するので、0-1 でも 0-100 でも同じ値になる。

    >>> round(win_entropy({1: 0.25, 2: 0.25, 3: 0.25, 4: 0.25}), 4)
    1.3863
    >>> round(win_entropy({1: 25, 2: 25, 3: 25, 4: 25}), 4)
    1.3863
    >>> win_entropy(None)
    0.0
    >>> win_entropy({1: 1.0})
    0.0
    """
    if not win_probs:
        return 0.0
    vals = [float(v) for v in win_probs.values() if v is not None and float(v) > 0]
    tot = sum(vals)
    if tot <= 0:
        return 0.0
    e = -sum((v / tot) * math.log(v / tot) for v in vals)
    return float(e) if e else 0.0        # 1車だけのとき -0.0 になるのを潰す



# ───────────────────────────── 型判定 ─────────────────────────────

@dataclass(frozen=True)
class RaceShape:
    """1レースの型と、その根拠。"""
    type_label: str          # 'A'..'F'
    axis_sum: float
    arare: int
    gap: float
    firm: bool
    order: tuple[int, ...]   # 3着内率の降順（車番）
    #: 1着率のエントロピー（大きいほど1着が読めない＝軸1が飛びやすい）。
    #: 型A の売り分け（`sell_plan_for`）だけが使う。渡されなければ 0.0。
    pw_ent: float = 0.0
    #: 並び（ライン）のうち **2車以上のもの**。各ラインは **隊列順**
    #: （先頭 → 番手 → 3番手…）に並べる。
    #: 🔴 **車番順ではない。** 先頭と番手を見分けられないと「両方向を買う」判断が
    #:    できなくなる（下記 `LINE_SWAP_MIN_ODDS` / `line_legs` のコメント）。
    #: 🔴 単騎（`line_group` が空／0）は含めない。
    #: ⚠️ `line_legs`（ライン決着への差し替え）は 3車ないと組めないので
    #:    あちら側で 2車ラインを弾いている。
    lines: tuple[tuple[int, ...], ...] = ()
    #: **3着内率の合計が最も大きいラインの上位2車**（3着内率の降順）。2車以上の
    #: ラインが1本も無ければ空。`line_axis2_flow`（9車型F の三連複）だけが使う。
    #: 🔴 **隊列順ではなく3着内率順**。買うのは三連複なので順序は要らず、
    #:    「どの2車を軸にするか」だけが問題になる。
    line_pair: tuple[int, ...] = ()


def _line_members(line_group: Mapping[int, object], car: int) -> list[int]:
    g = line_group.get(car)
    if g is None or str(g) in ("", "0"):
        return []
    return [c for c, v in line_group.items() if v == g]


def race_shape(top3_probs: Mapping[int, float], line_group: Mapping[int, object],
               line_pos: Mapping[int, object], style: Mapping[int, str],
               race_point: Mapping[int, float], behind_pct: Mapping[int, float],
               day_index: int,
               win_probs: Mapping[int, float] | None = None) -> RaceShape | None:
    """レースの型を返す。3着内率が3車未満なら None。

    top3_probs: {車番: 3着内率 0-1}。**0-100 のパーセントを渡さないこと**
    behind_pct: {車番: ex_left_behind_pct（0-100 のパーセント）}
    win_probs:  {車番: 1着率}。**渡さなくても型判定は一切変わらない**
                （`pw_ent` が 0.0 になり、型A の穴狙いへ振り分けられなくなるだけ）。
    """
    if not top3_probs or len(top3_probs) < 3:
        return None
    order = tuple(sorted(top3_probs, key=lambda c: (-float(top3_probs[c]), c)))
    axis_sum = float(top3_probs[order[0]]) + float(top3_probs[order[1]])
    others = order[2:]
    gap = 0.0
    if len(others) >= 5:
        gap = ((float(top3_probs[others[0]]) + float(top3_probs[others[1]])) / 2
               - sum(float(top3_probs[c]) for c in others[2:5]) / 3)

    mem = _line_members(line_group, order[0])
    lead = next((c for c in mem if str(line_pos.get(c)) == "1"), None)
    second = next((c for c in mem if str(line_pos.get(c)) == "2"), None)
    size = len(mem) if mem else 1

    s = 1 if size == 2 else (-1 if size >= 4 else 0)
    if lead is not None:
        s += -1 if float(behind_pct.get(lead, 0.0)) >= BEHIND_MID else 1
        s += 2 if str(style.get(lead, "")) == "追" else 0
    s += int(day_index or 0) - 2
    if (lead is not None and second is not None
            and float(race_point.get(second, 0.0)) > float(race_point.get(lead, 0.0))):
        s += 1

    pw_ent = win_entropy(win_probs) if win_probs else 0.0

    firm = axis_sum >= AXIS_SUM_FIRM
    if firm:
        label = "A" if s <= -1 else ("B" if s == 0 else "C")
    else:
        label = "D" if s <= -1 else ("E" if s == 0 else "F")
    lines = _lines_of(line_group, line_pos)
    return RaceShape(label, axis_sum, s, gap, firm, order, pw_ent, lines,
                     _strongest_pair(lines, top3_probs))


def _strongest_pair(lines: Sequence[Sequence[int]],
                    top3_probs: Mapping[int, float]) -> tuple[int, ...]:
    """3着内率の合計が最大のラインの上位2車を、3着内率の降順で返す。

    2車以上のラインが無ければ空タプル。

    >>> _strongest_pair(((1, 2, 3), (4, 5)),
    ...                 {1: .5, 2: .3, 3: .1, 4: .45, 5: .44, 6: .2})
    (4, 5)
    >>> _strongest_pair((), {1: .5})
    ()
    """
    best: tuple[float, tuple[int, ...]] | None = None
    for line in lines:
        mem = sorted((c for c in line if c in top3_probs),
                     key=lambda c: (-float(top3_probs[c]), c))[:2]
        if len(mem) < 2:
            continue
        score = sum(float(top3_probs[c]) for c in mem)
        cand = (score, tuple(mem))
        if best is None or cand > best:
            best = cand
    return best[1] if best else ()


def _lines_of(line_group: Mapping[int, object],
              line_pos: Mapping[int, object] | None = None,
              min_size: int = 2,
              ) -> tuple[tuple[int, ...], ...]:
    """`min_size` 車以上のラインを **隊列順**（先頭→番手→3番手）のタプルで返す。

    `line_pos` が無い車は最後尾へ回し、同順位は車番で並べる。

    🔴 **既定は 2車以上**（2026-09-06 に 3車以上から広げた）。9車型F の
       `line_axis2_flow` が「2車ラインも軸の候補にする」ため。ライン決着への
       差し替え (`line_legs`) は 3車ないと組めないので、**あちら側で
       `len(mem) < 3` を弾いている**——ここを広げても差し替えは変わらない。

    >>> _lines_of({1: 1, 2: 1, 3: 1, 4: 2, 5: 2, 6: 0, 7: None},
    ...           {1: 3, 2: 1, 3: 2, 4: 1, 5: 2})
    ((2, 3, 1), (4, 5))
    >>> _lines_of({1: 1, 2: 1, 3: 1}, None)
    ((1, 2, 3),)
    >>> _lines_of({1: 1, 2: 1, 3: 1, 4: 2, 5: 2}, None, min_size=3)
    ((1, 2, 3),)
    """
    pos = line_pos or {}

    def _p(car: int) -> tuple[int, int]:
        v = pos.get(car)
        try:
            n = int(str(v))
        except (TypeError, ValueError):
            n = 0
        return (n if n > 0 else 99, car)

    groups: dict[str, list[int]] = {}
    for car, g in line_group.items():
        if g is None or str(g) in ("", "0"):
            continue
        groups.setdefault(str(g), []).append(int(car))
    return tuple(tuple(sorted(v, key=_p))
                 for v in sorted(groups.values()) if len(v) >= min_size)


# ───────────────────────────── 買い目 ─────────────────────────────

@dataclass(frozen=True)
class Plan:
    """型ごとの買い方。`PLANS` の値。"""
    key: str                 # 'A_hit' など（テーブルの plan_key）
    type_label: str
    bet_type: str            # 'trifecta' | 'trio'
    structure: str           # 下記 build_legs の分岐
    n_partners: int          # 相手（3着列など）に使う車数
    min_odds: float = 0.0    # 予測オッズの下限（帯）
    max_odds: float = 0.0    # 予測オッズの上限（0=なし）
    max_legs: int = 0        # 上限点数（0=なし）
    sigma_max: float = 0.0   # Σ(1/予測オッズ) の上限（0=なし）
    alloc: str = "conf"      # 'conf' | 'dutch'
    floor_mult: float = DEFAULT_FLOOR_MULT
    note: str = ""


#: 🔴🔴 **2026-08-31: `A_hit` と `F_hit` を固定構成から「確率上位k点」へ替えた。**
#:    点数は据え置き（A_hit 3点 / F_hit 12点）で、**買い目の選び方だけ**を変えている。
#:
#:    きっかけは「型ラボの主力プランは確率で順序を選んでいない」という発見。
#:    `fixed12`（1着=軸1・2着=軸2 固定）と `all6`（全順列）は確率を配分にしか
#:    使わないので、確率をどれだけ良くしても買い目が変わらなかった。実際
#:    同ライン隣接ボーナス（λ/μ）を**配分にだけ**入れると A_hit は逆に悪化する
#:    （表示的中 32.80 → 32.18% / 32.84 → 32.32%）。
#:
#:    プラン単体（点数は同じ・確定払戻で採点・探索 2024-07〜2025-12 / 確認 2026）:
#:
#:      A_hit  現行 32.80 / 32.84%  →  確率順+λμ **34.40 / 34.91%**（ROI +1.6/+4.8pt）
#:      F_hit  現行 24.13 / 24.36%  →  確率順+λμ **27.68 / 27.92%**（ROI −2.0/−7.7pt）
#:
#:    ラインナップ全体では 表示的中 23.82→25.01% / 24.45→25.56%、
#:    **Δの95%CI 探索 [+0.67,+1.72] / 確認 [+0.26,+1.88] で両窓とも 0 を上回る**。
#:    ΔROI は両窓とも 0 を跨ぐ（点推定 −0.2pt）。件数は 8% 減る。
#:
#: 🔴 **これは `SUMMARY.md` 追補B の設計判断を意図的に覆している。** あちらは
#:    「大混戦(F)は順序が読めないので6順列すべてが最良（ROI 66.8→79.2%）」と
#:    **ROI で**選んでいた。今回は**表示的中で**選び直した結果で、F_hit 単体の
#:    ROI は確認窓で 80.6 → 72.9% と落ちる（全体では他が吸収して判別不能）。
#:    ROI を優先する方針へ戻すなら `all6` / `fixed12` に戻すこと。
#: ⚠️ `F_pay`（決勝で売る4点）は**替えていない**。確率順にすると表示的中は
#:    +5.5pt 上がるが確認窓 ROI が 97.8 → 77.2% と大きく落ち、件数も 1.8件/日 と薄い。
#:
#: 🔴 **この辞書が商品の定義そのもの**。値を変えたら `rule_version()` が変わり、
#:    `type_lab_picks.rule_version` で新旧の行が自動的に分かれる。
PLANS: dict[str, Plan] = {
    # 型A 鉄板 — 3点。**2026-08-31 に固定構成（`fixed12`）から確率順へ替えた。**
    "A_hit": Plan("A_hit", "A", "trifecta", "prob_top", 0, max_legs=3, alloc="conf",
                  floor_mult=MIN_PAYOUT_MULT,
                  note="確率上位3点（同ライン隣接ボーナス込み・表示的中を取る）"),
    "A_pay": Plan("A_pay", "A", "trifecta", "axis1_second2", 3, alloc="conf",
                  note="1着=軸1固定・2着を2車・3着流し（払戻を取る）"),
    # 型A ②三連複 — 順序リスクだけ捨てる。三連複でも2万円取れるレースで A_hit と入れ替える。
    "A_trio": Plan("A_trio", "A", "trio", "axis2_flow", 2, alloc="dutch",
                   note="軸2車＋相手2車の三連複2点（順序を捨てて当たる回数を取る）"),
    # 型A ①穴狙い — 軸1が飛ぶ側。軸1を外した6車から確率上位5点。
    "A_ana": Plan("A_ana", "A", "trifecta", "bust_top", 0, max_legs=5, alloc="dutch",
                  note="軸1を外した残り6車から確率上位5点（軸が飛ぶ側を取る）"),
    # 型B 堅い・中 — 確率上位を Σ(1/予測) の床まで積む。
    "B_hit": Plan("B_hit", "B", "trifecta", "prob_top", 0, max_legs=8,
                  sigma_max=1 / 3.0, alloc="conf", floor_mult=MIN_PAYOUT_MULT,
                  note="確率上位から想定平均払戻 30,000円を割らない点数まで"),
    # 型C 堅いが崩れ筋 — 帯を切って点数を増やす側。
    # 🔴 **2026-09-01 に帯を 20倍 → 15倍へ下げた（点数は12点のまま）。**
    #    発端は「12点も買っているのに当たらない」というユーザー観察。分解すると
    #    **軸2車が3着以内にそろったレース（型Cの60.8%）の外れの 56.5% は
    #    「決着の目が20倍未満で帯が外していた」**（探索 58.7%）。モデルではなく帯が主因。
    #    掃引の実測（探索2025 / 確認2026・型C 単体）:
    #      L=20 k=12（旧） 表示的中 22.35 / 24.22%  ROI 78.7 / 83.2  払戻中央 30,760 / 31,250
    #      **L=15 k=12**   表示的中 26.28 / 28.79%  ROI 78.9 / 84.9  払戻中央 26,395 / 26,400
    #    ＝**表示的中 +3.9/+4.6pt と ROI +0.2/+1.7pt が両窓で同時に上がる**（件数はほぼ不変・
    #    ゲート通過 99.6%）。代償は払戻中央 −4,400円。
    # 🔴 **「帯を下げる」と「点数を減らす」を同時にやってはいけない**（両方が払戻を同じ
    #    向きへ押す）。平均想定払戻を揃えて比べると L=10 k=6（35,858円）は 表示的中 22.06% で
    #    現行 L=20 k=12（34,385円・24.22%）より**悪い**。動かすのはどちらか一方だけにする。
    # 🔴 **合成オッズの下限は入れない**（2026-09-02 に帯15倍の上で測り直して不採用）。
    #    帯を下げたぶん合成オッズが下がる（live 中央 3.39→2.61倍）ので、同じ4倍でも
    #    切る点数が 12→6 に増える。表示的中 −1.6pt（C_hit は −7.4pt）を払う一方で
    #    ROI は窓で符号反転（探索 +0.8 / 確認 −3.8pt）。上の規律そのものに当たる。
    #    実測とやり直し手順: `docs/type_lab/synthetic_floor_recheck_2026_09_02.md`
    # ⚠️ 帯そのものの回収率は妥当（買っている目の帯別回収率は 20〜30倍が両窓とも 84.6% で最良、
    #    100倍超が 51.2 / 60.4% で最悪。ただし100倍超はダッチ配分ゆえ投資の3%しかない）。
    "C_hit": Plan("C_hit", "C", "trifecta", "prob_top", 0, min_odds=15.0,
                  max_legs=12, alloc="conf", floor_mult=MIN_PAYOUT_MULT,
                  note="予測15倍以上から確率上位12点"),
    # 型D 混戦・軸あり — 唯一の三連複。最人気の相手を外す。
    # 🔴 **2026-09-01 に相手を 4点 → 3点へ減らした。** 型D は「的中頻度に対して
    #    払戻が足りない」（的中 26.87% に対し損益分岐に要る 37,222円 ↔ 実際 28,696円）。
    #    同一レース対比較の実測（探索2025 / 確認2026）:
    #      ROI       76.2 → 78.8 / 77.1 → 81.3（Δ +2.7 [−0.9,+5.9] / **+4.2 [+0.9,+7.3]**）
    #      表示的中  24.77 → 22.22 / 26.70 → 24.88（Δ **−2.54 [−3.75,−1.47]** / −1.82 [−2.99,−0.66]）
    #      払戻中央  28,005 → 31,580 / 26,790 → 30,170円   ゲート通過 93.8 → 98.9 / 96.2 → 99.4%
    #    ＝**表示的中を確実に 1.8〜2.5pt 失い、ROI を 2.7〜4.2pt 得る取引**（ROI が有意なのは
    #    確認窓だけ）。件数はゲート通過が上がるぶん微増する。戻すなら 4 に戻すだけでよい。
    # ⚠️ 「最人気を外さない（軸2車流し）」案は測って否定した——通過率が 78 / 52 / 33%（相手2/3/4点）
    #    まで落ち、確認窓の ROI も 71.7 / 70.9 / 69.9% と現行を下回る。
    "D_hit": Plan("D_hit", "D", "trio", "axis2_drop_fav", 3, alloc="conf",
                  floor_mult=MIN_PAYOUT_MULT,
                  note="軸2車＋相手3点（相手4車のうち最人気1車を外す）"),
    # 型E 混戦・中 — 高い帯で点数を広げる。
    "E_hit": Plan("E_hit", "E", "trifecta", "prob_top", 0, min_odds=30.0,
                  max_legs=14, alloc="conf", floor_mult=MIN_PAYOUT_MULT,
                  note="予測30倍以上から確率上位14点"),
    # 型F 大混戦 — 12点。**2026-08-31 に全順列（`all6`）から確率順へ替えた。**
    "F_hit": Plan("F_hit", "F", "trifecta", "prob_top", 0, max_legs=12, alloc="conf",
                  floor_mult=MIN_PAYOUT_MULT,
                  note="確率上位12点（同ライン隣接ボーナス込み）"),
    "F_pay": Plan("F_pay", "F", "trifecta", "axis1_second2", 2, alloc="conf",
                  note="1着=軸1固定・2着を2車・3着流し（一撃を取る）"),
    # 型F ③9車の主力 — 三連複。**2026-09-06 に 9車の非決勝を F_hit から移した。**
    #
    # 発端はユーザー観察「9車の型Fが半分以上を占めるのに二軸が34%しか当たらない」。
    # 9車型F は 9車レースの 57.7% を占め、売らないと商品が大幅に減る。
    #
    # 実測（paper 1,958R・F_hit を売る母集団・探索2025 / 確認2026・確定オッズ）:
    #   ① 現行 三連単12点   商品 1117(90%) / 659(92%)  表示的中 22.38 / 19.88%  ROI 78.5 / 67.1
    #   ⑫ **本プラン**      商品 1139(92%) / 681(95%)  表示的中 31.61 / 33.48%  ROI 79.2 / 86.4
    #   Δ表示的中 +9.00pt CI[+6.27,+11.81] / +13.64pt CI[+9.98,+17.36]（**両窓とも0を跨がない**）
    #   ⚠️ **商品数は現行より増える**（固定点数はゲートに落ちたレースを丸ごと失うが、
    #      本プランは通る最大点数へ落とすので落ちない）。平均 6.19 / 6.25点。
    #
    # 🔴 **点数は固定しない。** 固定k点はどれも劣る（固定4点 24.88/27.20・
    #    固定6点 27.67/30.32・固定7点 28.92/30.58 で件数も少ない）。
    # 🔴 **配分はダッチ。** conf傾斜（床2倍＝他プランと同じ）は表示的中 −0.88/−0.74pt
    #    CI[−1.50,−0.35]/[−1.47,−0.15]・ROI −1.6/−1.0pt で**両指標とも負ける**。
    #    床1倍に緩めると表示的中 +0.61/+1.02pt だが ROI −4.0pt CI[−6.1,−1.8]/[−7.2,−0.5]。
    #    三連複はダッチが全点の払戻を揃え、Σ の上限が計画払戻2万円（＝投資の2.2倍）を
    #    保証するので**床の仕事が無い**。三連複の予測オッズ誤差は小さく
    #    （live 実測 実払戻/計画の最小 0.456・0.45未満は 0/65件）、最悪でも投資を割らない。
    #    三連単は 10.8% が 0.45未満なので、あちらでは床が効いている。
    # 🔴 **目標払戻を上げてはいけない。** T=20,000(=`MIN_MEAN_PAYOUT`) が最良で、
    #    22,000 / 25,000 / 30,000 / 35,000 と上げると表示的中もROIも単調に下がる。
    # ⚠️ 測定は**確定オッズ**。本番は予測オッズなので点数と「組めないレース」の
    #    割合は変わる。前向き実測で確かめること。
    # 詳細: `docs/type_lab/nine_car_type_f_2026_09_06.md`
    "F_line": Plan("F_line", "F", "trio", "line_axis2_flow", 0,
                   sigma_max=TRIO_LINE_SIGMA_MAX, alloc="dutch",
                   note="最強ラインの2車＋相手を計画払戻2万円まで（9車型F）"),
}

#: 看板枠（`structure="signboard"`）を全6型ぶん足す。**生成は常に行い、売るかどうかは
#: `SIGNBOARD_TYPES` が決める**——売らなかった型の成績を後から測れるようにするため
#: （`plans_for` の docstring と同じ思想）。
for _t in "ABCDEF":
    PLANS[f"{_t}_sign"] = Plan(
        f"{_t}_sign", _t, "trifecta", "signboard", 0,
        max_odds=SIGNBOARD_MAX_ODDS, alloc="dutch",
        note=f"看板枠: 確率順に Σ(1/予測オッズ) <= 予算/{SIGNBOARD_TARGET:,} まで積む")
del _t


#: 9車で型F を売る条件。**決勝だけ・`F_hit` だけ**（2026-08-28 実投入）。
#:
#: 🔴 9車は**型F が母集団の 55〜59%** を占め、そこが弱い
#:    （型F 11.9件/日・表示的中 11.55%・ROI 65.1%）。全8プランのままだと
#:    ROI 69.8%/72.8% と**両窓とも壁の下**になる。型F を外すと 5.6件/日・
#:    表示的中 22.89%・ROI 83.0%/89.1%。
#: 🔴 打ち手（相手を増やす／荒れ度で切る／1着固定フォーメーション）は3通りとも壁を
#:    超えない。同じフォーメーションが**7車では ROI 77.0%** なので、
#:    形ではなく **9車の型F という母集団が薄い**。
#: 🟢 例外が決勝。9車の決勝の **83.8%（80レース中 67）が型F**（残りは型C 8 / 型E 5 で、
#:    そちらは元々そのまま売る側）。その型F の決勝は現行 `F_hit` のまま
#:    表示的中 14〜21%・払戻中央 3.5〜5.2万円と型F 全体より良い側にある
#:    （⚠️ 20か月で 66件・CI [24,174] なので**収支は判定できない**。
#:    「看板レースは必ず出す」方針と衝突しないから残す、という判断）。
#: 🔴 **「決勝」は完全一致で見ること。** `"決勝" in race_type` は準決勝も拾う
#:    （CLAUDE.md の既知の罠。9車の準決勝は確認窓 ROI 50.3% で反転する）。
#:
#: 実測: `keirin/docs/type_lab/carcount_2026_08_27.md`（2026-08-28 追記）
NINE_CAR_TYPE_F_RACE_TYPES = ("決勝",)

#: 🔴 **2026-08-30 変更（ユーザー判断）: 9車の型F は「決勝は `F_pay`・それ以外は
#:    `F_hit`」で必ず売る。** それまでは決勝以外を**まるごと売らない**設計だった
#:    （`plans_for` が空を返す）。
#:
#:    そのままでは 2026-08-30 に **9車開催の看板8件（選抜3・特選3・特秀2）が
#:    無商品**になった。従来は旧ランクの看板穴埋め（7S）が拾っていたが、
#:    型ラボ全面移行で 7S が無効になり、穴埋めが機能しなくなったため
#:    （実測: 埋まらなかった看板 8/29 15件 → 8/30 21件）。
#:
#:    ⚠️ **売らない判断の根拠だった数字は消えていない。** 9車の型F を全部
#:       `F_pay` で売ると表示的中 6.07%・ROI 60.5% で壁を大きく下回る。
#:       そこで**決勝以外は `F_hit`**（当たる回数を売る側）に替える。
#:       9車の型F 全体でも `F_hit` なら表示的中は `F_pay` より高い
#:       （決勝で 16.42% ↔ 2.99%）。**収支は 20か月でも判定できていない**ので、
#:       これは「看板には必ず出す」方針を優先した選択であって、
#:       ROI が上がるという主張ではない。前向きに台帳で確かめる。
#: 🔴🔴 **2026-08-31 に 7車へも広げた（車数を問わない規則になった）。**
#:    それまで 7車の型F は一律 `F_pay`（一撃）だったが、実売3日で
#:    **37件すべて表示的中 0%**。`F_pay` の表示的中は設計上 8.4% しかない一方、
#:    型F は母集団の約30%を占めるため、ここが「毎日当たらない」体感を作っていた。
#:
#:    確認窓2026（軸信頼ゲートあと）で型F の売り方だけを替えた実測:
#:
#:      型F の売り方              件/日   表示的中    ROI    50%割れの日   10万+/日
#:      F_pay（〜2026-08-31）     39.4   22.00%   78.7%    18.5%      0.307
#:      F_hit                    39.4   27.21%   83.0%     9.2%      0.050
#:      **F_hit＋決勝だけF_pay**   39.4   26.80%   83.8%     8.8%      0.088
#:
#:    探索窓2025でも同じ向き（21.15→25.81% / 74.5→76.7% / 18.1→11.2%）。
#:    🔴 **代償は看板**（10万+ が 0.307 → 0.088件/日）。的中体験と看板は
#:       同じ予算では交換関係にあるので、方針が変わったらここを戻すこと。
#:    🔴 **「決勝」は完全一致で引く。** 部分一致にすると「準決勝」を拾い、
#:       表示的中の低い `F_pay` が母集団の何倍にも広がる（CLAUDE.md の既知の罠）。
TYPE_F_SELL_BY_RACE_TYPE = {"決勝": "F_pay", "チャレンジ決勝": "F_pay"}
#: 上の表に無い種別（＝決勝以外）で売るプラン。
TYPE_F_SELL_DEFAULT = "F_hit"
#: 決勝以外を**この車数のときだけ**差し替えるプラン（9車の型F は三連複）。
TYPE_F_SELL_LINE = "F_line"
TYPE_F_LINE_N_ENTRIES = 9

#: 旧名（9車専用だった頃の名前）。**参照している箇所があるので残す**。
NINE_CAR_TYPE_F_SELL_BY_RACE_TYPE = TYPE_F_SELL_BY_RACE_TYPE
NINE_CAR_TYPE_F_SELL_DEFAULT = TYPE_F_SELL_DEFAULT
#: 🔴 **2026-08-28 に `F_hit` → `F_pay` へ変更**（本番移行・ユーザー判断）。
#:    車数や種別で hit / pay を分けない、という方針に揃えた。
#:    ⚠️ ここは「売る／売らない」の分岐（`NINE_CAR_TYPE_F_RACE_TYPES`）とは別物。
#:       決勝限定という**母集団の絞り**は実測に基づくので残す（9車の型F を
#:       全部売ると表示的中 6.07%・ROI 60.5% で壁を大きく下回る）。
#:    🔴 9車決勝の `F_pay` は **67件中 表示的中2件（2.99%）**で収支は判定できない。
#:       `F_hit` なら 16.42%。それでも `F_hit` に戻さないのは、乖離・種別といった
#:       条件で hit/pay を切り替える案が**無作為対照に負けた**ため（下記）。
NINE_CAR_TYPE_F_PLANS = ("F_pay",)

#: 実際に netkeirin へ入稿するプラン。**型ごとにちょうど1つ**なので
#: 1レース1商品が構造的に守られる（型は排他）。
#:
#: 🔴 **生成（`type_lab_picks`）は8プランのまま**。ここで絞るのは入稿だけで、
#:    A_pay / F_hit の行は比較台として残す。両者を混同すると
#:    「売っていないものの成績」が消えて事後の比較ができなくなる。
#:
#: 🔴 **型F を `F_pay` にすると表示的中を 5.2pt 失い、10万円超の的中を 6倍にする**
#:    （確認窓 2026・軸信頼ゲートあと・`scripts/exp_type_lab/composition.py`）:
#:
#:      構成                     件/日   表示的中   払戻中央   10万+/日    ROI
#:      A〜E hit + F_pay（現行）  39.48    22.01%    27,830     0.307    78.7%
#:      A〜E hit + F_hit          39.46    27.23%    25,730     0.050    83.1%
#:
#:    `SUMMARY.md` §4「この体系では看板は作れない」を破るのは `F_pay` だけ
#:    （`F_hit` の 10万+ は 20日に1回＝実質ゼロ）。**店頭KPIの選択**であって
#:    収支の判断ではない（ROI 差の CI 下限は −0.03 / −0.40 とぎりぎり）。
#:
#: 🔴 **型Fを条件で hit / pay へ振り分ける案は否定済み**（2026-08-28・
#:    `scripts/exp_type_lab/typef_split_control.py`）。軸1と軸2の乖離
#:    `d = (P(1着=軸1) − P(1着=軸2)) / (P(1着=軸1) + P(1着=軸2))` で半分ずつに
#:    振り分け、**同数を無作為に振り分けた対照20本**と比べた:
#:
#:      母集団           全F_hit        全F_pay       無作為中央     乖離大→hit    乖離大→pay
#:      7車 探索2025   25.64%/73.7%   9.12%/66.1%  17.37%/70.0%  17.05%(2/20)  17.71%(18/20)
#:      7車 確認2026   25.19%/79.4%   8.52%/65.5%  17.02%/74.3%  15.85%(1/20)  17.86%(20/20)
#:      9車 全期間     16.94%/69.2%   6.07%/60.5%  11.70%/65.2%  11.01%(2/20)  12.00%(18/20)
#:
#:    「乖離が大きいほど F_hit」は3母集団すべてで**無作為対照に負ける**。
#:    向きを逆にすると無作為には勝つが、**得られるものが両端に届かない**
#:    ——表示的中は全F_hit の 25.2% が最大、10万+ は全F_pay の 65件が最大で、
#:    分割は中間（17.9% / 32件）を作るだけ。`SUMMARY.md` §2.6
#:    「型は edge を作らない。決めるのは帯とカバレッジだけ」と同じ構造。
#: 🔴 **型F はここに書かない**（2026-08-31）。種別で `F_pay` / `F_hit` に分かれるので
#:    固定の集合では表せない。判定は `sell_plans_for` の型F 分岐が正本。
SELL_PLANS: tuple[str, ...] = ("A_hit", "A_trio", "A_ana",
                               "B_hit", "C_hit", "D_hit", "E_hit")

#: 型ラボが**入稿しうる**プランの全体（2026-08-30）。
#:
#: 🔴 `SELL_PLANS` は 7車の固定集合で、**9車の型F はここに無い `F_line` を売る**。
#:    「型ラボの商品か」を判定する場所（既存ランクとの取り合い・重複判定）で
#:    `SELL_PLANS` を使うと、9車の型F の入稿を**他ランクの商品と誤認する**。
#:    そういう用途はこちらを使うこと。
SELLABLE_PLAN_KEYS: frozenset[str] = (
    frozenset(SELL_PLANS)
    | {f"{t}_sign" for t in SIGNBOARD_TYPES}
    | {TYPE_F_SELL_DEFAULT, TYPE_F_SELL_LINE}
    | set(TYPE_F_SELL_BY_RACE_TYPE.values()))


def plans_for(type_label: str, n_entries: int = 7,
              race_type: str | None = None) -> list[Plan]:
    """その型で**組む**買い方。**車数と種別で組まないものを外す**。

    7車は `PLANS` をそのまま返す（8プラン全部を `type_lab_picks` へ残す）。
    9車だけ、型F を `NINE_CAR_TYPE_F_*` の条件へ絞る。

    🔴 **これは「生成」の関数で「入稿」の関数ではない。** 実際に売るものは
       `sell_plans_for()`。7車は A_pay / F_hit を組むが売らない——比較台を
       残すためで、混同すると売っていない側の成績が消える。

    🔴 **2026-08-30 から 9車の型F も種別で落とさない。** 以前は決勝以外を空にして
       いたが、それは「生成」を止める形で「売らない」を実現していた。
       **比較台が消えるので良くない**（売らなかった側の成績が事後に測れない）。
       売る／売らないは `sell_plans_for` の責務へ寄せた。

    >>> [p.key for p in plans_for("F")]
    ['F_hit', 'F_pay', 'F_line', 'F_sign']
    >>> [p.key for p in plans_for("F", 9, "決勝")]
    ['F_hit', 'F_pay', 'F_line', 'F_sign']
    >>> [p.key for p in plans_for("F", 9, "準決勝")]
    ['F_hit', 'F_pay', 'F_line', 'F_sign']
    >>> [p.key for p in plans_for("A", 9, "特選")]
    ['A_hit', 'A_pay', 'A_trio', 'A_ana', 'A_sign']
    """
    return [p for p in PLANS.values() if p.type_label == type_label]


def sell_plans_for(type_label: str, n_entries: int = 7,
                   race_type: str | None = None, *,
                   pw_ent: float | None = None,
                   trio_ok: bool | None = None) -> list[Plan]:
    """その型で **netkeirin へ入稿する**買い方（`SELL_PLANS` で絞ったもの）。

    🔴 **必ず 0 個か 1 個**。型は排他なので、これが1レース1商品を構造的に保証する。
       2つ返るようになったら優先順位の設計が別途要る＝設計が変わった合図なので、
       `tests/test_type_lab_submit.py` が全型・全車数で 1 以下を固定している。

    >>> [p.key for p in sell_plans_for("A")]
    ['A_hit']
    >>> [p.key for p in sell_plans_for("F")]
    ['F_hit']

    🔴 **9車の型F だけ `SELL_PLANS` を使わない**（2026-08-30）。決勝は `F_pay`・
       それ以外は `F_line`（**9車だけ三連複**・2026-09-06）と分かれるので、
       固定の集合では表せない。ここでも返すのは**1つだけ**なので
       1レース1商品は保たれる。7車の決勝以外は `F_hit`（三連単12点）のまま。

    >>> [p.key for p in sell_plans_for("F", 9, "決勝")]
    ['F_pay']
    >>> [p.key for p in sell_plans_for("F", 9, "準決勝")]
    ['F_line']
    >>> [p.key for p in sell_plans_for("F", 9, None)]
    ['F_line']
    >>> [p.key for p in sell_plans_for("A", 9, "特選")]
    ['A_hit']
    >>> [p.key for p in sell_plans_for("F", 7)]
    ['F_hit']

    🔴 **型A だけ3つに分かれる**（2026-08-31・`docs/type_lab/type_a_upset_2026_08_31.md` §12）。
       型は排他なので**共食いではなく棲み分け**で、ここでも返すのは1つだけ:

         ① `pw_ent >= ANA_PW_ENT_MIN`（1着率が読めない上位10%）→ `A_ana`（穴狙い）
         ② そうでなく `trio_ok`（三連複2点が入稿ゲートを通る）→ `A_trio`
         ③ 残り → `A_hit`（現行）

       🔴 **判定できないものは A_hit へ倒す**（`pw_ent`/`trio_ok` が None なら現行のまま）。
          `passes_axis_gate` と同じ思想で、分からないことを理由に商品を変えない。
       🔴 **7車だけ**。`ANA_PW_ENT_MIN` は7車の分位で、9車に当てると絶対値切りになる。

    >>> [p.key for p in sell_plans_for("A", 7, pw_ent=1.50, trio_ok=True)]
    ['A_ana']
    >>> [p.key for p in sell_plans_for("A", 7, pw_ent=1.20, trio_ok=True)]
    ['A_trio']
    >>> [p.key for p in sell_plans_for("A", 7, pw_ent=1.20, trio_ok=False)]
    ['A_hit']
    >>> [p.key for p in sell_plans_for("A", 7)]
    ['A_hit']
    >>> [p.key for p in sell_plans_for("A", 9, pw_ent=1.50, trio_ok=True)]
    ['A_hit']
    """
    plans = plans_for(type_label, n_entries, race_type)
    # 🔴 看板枠は**他のどの分岐よりも先**に見る。型A の3分割も 9車の型F の種別分岐も
    #    「その型で何を売るか」の話なので、看板枠に指定された型ではそれらを上書きする。
    #    ここでも返すのは1つだけなので 1レース1商品は保たれる。
    if (type_label in SIGNBOARD_TYPES
            and int(n_entries or 0) == SIGNBOARD_N_ENTRIES
            and (not SIGNBOARD_RACE_TYPES
                 or str(race_type or "") in SIGNBOARD_RACE_TYPES)):
        return [p for p in plans if p.key == f"{type_label}_sign"]
    if type_label == "A":
        key = "A_hit"
        if int(n_entries or 0) == ANA_N_ENTRIES:
            if pw_ent is not None and float(pw_ent) >= ANA_PW_ENT_MIN:
                key = "A_ana"
            elif trio_ok:
                key = "A_trio"
        return [p for p in plans if p.key == key]
    # 🔴 **車数を問わない**（2026-08-31）。7車も 9車も「決勝は F_pay・それ以外は F_hit」。
    #    以前は 9車だけの規則で、7車は `SELL_PLANS` の `F_pay` に落ちていた。
    if type_label == "F":
        key = TYPE_F_SELL_BY_RACE_TYPE.get(
            str(race_type or ""), TYPE_F_SELL_DEFAULT)
        # 🔴 **9車の非決勝だけ三連複へ**（2026-09-06）。決勝系は `F_pay` のまま
        #    （9車の決勝は年40件前後しか無く、ここだけでは採否を決められない）。
        #    7車は測っていないので触らない——車数をまたいだ移植でこのリポジトリは
        #    繰り返し失敗している（7C の定数・3ヘッド軸・7M1）。
        if (key == TYPE_F_SELL_DEFAULT
                and int(n_entries or 0) == TYPE_F_LINE_N_ENTRIES):
            key = TYPE_F_SELL_LINE
        return [p for p in plans if p.key == key]
    return [p for p in plans if p.key in SELL_PLANS]


def build_legs(shape: RaceShape, plan: Plan,
               pred_odds: Mapping[tuple[int, ...] | frozenset, float],
               probs: Mapping[tuple[int, ...] | frozenset, float],
               ) -> list[tuple[int, ...]] | list[frozenset] | None:
    """買い目のリスト。組めなければ None。

    pred_odds / probs のキーは三連単なら (1着,2着,3着) のタプル、三連複なら frozenset。
    """
    order = list(shape.order)
    a1, a2 = order[0], order[1]
    rest = order[2:]

    if plan.bet_type == "trio":
        if plan.structure == "line_axis2_flow":
            # 🔴 **軸は `shape.order` の上位2車ではなくラインから取る**
            #    （`shape.line_pair` = 3着内率の合計が最大のラインの上位2車）。
            #    9車型F の実測（paper 1,958R・探索2025 / 確認2026）:
            #      軸2車がそろう率 36.48 / 37.55%（`order` の上位2車は 35.35 / 31.02%）
            #    軸1・軸2が同一ラインのレースでは**現行の軸と100%一致する**ので、
            #    いま当てられているレースは動かない。効くのは別ライン群だけ。
            #    ⚠️ 軸2を全体の3着内率上位から取ると 31.0%（確認窓）まで落ちる。
            #       **軸2はライン内から取ること。**
            pair = tuple(shape.line_pair)
            if len(pair) != 2:
                return None
            a, b = pair
            cs: list[frozenset] = []
            s = 0.0
            for c in order:
                if c in pair:
                    continue
                k = frozenset({a, b, c})
                o = pred_odds.get(k)
                if not _pos(o):
                    continue
                # 🔴 **超えたら止める（`continue` ではない）。** 相手は3着内率の
                #    降順なので、ここで飛ばして先の高オッズ点を拾うと「安い相手を
                #    外して穴だけ買う」別の商品になる。実測したのは前方から
                #    積める限り積む形。
                if plan.sigma_max and s + 1.0 / float(o) > plan.sigma_max:
                    break
                cs.append(k)
                s += 1.0 / float(o)
                if plan.max_legs and len(cs) >= plan.max_legs:
                    break
            return cs if len(cs) >= TRIO_LINE_MIN_LEGS else None
        if plan.structure == "axis2_flow":
            # 軸2車＋相手を確率降順に n_partners 点。順序を捨てるだけで
            # 相手選びは A_hit と同じ（p3 の並び順）。
            cs = [frozenset({a1, a2, c}) for c in rest[:plan.n_partners]]
            cs = [c for c in cs if _pos(pred_odds.get(c))]
            return cs if len(cs) == plan.n_partners else None
        if plan.structure != "axis2_drop_fav":
            return None
        cs = [frozenset({a1, a2, c}) for c in rest]
        cs = [c for c in cs if _pos(pred_odds.get(c))]
        if len(cs) <= plan.n_partners:
            return None
        cs.sort(key=lambda c: float(pred_odds[c]))          # 予測オッズ昇順＝人気順
        cs = cs[1:]                                          # 最人気を1点外す
        cs.sort(key=lambda c: -float(probs.get(c, 0.0)))
        out = cs[:plan.n_partners]
        return out if len(out) == plan.n_partners else None

    # ── 三連単 ──
    if plan.structure == "fixed12":
        out = [(a1, a2, c) for c in rest[:plan.n_partners]]
    elif plan.structure == "axis1_second2":
        seconds = [a2, order[2]] if len(order) > 2 else [a2]
        pool = [c for c in rest if c not in seconds][:plan.n_partners]
        out = [(a1, s, c) for s in seconds for c in pool]
    elif plan.structure == "all6":
        pool = rest[:plan.n_partners]
        out = []
        for c in pool:
            trio = (a1, a2, c)
            out.extend(p for p in itertools.permutations(trio))
    elif plan.structure == "bust_top":
        # 🔴 **軸1（p3 1位）を買い目から完全に外す。** 「軸1が3着にも入らない」
        #    ことに賭ける商品なので、軸1を含む目を1点でも買うと商品が矛盾する。
        pool = set(order[1:])
        cand = [k for k, v in pred_odds.items()
                if _pos(v) and len(set(k)) == 3 and set(k) <= pool]
        cand.sort(key=lambda k: -float(probs.get(k, 0.0)))
        out = [tuple(k) for k in cand[:plan.max_legs or 5]]
        if len(out) < 2:
            return None
    elif plan.structure == "signboard":
        # 🔴 **看板枠。** 「ダッチにしたときどの点が当たっても計画払戻 T 円以上」を
        #    制約 Σ(1/予測オッズ) <= 予算/T として表し、その枠に確率降順で詰める。
        #    ダッチ配分（`alloc="dutch"`）と対で初めて意味を持つ構成なので、
        #    配分を変えるなら商品が別物になると考えること。
        #
        # 🔴 **並べ方は確率順で固定する。** EV順・オッズ順・確率上位k点→EV順 も
        #    測ったが、10万+/日 も ROI も窓をまたいで判別できない
        #    （`SIGNBOARD_TYPES` の実測表）。**選び方は本数を動かさない。**
        cap = float(BUDGET) / float(SIGNBOARD_TARGET)
        cand = [k for k, v in pred_odds.items()
                if _pos(v) and len(set(k)) == 3
                and float(v) >= plan.min_odds
                and (not plan.max_odds or float(v) <= plan.max_odds)]
        cand.sort(key=lambda k: -float(probs.get(k, 0.0)))
        out, s = [], 0.0
        for k in cand:
            o = float(pred_odds[k])
            if s + 1.0 / o > cap:
                continue
            out.append(tuple(k))
            s += 1.0 / o
        if not out:
            return None
    elif plan.structure == "prob_top":
        cand = [k for k, v in pred_odds.items()
                if _pos(v) and float(v) >= plan.min_odds and len(set(k)) == 3]
        cand.sort(key=lambda k: -float(probs.get(k, 0.0)))
        out = []
        s = 0.0
        for k in cand:
            o = float(pred_odds[k])
            if plan.sigma_max and s + 1.0 / o > plan.sigma_max:
                continue
            out.append(tuple(k))
            s += 1.0 / o
            if plan.max_legs and len(out) >= plan.max_legs:
                break
        if plan.sigma_max and len(out) < 2:
            return None
    else:
        return None

    out = [tuple(c) for c in out if len(set(c)) == 3 and _pos(pred_odds.get(tuple(c)))]
    return out or None


def _pos(v) -> bool:
    try:
        return v is not None and float(v) > 0
    except (TypeError, ValueError):
        return False


# ───────────────────────────── 配分 ─────────────────────────────

def allocate(legs: Sequence, pred_odds: Mapping, probs: Mapping, plan: Plan,
             budget: int = BUDGET, unit: int = UNIT) -> dict | None:
    """買い目 -> 賭け金。組めなければ None。

    'dutch' … 賭け金 ∝ 1/予測オッズ（**払戻を全点で揃える**）
    'conf'  … 各点に floor = 予算×floor_mult ÷ 予測オッズ を置き、残りを**確率に比例**して配る
              （一番期待していない点は floor のまま＝最低 floor_mult 倍・自信のある点ほど厚い）

    🔴 **賭け金が 0 円になった点は返さない**（2026-08-28）。ダッチ配分では
       極端に高い予測オッズの点の取り分が 1 単位（100円）に満たず 0 円になる。
       `prob_top` が Σ(1/予測オッズ) の枠を超えた候補を `break` せず `continue` する
       ため、B_hit ではこれが常態化していた（**実測 3,052行＝B_hit の 61.7%**）:

         - `n_legs` が**実際に買う点数と違う**
         - `pred_min_payout`（画面の「想定最低」）の中央値が **0円**
         - `pred_mean_payout` が 0 点で薄まり、B_hit の設計「想定平均払戻 3万円の床」を
           下回る（中央 26,461円）
         - その点が当たると `hit=true, payout=0` になる（実績0件だが構造上起きうる）

       ⚠️ **お金はほとんど動かない。** 4,742倍の点の重みは 1/4,742 で、落としても
          他の点の配分はほぼ変わらない（＝商品の変更ではなく記録を実態に合わせる修正）。
       ⚠️ 買う集合そのものを変えたい（`continue` → `break`）場合は別の話で、
          そちらは商品が変わるので測ってから決めること。
    """
    legs = list(legs)
    for _ in range(len(legs)):
        stakes = _allocate_once(legs, pred_odds, probs, plan, budget, unit)
        if stakes is None:
            return None
        funded = [c for c in legs if stakes[c] > 0]
        if len(funded) == len(legs):
            return stakes
        if not funded:
            return None
        legs = funded          # 0円の点を外して配り直す
    return None


def _allocate_once(legs: Sequence, pred_odds: Mapping, probs: Mapping, plan: Plan,
                   budget: int, unit: int) -> dict | None:
    k = len(legs)
    n_units = budget // unit
    if k == 0 or k > n_units:
        return None
    o = [float(pred_odds[c]) for c in legs]
    if any(x <= 0 for x in o):
        return None

    if plan.alloc == "dutch":
        w = [1.0 / x for x in o]
        units = _proportional(w, n_units)
    else:
        floor = [max(int(math.ceil(budget * plan.floor_mult / x / unit)), 1) for x in o]
        if sum(floor) > n_units:
            return None
        rest = n_units - sum(floor)
        p = [max(float(probs.get(c, 0.0)), 0.0) for c in legs]
        add = _proportional(p, rest) if rest > 0 and sum(p) > 0 else [0] * k
        if rest > 0 and sum(p) <= 0:
            add = _proportional([1.0] * k, rest)
        units = [f + a for f, a in zip(floor, add)]
    return {c: u * unit for c, u in zip(legs, units)}


def _proportional(w: Sequence[float], n_units: int) -> list[int]:
    """重み w で n_units を配る（各要素0以上・合計ちょうど n_units）。

    配り方の骨格は `unit_distribution` に一本化した（3方針の比較表もそちらにある）。
    ここの方針: 切り捨て → 余りは「比例誤差が最大の点」へ（オッズを見ない）。
    重みが全部0のときだけ均等割りにするのはこの関数固有の扱い。
    """
    k = len(w)
    if n_units <= 0:
        return [0] * k
    if sum(w) <= 0:
        base = [n_units // k] * k
        for i in range(n_units - sum(base)):
            base[i] += 1
        return base
    units = distribute_units(
        dict(enumerate(w)), n_units,
        leftover=FLOOR_THEN_LARGEST_REMAINDER, order=list(range(k)),
    )
    return [units[i] for i in range(k)]


def mean_expected_payout(stakes: Mapping, pred_odds: Mapping) -> float:
    """入稿する買い目の想定払戻の**平均**（円）。表示と検証で同じ値を使う。"""
    if not stakes:
        return 0.0
    return sum(stakes[c] * float(pred_odds[c]) for c in stakes) / len(stakes)


#: ライン決着（同一ラインの3車がそのまま1・2・3着）への差し替えを掛けるプラン。
#:
#: 🔴 **発端はユーザー観察「他の並びの買い目は買っていて外している」**（2026-09-05）。
#:    買い目と実際の決着をライン構成で突き合わせると、はっきりずれていた
#:    （33,730レース・現行ルールで入稿相当・2025+2026）:
#:
#:      決着の形          買い目に占める割合   実際の決着   差    その形が決着したとき集合を買えた率
#:      2ライン(2+1)        71.1%           60.5%    +10.6         50.4%
#:      3ライン バラ         20.0%           20.2%     -0.2         37.5%
#:      **同一ライン3車**      **8.9%**        **19.3%** **-10.4**    **62.2%**
#:
#:    ＝**並びがそのまま来る決着は19.3%起きているのに、買い目の8.9%しか充てていない。**
#:    しかもライン決着は買えたときの集合カバーが最も高い（62.2%）。
#:
#: 🔴 **既存の同ライン隣接ボーナス（`rank_7t3_blend_probs` の λ=2.0 / μ=1.5）では足りない。**
#:    λμ は 1-2着・2-3着の隣接を見るので3車ラインは 3.0倍されているはずだが、
#:    それでも上表の -10.4pt が残る。方向は正しいが量が足りていない。
#:
#: 🔴 **「印（WTの◎○▲△）で並べ替える」案は否定済み。** 同じ枠で4通り測って
#:    全部が基準（モデル確率順）にも市場人気順にも**両窓で負けた**
#:    （印3車4通り優先 36.13/36.53% ↔ 基準 37.30/37.57%）。印の偏り
#:    （◎○＋無印を 29.0% 買って決着は 17.1%）は記述としては本物だが、
#:    印で機械的に並べ替えると悪化する。**効くのはラインだけ。**
#:
#: ⚠️ **`A_hit`（3点）には掛けない。** 下位2点＝本線の2/3を捨てることになり、
#:    実測でも **-3.26 / -1.74pt と両窓で悪化**した。`A_trio` / `D_hit` は三連複で
#:    順序リスクが無く、`A_ana` は軸1を外す商品、`F_sign` / `F_pay` は看板枠なので
#:    いずれも対象外（`F_sign` は 3点しかなく発動も19〜22%）。
LINE_SWAP_PLANS: frozenset[str] = frozenset({"B_hit", "C_hit", "E_hit", "F_hit"})

#: 差し替える点数。**先に2点を試し、入稿ゲートを割るなら1点へ落とす**（下記）。
#:
#: 🔴 **ゲートを割るなら差し替えない、が肝。** 素で2点差し替えると的中は
#:    +3.51 / +3.62pt と大きいが、**平均想定払戻が2万円を割って入稿ゲートの通過が
#:    88.8% → 58.0% に落ちる**（C_hit は 97.9% → 41.6%）。母集団が1/3消えるので
#:    「的中率が上がった」の意味が変わってしまう。ゲートを守る形にすると効果は
#:    半分になるが、**件数・払戻・ROI のどれも犠牲にしない**。
#:
#:    実測（7車・確定オッズ・探索2025 / 確認2026）:
#:
#:      案                    的中          ROI        ゲート通過    払戻中央
#:      現行                25.68 / 26.55  78.3 / 80.6  87.8 / 89.0  24,150 / 25,600
#:      素の2点差し替え       28.61 / 29.33  78.6 / 79.2  78.5 / 80.3  22,592 / 23,398
#:      **2点→無理なら1点**  **27.57 / 28.83**  79.0 / 80.6  87.8 / 89.0  24,262 / 24,873
#:
#:    Δ = **+1.88pt CI[+1.63,+2.13] / +2.27pt CI[+1.91,+2.66]**（レース単位ブートストラップ）。
#:    **無作為対照10本**（同じレース・同じ点数を無作為な目に差し替え）は
#:    24.82〜24.94 / 25.73〜25.89% で **10/10 で勝つ**。発動は 29 / 31%。
#:
#:    プラン別（7車・Δ的中 探索 / 確認・すべて両窓プラス）:
#:      E_hit +6.74 / +6.47（発動68/63%） C_hit +2.89 / +3.88（35/44%）
#:      B_hit +1.25 / +2.29（25/28%）    F_hit +1.26 / +1.16（36/37%）
#:
#:    9車でも同じ向き（+2.12 CI[+1.50,+2.80] / +1.37 CI[+0.53,+2.29]・対照10/10・
#:    発動20%）なので車数で分けていない。**車数依存の定数を持たない**構造的な
#:    操作なので、7車の分位を9車へ当てる過去の失敗とは型が違う。
#: ⚠️ 検証は**確定オッズ**で並べている。本番は予測オッズなので差し替える目の
#:    並び順がぶれる。向きは変わらないはずだが量は前向き実測で確かめること。
LINE_SWAP_LEGS: tuple[int, ...] = (2, 1)
#: 差し替えたあとに残す最低点数。これを割るなら差し替えない。
LINE_SWAP_MIN_KEEP = 3

#: 差し替え先に採ってよい**予測オッズの下限**。
#:
#: 🔴🔴 **これが無いと「当たりは増えないのに払戻だけ下がる日」が出る**（2026-09-05・
#:    導入初日にユーザーが実データで指摘）。ダッチ配分では
#:    **全点の払戻 = 予算 ÷ Σ(1/オッズ)** なので、人気の目（＝安い目）を1点足すと
#:    Σ が跳ね、**既存の当たり目の払戻がその比率でそのまま下がる**。
#:    実例 2026-09-05: 立川12R(C_hit) 36,280 → 18,140円・防府3R(E_hit) 38,000 → 19,000円
#:    ＝ Σ がちょうど2倍。**入稿ゲート（平均想定払戻2万円超）はこれを止められない**
#:    （下がった後でも2万円を超えていれば通ってしまう）。
#:
#: 🔴 **「希釈を一切許さない」ことはできない。** ライン決着の目は人気があるから安く、
#:    安いから配分を食う——同じコインの裏表。差し替え後の想定払戻に下限比 r を
#:    掛けて掃引すると、**r を上げるほど的中の増分が消える**（探索/確認）:
#:
#:      r        発動   Δ的中pt        既存的中の払戻維持
#:      なし   40/44%  +2.53/+3.13      80/79%
#:      0.70   32/35%  +1.13/+1.19      87/87%
#:      0.80   25/27%  +0.59/+0.47      93/94%
#:      0.90   17/18%  **-0.01/-0.01**  98/101%
#:      1.00    8/ 9%  **-0.18/-0.19**  105/108%
#:
#: 🟢 **効いたのは「差し替え先そのものの安さ」に下限を置く形**（比率ではなく絶対値）。
#:    安すぎる候補を飛ばして次に安いものを採るので、**発動はむしろ増える**
#:    （安すぎてゲートに落ちていた分が通るようになる）:
#:
#:      下限     発動   Δ的中pt        既存的中の払戻維持   ROI(探索/確認)
#:      なし   40/44%  +2.53/+3.13      80/79%          77.4/81.7
#:       5倍  49/52%  +2.99/+3.42      81/80%          77.4/81.5
#:      **10倍**  **52/56%**  **+1.48/+1.69**  **92/93%**      **77.3/81.1**
#:      15倍  53/57%  +0.64/+0.72      98/97%          77.2/80.9
#:      20倍  53/57%  +0.17/+0.17     101/100%         77.0/80.7
#:      30倍  53/57%  -0.25/-0.27     103/103%         77.0/80.7
#:
#:    10倍を採ったのは**ユーザーの許容条件**「的中レースが増え、多少の払戻が
#:    下がるなら許容。当たりが増えずに払戻だけ下がるのは想定と異なる」に合うため。
#:    Δ的中 +1.48pt CI[+1.15,+1.81] / +1.69pt CI[+1.22,+2.15]・**無作為対照10本に
#:    10/10 で勝つ**（同じ下限・同じ発動条件で無作為な目に差し替えた対照）。
#:    5倍のほうが的中は伸びるが払戻維持が 80% で導入前と変わらず、指摘の解決にならない。
#:
#: ⚠️ **プラン別では `B_hit` だけ ROI が落ちる**（82.8→80.8 / 82.7→81.3）。
#:    `sigma_max` で想定平均払戻3万円の床を持つ商品なので、10倍の目を足す影響が
#:    他より大きい。的中は +0.69/+0.80pt と両窓プラスなので残しているが、
#:    **前向き実測で最初に見るのはここ**。
LINE_SWAP_MIN_ODDS = 10.0


def line_legs(shape: "RaceShape", plan: Plan, legs: Sequence,
              pred_odds: Mapping, m: int) -> list[tuple[int, ...]] | None:
    """`legs` の確率下位 m 点を「同一ライン3車の目」へ差し替えたものを返す。

    組めなければ None。**点数は変えない。**

    🔴 **ラインは買い目に出ている車だけで数える。** 3車のうち1車でも買い目に
       出ていないラインは対象外（その車を新しく持ち込む操作は測っていない）。
    🔴 **差し替え先は「隊列順」と「先頭×番手だけ入れ替えた順」の2通りを先に採る。**
       ライン内2車の先着を実測すると（2車とも3着以内・215,636ペア）:

         先頭×番手(1-2)  **57.3%**   ← ほぼ五分。**両方向を買う**
         先頭×3番手(1-3)  82.3%      ← 逆転しない。一方向でよい
         番手×3番手(2-3)  87.1%      ← 同上
         4番手が絡む      89%        ← 同上

       3車ラインの先頭×番手に限れば **53.7%** と完全に五分。
       ⚠️ **ユーザー発案**（2026-09-05）。「順位の入れ替わりがありそうなものは両方、
          逆転無さそうであれば1方向のみ」。安い順に2点採る旧案と比べ、
          **同じ的中増を少ない Σ で買える**（探索/確認）:

            安い順          Δ的中 +1.37/+1.59pt  Σ +8.3/+8.8%  既存払戻維持 93/94%
            **先頭×番手優先**  Δ的中 +1.04/+1.34pt  **Σ +5.7/+6.3%**  **維持 95/95%**

          ＝ Σ 1% あたり 0.165pt → **0.182pt** へ改善。残りの4通りは安い順。
    🔴 **`LINE_SWAP_MIN_ODDS` 未満は採らない**（定数のコメント参照）。
    🔴 **落とすのは末尾 m 点。** `prob_top` は確率降順で組まれているので
       末尾＝最も確率の低い点。上位を触ると本線が崩れる（`A_hit` の悪化と同じ理由）。
    """
    if plan.bet_type != "trifecta" or plan.key not in LINE_SWAP_PLANS:
        return None
    legs = list(legs)
    if m < 1 or len(legs) - m < LINE_SWAP_MIN_KEEP:
        return None
    cars = {c for leg in legs for c in leg}
    have = set(legs)

    def _ok(p) -> bool:
        o = pred_odds.get(p)
        # 🔴 安すぎる目は採らない（`LINE_SWAP_MIN_ODDS`）。ダッチ配分では
        #    1点足すだけで既存の当たり目の払戻がその比率で下がるため。
        return p not in have and _pos(o) and float(o) >= LINE_SWAP_MIN_ODDS

    head: list[tuple[int, ...]] = []      # 先頭×番手の2通り（順位が入れ替わる側）
    rest: list[tuple[int, ...]] = []      # それ以外の並び
    for line in shape.lines:
        mem = [c for c in line if c in cars]
        if len(mem) < 3:
            continue
        m3 = tuple(mem[:3])               # 隊列の先頭3車
        pair = (m3[1], m3[0], m3[2])      # 先頭と番手だけ入れ替えた並び
        for p in (m3, pair):
            if _ok(p):
                head.append(p)
        rest += [p for p in itertools.permutations(m3)
                 if p not in (m3, pair) and _ok(p)]
    rest.sort(key=lambda p: float(pred_odds[p]))
    cand = head + rest
    if len(cand) < m:
        return None
    return legs[:len(legs) - m] + cand[:m]


def apply_line_swap(shape: "RaceShape", plan: Plan, legs: Sequence, stakes: Mapping,
                    pred_odds: Mapping, probs: Mapping,
                    min_mean_payout: float = MIN_MEAN_PAYOUT):
    """ライン決着への差し替え。**入稿ゲートを割るなら差し替えない。**

    戻り値は `(legs, stakes)`。掛からなければ受け取ったものをそのまま返す。

    🔴 **賭け金 0 円の点が出る差し替えは採らない**（点数が変わるため）。
    """
    for m in LINE_SWAP_LEGS:
        nl = line_legs(shape, plan, legs, pred_odds, m)
        if not nl:
            continue
        st = allocate(nl, pred_odds, probs, plan)
        if not st or len(st) != len(nl):
            continue
        if mean_expected_payout(st, pred_odds) > min_mean_payout:
            return nl, st
    return list(legs), stakes


#: 入稿ゲート（平均想定払戻 <= `MIN_MEAN_PAYOUT`）に落ちたとき、**代わりに組む買い方**。
#:
#: 🔴 **代替は元と同じ `key` を名乗る。** 行の一意キーは (race_key, plan_key, mode) で、
#:    別名にすると 1レース2商品になり、入稿・採点・画面のすべてが二重に動く。
#:    どちらで組んだかは `min_odds` が行の `legs` に出るので後から判別できる。
#:
#: 🔴 **既に組めている買い目は1つも書き換えない**（置換ではなくフォールバック）。
#:    `F_hit` を丸ごと帯15倍へ置換する案は測って**不採用**にした——型F 単体では
#:    2倍+ が +5.3〜9.0% と良く見えるが、ラインナップ全体では利得が +5.6% に薄まる
#:    一方で**表示的中の −0.8pt はそのまま残る**（2026-09-03・両窓一致）。
#:
#: 実測（`scripts/exp_type_lab/typef_band_lineup.py`・全8プラン込み・探索 / 確認）:
#:
#:              件/日          表示的中        2倍+/日         ROI
#:   現行      39.04 / 39.86  24.68 / 24.76%  6.69 / 7.10   78.2 / 81.7
#:   置換      41.25 / 42.03  23.82 / 23.95%  7.19 / 7.50   77.9 / 81.2
#:   本案      41.25 / 42.02  24.69 / 24.65%  7.12 / 7.43   77.9 / 81.1
#:
#:   ＝**在庫 +2.2件/日・2倍以上の的中 +4.7〜6.5%** を、**表示的中の代償ほぼゼロ**
#:     （+0.01 / −0.11pt）で得る。`F_hit` は平均想定払戻ゲートに 24% 落ちており、
#:     帯を入れると想定払戻が上がって通るようになるのが増分の正体。
#:
#: 🔴 **ROI は改善しない**（−0.3 / −0.6pt）。得られるのは在庫と的中体験であって
#:    収支ではない。この層は ±2.5pt に約15.6年かかるので ROI では採否を決めない。
#: 🔴 **帯は 15倍が最適点で、上げれば上げるほど良いのではない。** 型F 単体の
#:    対応比較（同一レース）で 2倍+ は 15〜20倍が頂点で、30倍では確認窓 −7.2% と
#:    崩れる（払戻中央は上がり続けるのに大きく獲れた回数は減る）。
#: ⚠️ 日次上限との相互作用（件/日 +5.4% が他の商品を押し出さないか）は**未検証**。
GATE_FALLBACK: dict[str, Plan] = {
    "F_hit": Plan("F_hit", "F", "trifecta", "prob_top", 0,
                  min_odds=15.0, max_legs=12, alloc="conf",
                  floor_mult=MIN_PAYOUT_MULT,
                  note="F_hit がゲートに落ちたとき: 予測15倍以上から確率上位12点"),
}

#: 最低2倍の床が置けないときに落とす配分（＝2026-09-05 以前の配分）。
#:
#: 🔴 **床が置けないレースを見送りにしない。** `Σ(1/予測オッズ) > 1/MIN_PAYOUT_MULT`
#:    だと `allocate` が None を返し、そのまま返すと **商品が丸ごと消える**
#:    （実測で hit系の 8.3〜8.5%）。母集団を静かに削るのが一番たちが悪いので、
#:    その分は**変更前の配分のまま売る**。＝ この変更で在庫は1件も減らない。
#: ⚠️ 落ちた分は「2万円未満の点があるレース」として残る（8.3〜8.5%）。
#:    構造的に2倍が組めない買い目なので、なくすには買い目の側を変えるしかない。
ALLOC_BEFORE_MIN_PAYOUT: dict[str, tuple[str, float]] = {
    "A_hit": ("conf", DEFAULT_FLOOR_MULT),
    "B_hit": ("dutch", DEFAULT_FLOOR_MULT),
    "C_hit": ("dutch", DEFAULT_FLOOR_MULT),
    "D_hit": ("dutch", DEFAULT_FLOOR_MULT),
    "E_hit": ("dutch", DEFAULT_FLOOR_MULT),
    "F_hit": ("conf", DEFAULT_FLOOR_MULT),
}


def alloc_fallback(plan: Plan) -> Plan | None:
    """最低2倍が組めないときに使う、変更前の配分のプラン。無ければ None。"""
    before = ALLOC_BEFORE_MIN_PAYOUT.get(plan.key)
    if before is None or (plan.alloc, plan.floor_mult) == before:
        return None
    return replace(plan, alloc=before[0], floor_mult=before[1])


def _build_plan(shape: "RaceShape", plan: Plan,
                pred_odds: Mapping, probs: Mapping):
    """買い目と賭け金。賭け金 0 円の点（`allocate` が落とす）は返さない。"""
    legs = build_legs(shape, plan, pred_odds, probs)
    if not legs:
        return None
    stakes = allocate(legs, pred_odds, probs, plan)
    if not stakes:
        # 🔴 最低2倍の床が置けないだけなら、変更前の配分で売る（見送りにしない）。
        fb = alloc_fallback(plan)
        if fb is not None:
            stakes = allocate(legs, pred_odds, probs, fb)
    if not stakes:
        return None
    return [c for c in legs if c in stakes], stakes


def build_with_gate_fallback(shape: "RaceShape", plan: Plan,
                             pred_odds: Mapping, probs: Mapping,
                             n_entries: int = 7,
                             min_mean_payout: float = MIN_MEAN_PAYOUT):
    """買い目と賭け金を作る。**入稿ゲートに落ちるなら代替の買い方へ切り替える。**

    戻り値は `(legs, stakes, plan_used)`。組めなければ `None`。

    🔴 **paper も live もこの関数を通すこと。** 生成側だけに書くと、検証台と
       本番で母集団が割れる（型ラボが「正本は1つ」を掲げている理由そのもの）。

    🔴 **7車だけに掛ける。** 測ったのは7車で、9車は同じ買い方でも予測オッズの
       分布が丸ごと違う（目が多いぶん高く出る）ため、同じ 15倍 は別の操作になる。
       このリポジトリは車数をまたいだ移植で繰り返し失敗している（7C の定数・
       3ヘッド軸・7M1）。9車で使うなら9車の窓で測り直してから。
    """
    def _done(got, pl: Plan):
        # 🔴 **ライン決着への差し替えは最後に掛ける**（買い方が確定してから）。
        #    ゲートを割るなら差し替えないので、ここで母集団は動かない。
        legs, stakes = apply_line_swap(shape, pl, got[0], got[1],
                                       pred_odds, probs, min_mean_payout)
        return legs, stakes, pl

    if n_entries != 7:
        got = _build_plan(shape, plan, pred_odds, probs)
        return _done(got, plan) if got else None
    def _build(pl: Plan):
        return _build_plan(shape, pl, pred_odds, probs)

    got = _build(plan)
    fb = GATE_FALLBACK.get(plan.key)
    if fb is None:
        return _done(got, plan) if got else None
    # 本命が組めた上でゲートを通るなら、そのまま使う（既存の行は書き換えない）
    if got and mean_expected_payout(got[1], pred_odds) > min_mean_payout:
        return _done(got, plan)
    alt = _build(fb)
    if alt and mean_expected_payout(alt[1], pred_odds) > min_mean_payout:
        return _done(alt, fb)
    # 代替もゲートに落ちるなら、元の結果をそのまま返す（見送りの判断は入稿側）
    return _done(got, plan) if got else None


#: 上帯の配分は必ずダッチ（∝1/予測オッズ）。どの点が当たっても払戻が揃うので、
#: 薄いスライスでも「当たれば投資を超える」が構造的に保たれる。
_UPPER_PLAN = Plan("_upper", "?", "trifecta", "prob_top", 0, alloc="dutch")


def _upper_legs(band: UpperBand, pred_odds: Mapping, probs: Mapping,
                base_legs: Sequence = ()):
    """上帯ひと切れの買い目。組めなければ None。

    `perm_fill` だけ**下帯の買い目**を見る（そこから3車の集合を数える）。
    """
    if band.structure == "perm_fill":
        bought = [tuple(c) for c in base_legs]
        cnt: dict = {}
        for c in bought:
            key = frozenset(c)
            cnt[key] = cnt.get(key, 0) + 1
        have = set(bought)
        cand = []
        for trio, n in cnt.items():
            if n < band.min_bought:
                continue
            for p in itertools.permutations(sorted(trio)):
                if p in have:
                    continue
                o = pred_odds.get(p)
                if _pos(o) and float(o) >= band.min_odds:
                    cand.append(p)
        cand.sort(key=lambda k: -float(probs.get(k, 0.0)))
        out = cand[:band.max_legs] if band.max_legs else cand
        return out or None
    cand = [tuple(k) for k, v in pred_odds.items()
            if _pos(v) and len(set(k)) == 3
            and float(v) >= band.min_odds
            and (not band.max_odds or float(v) <= band.max_odds)]
    cand.sort(key=lambda k: -float(probs.get(k, 0.0)))
    if band.structure == "prob_top":
        out = cand[:band.max_legs or 8]
    elif band.structure == "signboard":
        cap = float(band.budget) / float(band.target)
        out, s = [], 0.0
        for k in cand:
            o = float(pred_odds[k])
            if s + 1.0 / o > cap:
                continue
            out.append(k)
            s += 1.0 / o
    else:
        return None
    return out or None


def add_upper_band(legs: Sequence, stakes: Mapping, plan: Plan,
                   pred_odds: Mapping, probs: Mapping,
                   n_entries: int = 7,
                   bands: Sequence[UpperBand] | None = None,
                   ) -> tuple[list, dict, dict]:
    """下帯の賭け金を縮めて上帯を重ねる。戻り値 `(legs, stakes, roles)`。

    `roles` は 買い目 -> `UpperBand.kind`（下帯は `ROLE_BASE`）。

    🔴 **掛けられないときは今日と同じ商品をそのまま返す**（例外を投げない・
       黙って別物にしない）。掛けないのは次の場合:
       ①7車以外 ②三連複プラン ③上帯が1つでも組めない ④下帯を縮めると組めない。
    🔴 **重複した目は賭け金を足し、役割は下帯のまま**。同じ目を2行に分けると
       入稿でも採点でも二重に数えられる。
    🔴 **呼ぶのは `build_with_gate_fallback` の後**。ゲートの判定（と `F_hit` の
       帯15倍フォールバック）は**下帯を予算全額で組んだ状態**で行う——ここを
       混ぜると入稿母集団が静かに変わる（上の節）。

    >>> po = {(1, 2, 3): 5.0, (1, 3, 2): 9.0, (2, 1, 3): 40.0, (3, 2, 1): 90.0}
    >>> pr = {(1, 2, 3): .2, (1, 3, 2): .1, (2, 1, 3): .03, (3, 2, 1): .01}
    >>> plan = PLANS["F_hit"]                 # 掛かるのは E_hit / F_hit だけ
    >>> legs = [(1, 2, 3), (1, 3, 2)]         # 同じ3車の並びを2通り買っている
    >>> st = allocate(legs, po, pr, plan)
    >>> sum(st.values())
    10000

    ⚠️ 上帯どうしが同じ目を選ぶことは起こりうる（複数の切れを入れた場合）。
       そのときは賭け金が厚くなるだけで行は増えない。`perm_fill` は定義上
       下帯と重ならない（まだ買っていない並びしか拾わない）。

    🔴 いま `UPPER_BANDS` は**空**（2026-09-04 夕方に切り戻し）なので、既定では
       何も重ならない。仕組みを見るときは `bands=` を明示する。

    >>> add_upper_band(legs, st, plan, po, pr)[1] == st      # 既定は素通し
    True
    >>> lg2, st2, roles = add_upper_band(legs, st, plan, po, pr,
    ...                                  bands=(_PERM_BAND,))
    >>> sum(st2.values())
    10000
    >>> sum(v for k, v in st2.items() if roles[k] == ROLE_BASE)
    8000
    >>> sorted(set(roles.values()))
    ['base', 'perm']
    >>> sorted(k for k, v in roles.items() if v == "perm")
    [(2, 1, 3), (3, 2, 1)]
    """
    # 🔴 既定は**呼ばれた時点の** `UPPER_BANDS` を読む。引数の既定値にすると
    #    def 時に束縛され、定数を差し替えても効かない（検証で実際に踏んだ）。
    bands = UPPER_BANDS if bands is None else bands
    plain = (list(legs), dict(stakes), {tuple(c): ROLE_BASE for c in legs})
    if (not bands or int(n_entries or 0) != UPPER_BAND_N_ENTRIES
            or plan.bet_type != "trifecta"
            or (UPPER_BAND_PLANS and plan.key not in UPPER_BAND_PLANS)):
        return plain
    up_total = sum(b.budget for b in bands)
    base = allocate(legs, pred_odds, probs, plan, budget=BUDGET - up_total)
    if not base:
        return plain
    out_legs = [c for c in legs if c in base]
    out: dict = dict(base)
    roles = {c: ROLE_BASE for c in out_legs}
    for band in bands:
        ul = _upper_legs(band, pred_odds, probs, legs)
        if not ul:
            return plain
        us = allocate(ul, pred_odds, probs, _UPPER_PLAN, budget=band.budget)
        if not us:
            return plain
        for c in ul:
            if c not in us:
                continue
            if c in out:
                out[c] += us[c]
            else:
                out[c] = us[c]
                out_legs.append(c)
                roles[c] = band.kind
    return out_legs, out, roles


def split_legs_by_role(legs: Sequence[Mapping], role: str = ROLE_BASE) -> list:
    """行の `legs`（dict の列）から役割で絞る。**役割が無い行は下帯とみなす**。

    >>> split_legs_by_role([{"combo": "1-2-3"}, {"combo": "4-5-6", "role": "band"}])
    [{'combo': '1-2-3'}]
    """
    return [lg for lg in legs if (lg.get("role") or ROLE_BASE) == role]


def min_expected_payout(stakes: Mapping, pred_odds: Mapping) -> float:
    """買った点の**最低**想定払戻（円）。信頼度傾斜の床がここに出る。"""
    if not stakes:
        return 0.0
    return min(stakes[c] * float(pred_odds[c]) for c in stakes)


def rule_version(n_entries: int = 7) -> str:
    """`PLANS` と主要定数から導く版。値を変えると自動で別世代になる。

    🔴 **7車のハッシュは車数の規則を含めない。** 含めると 9車を足しただけで
       既存の 7車 53,017行と新しい行の版が割れ、「規則が変わった」と誤読される
       （7車の買い方は一切変えていない）。9車だけ `NINE_CAR_TYPE_F_*` を混ぜて
       別世代にする — `paper9` の行は**全8プランで作った**ので、決勝限定へ絞った
       今の規則とは実際に別物だから。
    """
    import hashlib
    import json
    payload: dict = (
        {k: [v.bet_type, v.structure, v.n_partners, v.min_odds, v.max_odds,
             v.max_legs, round(v.sigma_max, 6), v.alloc, v.floor_mult]
         for k, v in sorted(PLANS.items())}
        | {"_axis": AXIS_SUM_FIRM, "_behind": BEHIND_MID, "_budget": BUDGET}

        # 🔴 看板枠は**プランの属性だけでは表せない**（計画払戻 T が `build_legs` の
        #    中で使われる）。ここに入れておかないと T を動かしても版が割れず、
        #    新旧の行が同じ `rule_version` で混ざる。
        | {"_sign": [list(SIGNBOARD_TYPES), SIGNBOARD_TARGET,
                     SIGNBOARD_MAX_ODDS, SIGNBOARD_N_ENTRIES,
                     sorted(SIGNBOARD_RACE_TYPES)]}

        # 🔴 ライン決着への差し替えも `PLANS` の属性では表せない（`build_with_gate_fallback`
        #    の中で効く）。ここへ入れないと対象プランや差し替え点数を動かしても
        #    版が割れず、新旧の行が同じ `rule_version` で混ざる（`_sign` と同じ理由）。
        #    ⚠️ 7車にも9車にも掛けているので、`if n_entries == 7` の中には入れない。
        | {"_line": [sorted(LINE_SWAP_PLANS), list(LINE_SWAP_LEGS),
                     LINE_SWAP_MIN_KEEP, LINE_SWAP_MIN_ODDS]})
    if n_entries == 7:
        # 🔴 フォールバックは `PLANS` に無いので、ここへ入れないと帯を動かしても
        #    版が割れず新旧の行が混ざる（`_sign` と同じ理由）。
        #    ⚠️ **7車のときだけ入れる。** 9車には掛けていないので、9車の版まで
        #       割ると「規則が変わった」と誤読される（車数の規則を7車のハッシュへ
        #       入れない、という既存の判断と同じ理由）。
        # 🔴 上帯も `PLANS` の外なので、ここへ入れないと帯や予算を動かしても
        #    版が割れず新旧の行が混ざる（`_sign` / `_fallback` と同じ理由）。
        payload["_upper"] = [[[b.kind, b.budget, b.structure, b.min_odds,
                               b.max_odds, b.max_legs, b.target, b.min_bought]
                              for b in UPPER_BANDS],
                             sorted(UPPER_BAND_PLANS)]
        payload["_fallback"] = {
            k: [v.bet_type, v.structure, v.n_partners, v.min_odds, v.max_odds,
                v.max_legs, round(v.sigma_max, 6), v.alloc, v.floor_mult]
            for k, v in sorted(GATE_FALLBACK.items())}
    if n_entries == 9:
        # 🔴 ルーティング（決勝以外を `F_line` へ）と Σ の上限は `PLANS` の
        #    属性だけでは表せないので、ここへ入れないと新旧の行が同じ
        #    `rule_version` で混ざる（`_sign` / `_line` と同じ理由）。
        payload["_sell9"] = [list(NINE_CAR_TYPE_F_RACE_TYPES),
                             list(NINE_CAR_TYPE_F_PLANS),
                             TYPE_F_SELL_LINE, TYPE_F_LINE_N_ENTRIES,
                             round(TRIO_LINE_SIGMA_MAX, 6), TRIO_LINE_MIN_LEGS]
    return hashlib.sha1(
        json.dumps(payload, sort_keys=True, ensure_ascii=False).encode()
    ).hexdigest()[:12]
