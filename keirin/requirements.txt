# Data collection
requests==2.31.0
beautifulsoup4==4.12.3
selenium==4.18.1
webdriver-manager==4.0.1
lxml==5.1.0

# Data processing
pandas==2.2.1
numpy==1.26.4
sqlalchemy==2.0.28

# Machine learning
scikit-learn==1.4.1
xgboost==2.0.3
# ⚠️ 学習(Mac)と推論(VPS)で版が食い違うと、モデル成果物を「作った版より古い版で読む」
#    という壊れやすい向きになる。2026-08-11 に Mac 4.6.0 / VPS 4.3.0 のずれを解消し
#    **4.6.0 へ揃えた**（Mac は numpy 2.x のため 4.3.0 へ下げられない＝4.3.0 は numpy 2 未対応）。
#    ずれていた間も予測はビット単位で一致していた（実測）が、潜在リスクとして解消した。
#    版を動かすときは両機を同時に揃えること。片側だけ動かすと静かに逆向きになる。
lightgbm==4.6.0
optuna==3.5.0

# Analysis
jupyter==1.0.0
matplotlib==3.8.3
seaborn==0.13.2

# CLI
click==8.1.7

# Database
# sqlite3 is built-in

# Testing
pytest==8.1.0

# SNS
tweepy==4.14.0
