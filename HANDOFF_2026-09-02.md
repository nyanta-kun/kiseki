# 引き継ぎ 2026-09-02 — リポジトリ監査と、面の色の3状態化

> 📇 引き継ぎメモの索引は [`HANDOFF.md`](HANDOFF.md)。**2026-09-02 時点のスナップショット**であって、
> 現在の運用ルールではない。正本は `CLAUDE.md`（競輪の推奨は
> `keirin/docs/RECOMMENDATION.md`）。食い違ったら正本が正しい。
> 「次にやること」は書いた時点の未着手リストなので、着手前に済んでいないか確認すること。

前回（[2026-08-31](HANDOFF_2026-08-31.md)）以降、**PR #388〜#424 の 37本**が入っている。
性格の違う2本のセッションが並んでいるので分けて書く。

---

## 1. 競輪（8/31 夕〜9/1）— 型ラボの運用調整

| PR | 中身 |
|---|---|
| #388〜#390 | 看板枠（高額払戻を作る買い方）を新設 → 7車の型F を「決勝は `F_pay`・それ以外は `F_hit`」へ |
| #392 / #394 | 軸信頼ゲートの素通り2件を塞ぐ（看板レース / `A_ana`・`A_trio`） |
| #393 | 買い目の絞り込みを**確率順＋同ライン隣接ボーナス**へ（点数は据え置き） |
| #395 | **ユーザー判断**: 型C の帯 20倍 → **15倍**・型D の相手 4点 → **3点**・型F へ看板枠を戻す |
| #396〜#399 | 型ラボに日次上限（レース数の半分・決勝/準決勝は枠外）。分母の定義を2回直した |
| #400 | `favbust` の月次 vintage を自動生成（9/1 の障害対応） |
| #401 | 日次上限の「残す順」に `rp_sd`（競走得点のレース内ばらつき）を混ぜる |

🔴 **8/31 の引き継ぎに書いてあった「型C の合成オッズ下限 4倍」は入っていない。**
同じ不満（12点買って当たらない）に対して #395 の**帯 15倍**という別の手を当てた。
合成下限の仕組みは `keirin/src/type_lab.py` には無い（実験スクリプトだけがある:
`keirin/scripts/exp_type_lab/points_floor.py` / `synthetic_odds_floor.py`）。
**まだ要るのかを先に判断すること。**

## 2. リポジトリ監査（9/1 夜〜9/2）— 「静かに死んでいたもの」を掘り起こす

数字を動かす作業ではなく、**壊れても誰も気づかない箇所**を潰す作業。

| PR | 見つかった状態 |
|---|---|
| #406 | 並列開発ガードが `backend/scripts`(118件) と `frontend`(104件) を `other` に落としており、**shared 警告にも複数柱警告にも一切かからなかった** |
| #407 | 地方の複穴カテゴリが名前と別ルールを配信。個別馬バッジは v11〜v14 の間ずっと点いていなかった |
| #408 | JRA スイートスポットの赤字表示が、描画先ファイルの置き換えで**参照ゼロ**になり静かに落ちていた |
| #413 / #414 | 地方5カテゴリ推奨は画面もエンドポイントも消費者ゼロ → 実稼働停止・コードは参考として保持 |
| #417 | `windows-agent` の pytest 収集が壊れていた／特徴量の死活監視 LaunchAgent が動いていなかった |
| #402〜#404 | 未追跡だった検証スクリプト72本・ツール設定・引き継ぎメモを git 管理下へ |
| #405 | 競輪の推奨の現状を `keirin/docs/RECOMMENDATION.md` 1本へ集約し、古い文書70本を整理 |
| #416 | 引き継ぎメモの索引 `HANDOFF.md` を新設し、`test_handoff_index.py` で機械的に縛った |
| #419 | `backend/models` から検証記録を分離し、参照ゼロの旧モデル5本を削除 |
| #420 | **Vitest を純ロジック限定で導入**（TS 側に初めてテスト基盤が入った） |
| #422 | ページ面の色をテーマトークンへ寄せ、light / OS dark / `[data-theme]` の3状態を設計 |
| #423 | 入稿確認画面の文面を既定で畳む |

## 3. 今日やったこと — #422 の「目視できていない」を閉じた（PR #424）

#422 は面だけをトークン化し、その上に載る文字は `text-gray-*` のまま残していた。
**面はテーマで反転するが Tailwind の固定色は反転しない**ので、dark で見出しが消えていた。

    /my 「マイページ」        text-gray-800  コントラスト比 1.18（ほぼ不可視）
    /results 「AI指数 実績」  text-gray-800              1.18
    /yoso 「予想一覧」        text-gray-700              1.68
    /races 「全36レースを…」  text-gray-500              3.58

`--surface-heading` / `--surface-muted` を3状態すべてで定義し、面の上の文字だけ置換した
（**カードの中は従来どおり**）。静的検査は「全ての面 × 見出し/補足が light / dark とも
4.5:1 以上」を CSS の hex から実際に計算する形にしてある（実測 6.9〜15.2）。

🔴 **確認の手順が確立した。** `LOCAL_AUTH_BYPASS` で認証を一時迂回して実データを描画し、
`data-theme` を切り替えて全テキストのコントラスト比を計測する。
手順とハマりどころはメモリ `local-visual-verification-gated-pages` にある。
「ローカルでは認証で開けないから目視できない」は**もう理由にならない**。

---

## 4. 次にやること（2026-09-02 時点の未着手リスト）

### すぐ効く

1. **カード類のダーク対応** — #422 は面だけを暗くし `bg-white` のカードは範囲外にした。
   カード内の `text-gray-*` を全部見直す必要がある（light でも 2.1〜3.8 と低いものが混ざっている）
2. `/chihou/results` の目視 — 上流 API が3〜5分かかり、#424 では描画まで到達できなかった

### 競輪

3. ~~**型C の合成オッズ下限 4倍**~~ → **不採用で決着**（2026-09-02・下記5.）
4. `RECOMMENDATION.md` §8「いま決まっていないこと」5件（券種変更 / モデル確率の較正崩れを
   `p3` の特徴量側で直す / `rp_sd` を `p3` の特徴量へ / 旧ランクのペーパー継続 / カバレッジ τ の一本化）
5. 看板穴埋めを型ラボで埋めるか（未決）
6. 二車複の新設（予測オッズ・採点・入稿の3箇所が要る・要判断）

### 中央 / 地方

7. **DM 欠損の閾値監視**（`docs/jra_rebuild_2026_08.md`・レース内ばらつきが 28〜30 → 14 前後に落ちる）
8. 地方 P2 の残り（`calculated_indices` 旧世代の削除 / `odds_history` 9.9GB の保持方針）
9. 地方 P4（`passing_*` 97.9%・`running_style` 100% 充足で**欠測ではなく未活用**）

### 片付け

10. マージ済みローカルブランチ25本と worktree 3つ（`axis-gate-40` / `gate-thresholds` / `marquee-gate`）
11. `.planning/` が 2026-08-03 で停止

## 5. 型C の合成下限は不採用で決着した（2026-09-02 午後）

8/31 に承認済みだった「合成オッズ4倍の下限」は、**帯20倍の paper の上で測った値**だった。
翌 09-01 の #395 で帯を15倍へ下げたあとに測り直すと、代償が約2倍になっていた:

    4倍で C_hit が失う表示的中   −3.0pt（承認時）→ **−7.4pt**（現行）
    全体の表示的中              −0.9/−0.7pt   → **−1.6/−1.6pt**
    ROI                        ほぼ不変        → 探索 +0.8 / 確認 **−3.8pt**（符号反転）

帯を下げると1点が安くなり合成オッズが下がる（live 中央 3.39→2.61倍）ので、
同じ下限でも切る点数が 12→6 に増える。`type_lab.py` の
「**帯を下げる**と**点数を減らす**を同時にやってはいけない」という規律そのものだった。

🔴 **測り直しには paper の再生成が要る**（20か月・98,244行）。商品の定義を変えると
`type_lab_picks` の paper は古い版のまま残るので、**版で絞らずに測ると別の商品の話になる**。
2025 は vintage モデル・2026 は板 npz と作り分けること（板は2025では in-sample）。
手順と実測: `keirin/docs/type_lab/synthetic_floor_recheck_2026_09_02.md`

## 6. この日の状態（2026-09-02 朝の実測）

- `main` クリーン・CI 緑・Open PR / Issue **ゼロ**
- LaunchAgent 4本（`db-backup` / `dm-auto-fetch` / `feature-health` / `jra-odds-prune`）とも `LastExitStatus = 0`

## 7. 手順の教訓

- **worktree には `node_modules` も `backend/.venv` も無い。** そのままだと
  `preflight.sh` の eslint / tsc / vitest / Python 検査が**一つも走らずに失敗**する
  （幸い skip ではなく失敗として出る）。本体からシンボリックリンクを張れば足りる
- **「目視できない」は静的検査に置き換えるだけでは閉じない。** #422 は構造（面の色の
  直書き・3状態の定義）を縛ったが、**その上に載る文字**は検査の外にあり、実際に壊れていた。
  検査は「壊れ方を1つ固定する」ものであって、目視の代わりにはならない
