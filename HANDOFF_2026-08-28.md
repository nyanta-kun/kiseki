# 引き継ぎ 2026-08-28 — 型ラボ（窓の拡張・軸信頼ゲート・9車対応）

> 📇 引き継ぎメモの索引は [`HANDOFF.md`](HANDOFF.md)。**2026-08-28 時点のスナップショット**であって、
> 現在の運用ルールではない。正本は `CLAUDE.md`（競輪の推奨は
> `keirin/docs/RECOMMENDATION.md`）。食い違ったら正本が正しい。
> 「次にやること」は書いた時点の未着手リストなので、着手前に済んでいないか確認すること。


このセッションで **main へ入れたもの**と、**次に判断が要ること**。
数字の正本は各 doc、判定の正本はコードのコメント。

## 1. main に入ったもの（すべてデプロイ済み・確認済み）

| PR | 中身 |
|---|---|
| #339 | 三連単オッズモデルの**出力先を差し替え可能に**（`KEIRIN_ODDS_TF_MODEL_DIR`） |
| #346 | **窓を20か月へ** + **軸信頼ゲートを下位1/5へ緩和**（本番稼働中） |
| #347 | **`odds_tf_n9` 新設** + 型ラボの `--n-entries 9` + 月ごと保存 |
| #264 | closed（7T2 は 7T3・型ラボへ設計が引き継がれた） |

## 2. 🔴 データが増えた — 窓が 8か月 → 20か月になった

    探索窓  2025-01-01〜2025-12-31（12か月・**このセッションで新規生成**）
    確認窓  2026-01-01〜2026-08-26（ 8か月）

`keirin.type_lab_picks` の現状:

| mode | 行 | 期間 |
|---|---|---|
| `paper` | 53,017 | 2025-01-01〜2026-08-26（**欠けなし**） |
| `paper9` | 5,783 | 2025-01-04〜2026-08-04（9車・検証用） |
| `live` | 日々増加 | 2026-08-27〜 |

🔴 **2025年ぶんは vintage オッズで作った。** 本番の `odds_tf_n7` は train_end
2025-12-31 なので、**そのままでは2025年が in-sample になる**。
`data/models_vintage/odds_tf_te20241231/`（train_end 2024-12-31）を
`KEIRIN_ODDS_TF_MODEL_DIR` で指して生成した。
⚠️ **`data/` は git 管理外なのでこの vintage モデルは Mac にしか無い。**
2025年を作り直すときは同じ環境変数で同じディレクトリを指すこと。

🔴 **`paper9` は mode に車数を付けてある。** API と画面は `paper`/`live` しか
読まないので画面には出ない。7車の集計に混ぜないこと。

## 3. 軸信頼ゲート（本番稼働中・**採用ではなく検証中**）

正本: `backend/src/services/keirin_type_lab_gate.py`

各プランの中で軸信頼（上位2車の3着内率の合計）が**下位1/5**のレースを外す。
閾値は**探索窓2025年のプラン内五分位**由来。

| | 件/日 | 表示的中 | ROI | 全体との差 |
|---|---|---|---|---|
| 絞らない | 49.7 | 25.33% | 79.3% | — |
| **下位1/5（現行）** | **39.2** | **27.12%** | **82.4%** | **+3.1pt CI[+1.3,+4.8]** |

無作為に同数を落とす対照20本に **20/20 で勝つ**。探索窓でも同符号。
プラン別の向きは 5/6 が両窓プラス。

🔴 **絶対閾値では効かない**（1.20〜1.44 のどこで切っても差が0を跨ぐ）。
効くのは「各プランの中で相対的に下を外す」形だけ。**この性質を壊さないこと。**

## 4. 9車 — 結論は「決勝は出す・型Fのその他は売らない」

詳細: `keirin/docs/type_lab/carcount_2026_08_27.md`（追記部）

| | 件/日 | 表示的中 | ROI（探索/確認） |
|---|---|---|---|
| 9車 全8プラン | 17.6 | 15.19% | 69.8% / 72.8%（**両窓とも壁の下**） |
| **9車 型Fを売らない** | 5.6 | 22.89% | **83.0% / 89.1%** |

型Fの的中は **①軸2車そろい 31.35% × ②相手カバー 51.29%** に分解できる（実測と完全一致）。
7車は 39.36% × 63.00%。**①と②がほぼ半分ずつ効いている。**

🔴 打ち手は3通りとも壁を超えない（相手増 64→70% / 荒れ度は窓で反転 /
1着固定フォーメーション 57〜65%）。同じフォーメーションが**7車では ROI 77.0%** なので
**形ではなく母集団の問題**。

🟢 **9車の決勝は 100% が型F**。現行 F_hit のまま 表示的中 14〜21%・
払戻中央 3.5〜5.2万円で型F全体より良い側。ただし **n=66（0.11件/日）で CI [24,174]**
＝収支は判定できない。「看板は必ず出す」方針とは衝突しない。

## 5. 🔴 今日踏んだ落とし穴（同じ轍を踏まないため）

1. **`ODDS_SENTINEL = 9000` が9車で壊れる** — 9車の三連単は 8.2% が表示上限
   9999.9 に張り付き、「504点そろっていない」で **5,698R 中 4,834R がレースごと除外**、
   学習が 339R まで痩せていた。打ち切り位置は**車数別に持つ**（修正済み）
2. **メタの丸ごと上書き** — 9車を学習すると `target_sum["7"]` が消え、本番の
   三連単予測オッズが**全滅**する（出るのは翌朝の入稿時）。車数別マージ + テスト5本（修正済み）
3. **長時間ジョブの一括保存** — VPS DB のタイムアウト1回で **2025-Q2 が丸ごと消えた**。
   原因はおそらく**自分の分析クエリ**（同時刻に `wt_odds` を 48,783レース読んでいた）。
   → 月ごと保存へ変更（修正済み）。**VPS へ重いクエリを投げるときは他ジョブと重ねない**
4. **ブランチ切り替えで実装が作業ツリーから消えた** — 長時間ジョブを走らせている
   作業ツリーで `git switch` すると、次に起動する Python が別のコードを読む。
   例外もログも出ない。**コミット・PR は別 worktree で**
5. **`"決勝" in race_type` が準決勝も拾う** — CLAUDE.md に明記されている既知の罠。
   完全一致で書くこと
6. **古い worktree からファイルを丸ごとコピーして退行させかけた** — テストが検知した。
   worktree 間で持ち込むときは**差分だけ当てる**

## 6. 次に判断が要ること

1. **9車を実投入するか**（「決勝は現行 F_hit・型Fのその他は売らない」）。
   実装は `--n-entries` があるので日次バッチの設定が中心
2. **集中型プランを足すか** — 上位2点（EV順）で **10万+ が約10倍**（0.08→0.97件/日）。
   ROI は不動、**代償は表示的中 25% → 5%**。店頭での並走設計が要る
   （netkeirin の主要ソートは表示的中率なので単独では売れない）
3. **`gap 3/4` の事前登録** — 9車型Fで唯一両窓一貫したセル（79.2% → 80.1%）。
   ただし **16セル中1つ**なので多重比較の範囲。使うなら事前登録して新しい窓で一度きり
4. **7車の1着固定フォーメーション** — 現行 F_hit より表示的中で 5.3pt 劣るが
   **10万+ が3.1倍**。帯のダイヤルとしてなら選べる

## 7. 再現に使うもの

    keirin/scripts/exp_type_lab/
      race_filter{,2,3}.py   レース属性での絞り込み（🔴 全部否定済み）
      ev_axis_rank.py        EV・的中確率・軸信頼をプラン内の順位付けに使えるか
      axis_gate.py           軸信頼ゲートの検定（対照つき）
      concentrate.py         点数を絞る（集中型）
      carcount.py            7/9/6車の型分割の比較
      carcount6.py           6車・9車の walk-forward 予測を作る（EXP_N_CAR で車数指定）
      nine_feasibility.py    9車のプラン成績
      typef_{anatomy,fix,formation,segments}.py  型Fの分解と打ち手

窓は `race_filter.py` の `EXPLORE` / `CONFIRM`（環境変数 `TL_EX_FROM` 等で上書き可）。

---

# 追記 — 型ラボの「答え合わせ」側（セッション: 競輪 / 2026-08-27〜28）

上の引き継ぎ（生成・9車対応）と**補完関係**。こちらは **`type_lab_picks` に決着の答え合わせを載せた**話。

## A. main に入ったもの（すべてデプロイ・本番確認済み）

| PR | 中身 |
|---|---|
| #340 | 型ラボの採点を **15分ごと**（`5,20,35,50 8-23,0`）へ・**前日+当日**の2本立て |
| #341 | **型分けの答え合わせマトリクス**（`p3_order` / `win_tf_odds` の2列 + `/outcome` API + 画面）|
| #342→#343 | 並び復元の判定を「行が合うか」→「**ソースが正しいか**」へ |
| #344 | 充填状況の doc |
| #345 | 答え合わせを **9車（`mode=paper9`）** にも通す |
| #348 | 9車モード選択のラベル修正（「三連複のみ」は誤り） |
| #349 | （相手セッション）三連単オッズモデルの VPS 配布漏れ — マージ済み |

Alembic: `202608272200_keirin`（`p3_order` / `win_tf_odds`）。ORM `KeirinTypeLabPick` も追加
（`type_lab_picks` はモデルが無く autogenerate が **DROP TABLE を作りうる**状態だった）。

## B. 見るところ

`/keirin/type-lab` の「**型分けの答え合わせ**」。モードは 実地 / ペーパー / **9車ペーパー（検証）**。
API `GET /api/keirin/type-lab/outcome`、計算の正本は
`backend/src/services/keirin_type_lab_outcome.py`（DB にも FastAPI にも依存しない純関数）。
実測と設計: `keirin/docs/type_lab/outcome_matrix_2026_08_27.md`。

表は4つ: ①型×決着クラス ②相手の開き(gap)×決着クラス ③型×確定三連単オッズ帯
④プラン×決着クラス（**セルは的中率**＝どこで落としているか）。

**決着クラス**＝1〜3着の3車が指数順位のどこから来たか:
`順当`(軸2車+指数3〜4位) / `軸2+穴`(軸2車+指数5〜7位) / `片軸+中位` / `片軸+穴` / `軸崩壊`。

## C. データの現況（2026-08-28 時点・充填済み）

| mode | 行 | `p3_order` | `win_tf_odds` |
|---|--:|--:|--:|
| `paper` | 53,017 | 52,984 | 52,658 |
| `paper9` | 5,783 | 5,781 | 5,733 |
| `live` | 94〜 | 全件 | 全件 |

残りは**軸不一致で意図的に飛ばした35行**とオッズが引けない十数行だけ。
🟢 **以後のバックフィルは不要**（`build_type_lab_picks.py` の `COLS` に `p3_order` が入っており
生成時に書かれる）。作り直した期間があるときだけ `backfill_type_lab_outcome.py` を回す。

## D. 🔴 効いた結論

1. **層①②③はすべて単調に再現**（7車・通期 34,811R）:
   順当 A 50.1%→F 24.8% / 軸崩壊 3.9%→10.7% / 決着の中央倍率 17.5→57.5倍 /
   gap 小→大 で順当 28.1→46.7%
2. 🔴🔴 **軸崩壊は全8プランで的中0%**。7車 7.1% / 9車 12.6% が**構造的に取れない**。
   軸2車前提の設計では買い方を変えても届かない＝**単一の最大損失要因**
3. 🔴 **D_hit は「順当」より「軸2+穴」で当たる**（7車 44.1% ↔ 87.9%）。
   「最人気の相手を1点外す」で落としているのが**多くの場合 指数3位＝最も来やすい相手**
4. 🔴 **C_hit / E_hit は帯で切るぶん「順当」を落とす**
5. 🟢 **F_hit の的中率＝その型の順当率**（買い目が順当の定義そのもの・マトリクスで
   順当100%/他0%）。9車Fの順当率は 17.4% しかなく、しかも**9車はFが母集団の55〜59%**
   → 相手セッションの「**9車は型Fを売らない**」を機序で裏づける
6. 🟢 **9車 vs 7車で両窓一貫して差が出るのは F_hit / F_pay だけ**。
   他6プランは窓で符号が反転＝**差は読めない**

## E. 🔴 踏んだ落とし穴（同じ轍を踏まないため）

1. **復元の判定は「行が合うか」でなく「ソースが正しいか」。**
   照合できるのは `axis1`/`axis2` ＝先頭2つだけで、**違うソースだと先頭2つが合った行でも
   半分は3位以下が違う**（2025-07-15 実測: 47行中 完全一致24 / 先頭2つだけ一致23）。
   最初「先頭2つ一致で書く」実装で 2025年 16,864行を誤充填 → 全 NULL へ戻した。
   次に「1行でも不一致なら不採用」にしたら 0.07% でも落ちて厳しすぎた。
   → **食い違い率で判定**（`AXIS_MISMATCH_LIMIT_PCT=1%`）。実測は桁で分かれる
   （正しいソース 0.00〜0.24% ↔ 板で2025を復元 34%）。**実測してから閾値を置くこと**
2. 🔴 **オッズの出所は「メタを読む」でなく「実測で突き合わせる」。**
   `odds_tf_n7` のメタ（train_end 2025-12-31）だけ見て「7車の2025年は in-sample」と
   報告しかけた。正しくは保存済み `legs[].pred_odds` と再計算の一致率:
   **2025 = vintage(te2024-12-31) 100% / 2026-01〜08-04 = 板 npz の PO 100%**
   → **両窓とも OOS**。探索2025 / 確認2026 の二窓で比べてよい
3. **`--order-from-models` は vintage 窓ごとにまとめて予測する。**
   1日ずつだと 2025年で9時間・窓一括なら 1か月 1分23秒（40倍）
4. **採点は上流（`intraday_results_wt.sh` */15）の刻みに合わせる。** 毎時1回だと
   最大60分遅れ、当日ぶんだけ見ていると**23:30発走のミッドナイト最終Rが翌朝まで埋まらない**
5. **新テーブルを `create_table` だけで足すと ORM 同期ガードをすり抜ける**
   （`add_column` を1つも持たないテーブルは検査対象に入らない）
6. **デプロイ直後の 502 は blue-green の入れ替え中**。コンテナの `Up N seconds` を見る

## F. 未決（双方とも手を付けていない）

1. **9車の実投入**（結論候補: 決勝は現行 F_hit・型Fのその他は売らない）
2. **集中型プランの追加**（10万+ が約10倍 ↔ 表示的中 25%→5%）
3. **`gap 3/4` の事前登録**（9車型Fで唯一両窓一貫だが16セル中1つ）
4. **7車の1着固定フォーメーション**（表示的中 −5.3pt ↔ 10万+ 3.1倍）

🟢 判断材料は揃っている。**`gap` は `race_shape` が計算しているのに型判定にも買い目にも
使われていない**——通期で順当 28.1→46.7%・軸崩壊 9.0→5.0% と型A↔Fに匹敵する分離があり、
見送り判断・点数の増減の候補（⚠️ 軸の選定に使うのは別途否定済み）。

## G. vintage オッズモデル（2026-08-28 に複製済み）

`data/models_vintage/odds_tf_te20241231/`（train_end 2024-12-31）は **git の外**。
2026-08-28 に Mac → VPS の同じパスへ複製し**2部**にした（md5 一致・本番非影響を確認）。
2025年を作り直すときは `KEIRIN_ODDS_TF_MODEL_DIR` でここを指す。**指し忘れると
本番モデル（train_end 2025-12-31）が使われ 2025年が静かに in-sample になる。**
