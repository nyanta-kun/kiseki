# 引き継ぎ 2026-08-24（JRA）

前日分は `HANDOFF_2026-08-23.md`。このセッションは
「地方競馬で同じ walk-forward を当てる」から始まり、**穴馬検出の新指標を見つけ**、
その指標を**開催日1日まるごと実地で回し**、あわせて**データ基盤の障害を3件つぶした**。

---

## 1. 本題 — 三連複で ROI 1.0 に届くか

### 前提（前日から変わらず）

現行指数の市場への上乗せは複勝で +0.010、三連複6点で +0.066。
三連複6点で ROI 1.0 に必要な w は 0.031、現在地は w≈0.009。

### 今日つぶしたレバー（同じ穴を掘らないこと）

| 施策 | 結果 |
|---|---|
| 地方競馬への横展開 | **打ち切り**。上乗せは JRA の1/3（後述） |
| 三連複 EV による**買い目**選別 | **構造的に不成立**（後述） |
| 市場順とモデル順の混合（w スキャン） | 内側に最適点なし。3点/6点/12点とも現行 w=1 で十分 |
| エキゾチック marginal を3ヘッドの特徴に追加 | ROI 差は 3点 +0.035 / 6点 −0.060 / 12点 +0.050、**全てCIがゼロ跨ぎ** |

⚠️ 最後の1つは**学習2,082R / 評価1,368Rの過小検定**。backfill で母集団が2.7倍になったので
**やり直す価値がある**。

### 唯一生きている線: 券種プール間の食い違い（D）

```
M_trio = 三連複の全組オッズから逆算した「3着以内」確率（Σ=3）
M_win  = 単勝オッズだけから Henery(λ=0.82) で組んだ同じ量（Σ=3）
D = log M_trio − log M_win
```

**モデルを一切使わない。** 穴馬帯（単勝12倍以上・31,605頭 / 3,450R）で複勝的中の識別力:

| | AUC | 増分 |
|---|---|---|
| 単勝オッズのみ | 0.7461 | — |
| +D | 0.7526 | **+0.0065** |
| +D+M_trio | 0.7541 | **+0.0080** |

これまでどの軸も +0.0005〜0.002 だったので**一桁違う**。

**本物らしさの根拠**
1. オッズ帯を細かく揃えた8帯**すべて**で複勝率が上（+2.0〜+6.9pt）
2. **両側性**: D≤−0.3 は複勝率 0.020〜0.049・ROI 0.33〜0.56
3. 期間分割（閾値を探索期間で固定）: D≥0.3 → 0.721/0.917/0.889、D≥0.5 → 1.190/1.349/1.263
4. **複勝市場では再現しない**。`log M_place − log M_win` は ROI 0.647→0.638 と無力

🔴 **ただし三連複固有ではない。** 馬連 +0.0059 / ワイド +0.0047 も同じ情報を持つが、
三連複を統制すると他は +0.0001 しか足さない＝**全部が同じ一つの情報**。
「変換(Henery)が下手なだけ」説は潰した: 単勝プールだけで8列に厚くした LightGBM が
out-of-sample AUC 0.7179 なのに対し、**エキゾチック5列だけで 0.7238、単勝を足すと −0.0029**。

---

## 2. 🔴 実地1日（2026-08-23・36レース）で分かったこと

**発見当日にそのまま実戦形式で回した。ここが今日いちばん重要。**

| | 頭数 | 的中 | 複勝率 | ROI |
|---|---|---|---|---|
| 候補 D≥0.3 | 28 | 6 | 0.214 | **0.607** |
| 参考: 単勝12倍以上を全部買う | 303 | 40 | 0.132 | **0.949** |

**選別しないほうが良かった。** ただし帯別の単調性は保たれている（順位相関 +0.886）:

| D 帯 | 頭数 | 複勝率 | ROI | 中央単勝 |
|---|---|---|---|---|
| ≤−0.3 | 9 | 0.000 | 0.000 | 104.9 |
| −0.3〜−0.15 | 33 | 0.091 | 1.473 | 75.5 |
| −0.15〜0 | 62 | 0.048 | 0.481 | 55.9 |
| 0〜0.15 | 101 | 0.119 | 1.088 | 35.4 |
| **0.15〜0.3** | 70 | **0.229** | **1.176** | 29.4 |
| ≥0.3（候補）| 28 | 0.214 | 0.607 | 29.1 |

**ROI が最良なのは 0.15〜0.3 帯で、≥0.3 帯ではない。** 候補帯の中央単勝が 29.1倍しかなく
（バックテストは63.5倍）、当てても300円前後にしかならない。

### 🔴 最大の問題: 閾値が締切間際に跨ぐ

1日で**8件**。上がる方向も下がる方向も起きる。

| レース | 早い時点 | 締切時点 | 結果 |
|---|---|---|---|
| 札幌9R 3番 | +0.241（1分前）| **+0.313** | 6着 |
| 札幌10R 13番 | −0.693（26分前）| **−0.279** | **1着** |
| 札幌11R 1番 | +0.282（26分前）| **+0.341** | 着外 |
| 新潟8R 3番 | 候補外（11分前）| **+0.368** | 着外 |
| 中京9R（障害）| 候補なし（11分前）| **3頭同時に跨ぐ** | 1頭的中 |
| 新潟10R 11番 | +0.309 | 候補外 | — |
| 新潟12R 10番 | +0.348 | +0.241 | — |

単勝オッズ自体も動く（中京8R 12番は締切24秒前19.8倍→確定14.9倍）。
締切5分前で締めると候補が 15頭→9頭、ROI も 0.820→0.644 に変わった。

⚠️ **「30分窓で D は安定」と一度報告したが誤り。** レース内 D 最大値の平均を見ており、
個々の馬の閾値跨ぎが平均化で消えていた。**必ず馬単位で見ること。**

---

## 3. 次にやること（優先順）

1. 🔴 **事前登録を書き直す。** `docs/jra_exotic_ev_preregistration_2026_08_23.md` は
   (a) 確定オッズ前提、(b) 複勝オッズ必須、(c) 閾値 D≥0.3 固定 の3点が実態に合わない。
   - **(a)** 主指標を「締切N分前のオッズで作った D」にする。確定オッズ版は上限の参考値
   - **(b)** 複勝オッズは 2,567R しか無い（後述）。**D による選別に寄せれば
     単勝＋三連複だけで 9,171R 全部で測れる**
   - **(c)** 0.15〜0.3 帯を含めて測る
2. **エキゾチック特徴の3ヘッド A/B をやり直す**（母集団が2.7倍になった）
3. 三連複 3点 vs 6点の決着（前日の宿題。確定オッズ期間では3点が優勢だった）
4. 執行可能性の本検定。三連複の前向き蓄積は 2026-08-23 開始・発走前30分窓・約43秒間隔。
   刈り込みは21日保持に変更済み（#276）なので溜まる。数週間後に測る

---

## 4. データの現状

| 券種 | 最古 | レース数 | 由来 |
|---|---|---|---|
| trio / wide / quinella / exacta | 2024-01-06 | **9,171** | option=4（昨夜 backfill 成功）|
| **win / place** | **2025-08-24** | **2,567** | option=1 のみ |

🔴 **単勝・複勝は backfill では取れない（2026-08-24 実測）。**
蓄積系のファイル名は中身のレコード種別と**1つずれている**:

```
file=O1VM2024039920240401144912.jvd  rec_id='O2'  len=2042
```

`O1VM...` の中身は O2（馬連）。O2VM→O3、O3VM→O4 … と続き、
**真の O1（単複枠）は setup データに1件も無い**。option=4 を何度流しても
win/place は増えない（POST は 200 が返るが 0 行）。保持窓ちょうど1年が上限。

検算は3年分すべて通過: 馬番妥当性 99.09/99.15/99.28%、Σ(1/odds)→払戻率 74.9%（公式75.0%）。

---

## 5. 今日マージした PR

| PR | 内容 |
|---|---|
| **#276** | `prune_odds_history` に **exotic 保持期間21日**を追加。週次刈り込みが exotic の
発走前時系列を「最終スナップショット以外削除」していた（根拠は「読むスクリプトが無い」だった）。
三連単だけは常に潰す（上位500人気で打ち切られ期待値計算に使えない・行数の45%） |
| **#277** | `odds_backfill` の JVOpen を `BlockingCallGuard` に通し、`--option 4` を許可。
5.0.0 の不可視セットアップダイアログで永久ブロックするはずだった。**本番で実際に発火して自動応答を確認済み** |
| **#284** | 調教 UPSERT の分割。asyncpg のバインド上限32,767に対し wood は25列で
2,000件=50,000パラメータ。`_upsert` が分割していなかった |
| **#292** | `odds_backfill` のスキップ件数を理由別に最終サマリへ出す。
`rc<-1` で break すると `JVRead 完了` 行ごと消え、常に「0 スキップ」と出ていた |

### 復旧した欠測

- 調教 wood: 今朝落ちた4,000件を `--mode retry` で回収
- 調教 **第31週（7/27〜8/2）の8日間**を `--mode chokyo --from-date 20260727` で回収
  （slope 8,721件 / wood 2,247件）。週平均 148 → 342 と平常値に復帰

---

## 6. 地方競馬（打ち切り）

honest 25,131R / 687開催日 / 8四半期。`chihou.calculated_indices` は全期間 in-sample
なので使わず、`race_results` の過去走から自前で80特徴を組んだ。

| | 市場のみ AUC | 上乗せ ΔAUC | 三連複6点 上乗せ |
|---|---|---|---|
| JRA | 0.838 | **+0.0025** | **+0.063** |
| 地方・平地 | **0.865** | **+0.0006** | +0.025 |
| ばんえい | 0.778 | −0.0013 | +0.013（非有意）|

地方の市場は JRA より**予測しやすい**（1番人気の勝率 45.4% vs 33.8%）＝余地が小さい。
**ばんえいが仮説を落とした**: 市場AUC 0.778 と明白に非効率なのに上乗せはゼロ。
**市場の非効率と自分の情報優位は別物。**

⚠️ 地方の `race_payouts` は **2026-01 以降しか無く**、三連系オッズは存在しない。

---

## 7. 踏んだ罠（次も踏む）

- **逸話で判断しない。** 「避け側から来ている」と3レースで読んだが、183頭で測ると
  複勝率は D とともに単調増加していた。**必ず母集団で測る**
- **平均は個体の動きを隠す。** 「D は30分窓で安定」はレース内最大値の平均を見た結果で、
  馬単位では閾値を跨いでいた
- **ログの「0件」を「起きなかった」と読まない。** スキップ数は break で消えていた（#292）
- **コンテナの再起動でログが消える。** `docker logs` は当てにならない。**データ側から測る**
- **SSH 越しの PowerShell は `|` を cmd に食われる。** `Select-String -Pattern "a|b"` は壊れる。
  先頭に `$OutputEncoding=[Console]::OutputEncoding=[Text.Encoding]::UTF8;`、パイプは避ける
- **ファイル転送は `scp`。** UNC パスは解決しない
- **`mark_file_completed` は POST の成否に関係なく呼ばれる**（`jvlink_agent.py::on_file_done`）。
  今回は pending 退避が効いたが、pending でも回収できない失敗はファイルが完了扱いになり
  二度と取り直せない。調教は過去へ遡れないので将来の穴。**未対応**

---

## 8. 参照

- 報告書: 地方 https://claude.ai/code/artifact/57d2d078-c71a-4092-9e8c-51244569d966 /
  三連複EV https://claude.ai/code/artifact/0ec54585-0031-4e46-aacc-04cf5df02f18 /
  穴馬検出 https://claude.ai/code/artifact/ad152d74-f87a-4cc1-82d3-10d6d2c298a0
- 事前登録: `docs/jra_exotic_ev_preregistration_2026_08_23.md`（**要改訂**）
- 台帳: `backend/scripts/JRA_TEST_USAGE_LEDGER.md`（2026-08-23 の行を追記済み）
- スクリプト: `scratchpad/anaba/`（D の生成・検証）/ `scratchpad/ev/`（三連複EV）/
  `scratchpad/chihou_wf/`（地方）。実地監視は `scratchpad/watch.py`・`live_d.py`・`final_d.py`
- memory に本日分を6件記録（`jra-cross-market-trio-signal` / `jra-cross-market-live` /
  `jra-trio-ev-selection` / `jra-trio-ranking-levers` / `jra-anaba-place3x` /
  `jra-elo-opponent-adjust`）
