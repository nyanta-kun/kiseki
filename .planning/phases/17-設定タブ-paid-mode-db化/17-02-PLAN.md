---
phase: 17-設定タブ-paid-mode-db化
plan: "02"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - frontend/src/app/api/admin/settings/route.ts
  - frontend/src/app/admin/SettingsTab.tsx
  - frontend/src/app/admin/actions.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/my/page.tsx
  - frontend/src/app/races/[id]/page.tsx
  - frontend/src/components/PaywallGate.tsx
  - frontend/src/components/RaceDetailClient.tsx
autonomous: false
requirements: [ADMIN-07, ADMIN-08]

must_haves:
  truths:
    - "設定タブに PAID_MODE トグルが表示され、ON/OFF 切り替えで DB に反映される"
    - "layout.tsx・my/page.tsx・races/[id]/page.tsx が NEXT_PUBLIC_PAID_MODE 環境変数を参照せず、バックエンド API から paidMode を取得する"
    - "PaywallGate.tsx が NEXT_PUBLIC_PAID_MODE を参照せず、paywallEnabled prop でペイウォールを制御する"
    - "バックエンド障害時はフェイルセーフとして paidMode=false（ペイウォール無効）になる"
  artifacts:
    - path: "frontend/src/app/api/admin/settings/route.ts"
      provides: "admin 認証付き設定 Route Handler（GET/PUT）"
      exports: ["GET", "PUT"]
    - path: "frontend/src/app/admin/SettingsTab.tsx"
      provides: "PAID_MODE トグル UI（Client Component）"
      contains: "useTransition"
    - path: "frontend/src/components/PaywallGate.tsx"
      provides: "paywallEnabled prop でペイウォールを制御"
      contains: "paywallEnabled: boolean"
  key_links:
    - from: "frontend/src/app/admin/SettingsTab.tsx"
      to: "/api/admin/settings"
      via: "fetch('/api/admin/settings')"
      pattern: "fetch.*api/admin/settings"
    - from: "frontend/src/app/layout.tsx"
      to: "backend GET /api/admin/settings"
      via: "fetchPaidMode() with BACKEND_URL + INTERNAL_API_KEY"
      pattern: "BACKEND_URL.*admin/settings"
    - from: "frontend/src/app/races/[id]/page.tsx"
      to: "frontend/src/components/RaceDetailClient.tsx"
      via: "paywallEnabled prop"
      pattern: "paywallEnabled=\\{paidMode\\}"
---

<objective>
フロントエンドの PAID_MODE 管理をビルド時環境変数からDB動的取得へ移行し、設定タブに PAID_MODE トグル UI を実装する。

Purpose: ADMIN-07（管理者が UI から PAID_MODE を切り替え）と ADMIN-08（フロントエンドが起動時に API から paidMode を動的取得）を完成させる。
Output: 管理者用設定 Route Handler、SettingsTab トグル UI、paidMode の動的取得実装（3サーバーコンポーネント）、PaywallGate の props 化
</objective>

<execution_context>
@/Users/ysuzuki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ysuzuki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-設定タブ-paid-mode-db化/17-01-SUMMARY.md

@frontend/src/components/PaywallGate.tsx
@frontend/src/components/RaceDetailClient.tsx
@frontend/src/app/layout.tsx
@frontend/src/app/my/page.tsx
@frontend/src/app/races/[id]/page.tsx
@frontend/src/app/admin/SettingsTab.tsx
@frontend/src/app/admin/actions.ts
@frontend/src/app/api/admin/data-coverage/route.ts
</context>

<interfaces>
<!-- 既存コンポーネントのインターフェース（executor が参照すること） -->

From frontend/src/components/PaywallGate.tsx (変更前):
```typescript
type Props = {
  isPremium: boolean;
  raceNumber: number;
  children: React.ReactNode;
};
export function PaywallGate({ isPremium, raceNumber, children }: Props) {
  const paywallEnabled = process.env.NEXT_PUBLIC_PAID_MODE === "true";  // 削除対象
  ...
}
```

From frontend/src/components/RaceDetailClient.tsx (変更前):
```typescript
// Props (抜粋)
isPremium?: boolean;
// PaywallGate を以下で呼び出し中:
<PaywallGate isPremium={isPremium} raceNumber={raceNumber}>
```

From frontend/src/app/admin/actions.ts (既存パターン):
```typescript
const BACKEND_URL = process.env.BACKEND_URL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api";
const API_KEY = process.env.INTERNAL_API_KEY ?? "";
// Server Actions: updateUser, createInvitationCode, toggleInvitationCode, triggerFetchData, grantUserAccess
```

From frontend/src/app/api/admin/data-coverage/route.ts (Phase 16 で作成済み — 同じパターンを使う):
```typescript
import { auth } from "@/auth";
import { NextResponse } from "next/server";
const BACKEND_URL = process.env.BACKEND_URL ?? "http://localhost:8000/api";
const API_KEY = process.env.INTERNAL_API_KEY ?? "";
export async function GET() {
  const session = await auth();
  if (session?.user?.role !== "admin") return NextResponse.json({ error: "Forbidden" }, { status: 403 });
  const res = await fetch(`${BACKEND_URL}/admin/data-coverage`, { headers: { "X-API-Key": API_KEY }, cache: "no-store" });
  if (!res.ok) return NextResponse.json({ error: "Backend error" }, { status: 502 });
  return NextResponse.json(await res.json());
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 管理者向け設定 Route Handler + SettingsTab.tsx 実装 + actions.ts に updatePaidMode 追加</name>
  <files>
    frontend/src/app/api/admin/settings/route.ts,
    frontend/src/app/admin/SettingsTab.tsx,
    frontend/src/app/admin/actions.ts
  </files>
  <action>
**1. frontend/src/app/api/admin/settings/route.ts を新規作成（ディレクトリも作成）**

Phase 16 の `data-coverage/route.ts` と同じパターン。GET は全設定取得、PUT は設定値更新。

```typescript
import { auth } from "@/auth";
import { NextResponse, type NextRequest } from "next/server";

const BACKEND_URL =
  process.env.BACKEND_URL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api";
const API_KEY = process.env.INTERNAL_API_KEY ?? "";

export async function GET() {
  const session = await auth();
  if (session?.user?.role !== "admin") {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 });
  }
  const res = await fetch(`${BACKEND_URL}/admin/settings`, {
    headers: { "X-API-Key": API_KEY },
    cache: "no-store",
  });
  if (!res.ok) {
    return NextResponse.json({ error: "Backend error" }, { status: 502 });
  }
  return NextResponse.json(await res.json());
}

export async function PUT(req: NextRequest) {
  const session = await auth();
  if (session?.user?.role !== "admin") {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 });
  }
  const body = await req.json();
  const res = await fetch(`${BACKEND_URL}/admin/settings`, {
    method: "PUT",
    headers: {
      "Content-Type": "application/json",
      "X-API-Key": API_KEY,
    },
    body: JSON.stringify(body),
  });
  if (!res.ok) {
    return NextResponse.json({ error: "Backend error" }, { status: 502 });
  }
  return NextResponse.json(await res.json());
}
```

**2. frontend/src/app/admin/actions.ts の末尾に updatePaidMode を追加**

（既存の `triggerFetchData` の後ろに追加。`BACKEND_URL` と `API_KEY` は既にファイルの先頭で定義済み）

```typescript
export async function updatePaidMode(enabled: boolean): Promise<{ error?: string }> {
  const res = await fetch(`${BACKEND_URL}/admin/settings`, {
    method: "PUT",
    headers: {
      "Content-Type": "application/json",
      "X-API-Key": API_KEY,
    },
    body: JSON.stringify({ key: "PAID_MODE", value: enabled ? "true" : "false" }),
  });

  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    return { error: (body as { detail?: string }).detail ?? "更新に失敗しました" };
  }
  return {};
}
```

**3. frontend/src/app/admin/SettingsTab.tsx をプレースホルダーから実装へ全面置き換え**

現在の内容（`return <div>設定タブは準備中です</div>` のようなプレースホルダー）を削除し、以下で置き換える:

```tsx
"use client";

import { useEffect, useState, useTransition } from "react";
import { updatePaidMode } from "./actions";

type Setting = { key: string; value: string };

export function SettingsTab() {
  const [paidMode, setPaidMode] = useState<boolean | null>(null);
  const [loading, setLoading] = useState(true);
  const [isPending, startTransition] = useTransition();

  async function loadSettings() {
    setLoading(true);
    try {
      const res = await fetch("/api/admin/settings");
      if (res.ok) {
        const data = (await res.json()) as { settings: Setting[] };
        const pm = data.settings.find((s) => s.key === "PAID_MODE");
        setPaidMode(pm?.value === "true");
      }
    } finally {
      setLoading(false);
    }
  }

  useEffect(() => {
    loadSettings();
  }, []);

  function handleToggle() {
    if (paidMode === null) return;
    const newValue = !paidMode;
    startTransition(async () => {
      const result = await updatePaidMode(newValue);
      if (result.error) {
        alert(`更新に失敗しました: ${result.error}`);
      } else {
        setPaidMode(newValue);
      }
    });
  }

  if (loading) {
    return <div className="py-8 text-center text-gray-400 text-sm">読み込み中...</div>;
  }

  return (
    <div className="space-y-6">
      <div className="bg-white rounded-xl border border-gray-200 p-5">
        <h3 className="text-sm font-bold text-[#0d1f35] mb-1">有料モード (PAID_MODE)</h3>
        <p className="text-xs text-gray-500 mb-4">
          ONにするとペイウォールが有効になります。次回ページアクセス時から反映されます。
        </p>
        <div className="flex items-center gap-3">
          <button
            onClick={handleToggle}
            disabled={isPending || paidMode === null}
            className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors focus:outline-none disabled:opacity-40 ${
              paidMode ? "bg-[#1a5c38]" : "bg-gray-300"
            }`}
            role="switch"
            aria-checked={paidMode ?? false}
          >
            <span
              className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${
                paidMode ? "translate-x-6" : "translate-x-1"
              }`}
            />
          </button>
          <span className="text-sm text-gray-700">
            {paidMode ? "有効（ペイウォールON）" : "無効（ペイウォールOFF）"}
          </span>
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki/frontend && npx tsc --noEmit 2>&1 | grep -E "SettingsTab|actions|admin/settings" | head -20 || echo "TypeScript check done"</automated>
  </verify>
  <done>
    - `frontend/src/app/api/admin/settings/route.ts` が存在し GET/PUT をエクスポートしている
    - `frontend/src/app/admin/actions.ts` に `updatePaidMode` が追加されている
    - `frontend/src/app/admin/SettingsTab.tsx` がトグル UI を実装している（`useTransition`・`fetch('/api/admin/settings')` を含む）
    - TypeScript エラーが該当ファイルに存在しない
  </done>
</task>

<task type="auto">
  <name>Task 2: PAID_MODE 動的取得へ移行（PaywallGate / RaceDetailClient / layout.tsx / my/page.tsx / races/[id]/page.tsx）</name>
  <files>
    frontend/src/components/PaywallGate.tsx,
    frontend/src/components/RaceDetailClient.tsx,
    frontend/src/app/layout.tsx,
    frontend/src/app/my/page.tsx,
    frontend/src/app/races/[id]/page.tsx
  </files>
  <action>
**共通ヘルパー関数（各サーバーコンポーネントに個別定義、または layout 側でのみ定義してもよい）**

`layout.tsx`・`my/page.tsx`・`races/[id]/page.tsx` それぞれで直接バックエンドを叩くパターン（自己参照 Route Handler は使わない — Pitfall 3 回避）:

```typescript
async function fetchPaidMode(): Promise<boolean> {
  const BACKEND_URL =
    process.env.BACKEND_URL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api";
  const API_KEY = process.env.INTERNAL_API_KEY ?? "";
  try {
    const res = await fetch(`${BACKEND_URL}/admin/settings`, {
      headers: { "X-API-Key": API_KEY },
      cache: "no-store",
    });
    if (!res.ok) return false;
    const data = (await res.json()) as { settings: { key: string; value: string }[] };
    const setting = data.settings.find((s) => s.key === "PAID_MODE");
    return setting?.value === "true";
  } catch {
    return false;
  }
}
```

**1. frontend/src/components/PaywallGate.tsx を変更**

- `paywallEnabled: boolean` を Props に追加
- `const paywallEnabled = process.env.NEXT_PUBLIC_PAID_MODE === "true";` の行を削除
- デストラクチャリングに `paywallEnabled` を追加

変更後の型:
```typescript
type Props = {
  isPremium: boolean;
  raceNumber: number;
  paywallEnabled: boolean;
  children: React.ReactNode;
};

export function PaywallGate({ isPremium, raceNumber, paywallEnabled, children }: Props) {
  // paywallEnabled は外から受け取る（環境変数参照を削除）
  const isFree = !paywallEnabled || isPremium || raceNumber === 1;
  ...
}
```

**2. frontend/src/components/RaceDetailClient.tsx を変更**

Props に `paywallEnabled?: boolean` を追加し、PaywallGate に渡す。

```typescript
// Props 変更箇所
paywallEnabled?: boolean;

// PaywallGate 呼び出し変更箇所
<PaywallGate isPremium={isPremium} raceNumber={raceNumber} paywallEnabled={paywallEnabled ?? false}>
```

**3. frontend/src/app/layout.tsx を変更**

`async function RootLayout` 内で `fetchPaidMode()` を呼び出し、`{process.env.NEXT_PUBLIC_PAID_MODE === "true" && <Footer />}` を `{paidMode && <Footer />}` に変更する。

```typescript
// layout.tsx に追加する関数（コンポーネント外）
async function fetchPaidMode(): Promise<boolean> { ... }

// RootLayout 内
const paidMode = await fetchPaidMode();
// JSX 内で
{paidMode && <Footer />}
```

**4. frontend/src/app/my/page.tsx を変更**

`const paidMode = process.env.NEXT_PUBLIC_PAID_MODE === "true"` を削除し、API 呼び出しに変更:

```typescript
const paidMode = await fetchPaidMode();
```

`fetchPaidMode` 関数を my/page.tsx の先頭（import の後）に定義すること。

**5. frontend/src/app/races/[id]/page.tsx を変更**

`paidMode` を `fetchPaidMode()` で取得し、`RaceDetailClient` に `paywallEnabled={paidMode}` として渡す。

```typescript
const paidMode = await fetchPaidMode();
// ...
<RaceDetailClient
  raceId={race.id}
  indices={indices}
  initialOdds={initialOdds}
  initialResults={results}
  isPremium={isPremium}
  raceNumber={raceNumber}
  paywallEnabled={paidMode}
/>
```

**重要: `NEXT_PUBLIC_PAID_MODE` の参照を全5ファイルから完全に削除すること。**
残留確認: `grep -r "NEXT_PUBLIC_PAID_MODE" frontend/src/` が 0 件になること。
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki && grep -r "NEXT_PUBLIC_PAID_MODE" frontend/src/ && echo "FAIL: env var still referenced" || echo "PASS: no NEXT_PUBLIC_PAID_MODE references"</automated>
  </verify>
  <done>
    - `grep -r "NEXT_PUBLIC_PAID_MODE" frontend/src/` が 0 件
    - `PaywallGate.tsx` の Props に `paywallEnabled: boolean` が含まれ、環境変数参照が存在しない
    - `layout.tsx`・`my/page.tsx`・`races/[id]/page.tsx` がそれぞれ `fetchPaidMode()` 関数でバックエンドから取得する実装になっている
    - `npx tsc --noEmit` が新規 TypeScript エラーなしで通る
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PAID_MODE DB 管理の動作確認</name>
  <action>Task 1・2 の実装が完了していることを前提に、管理者が UI からトグルを操作すると DB が更新され、フロントエンドが次回アクセス時に新しい paidMode 値を取得することを検証する。</action>
  <what-built>
    Phase 17 全体の実装:
    - keiba.app_settings テーブル（Alembic マイグレーション + AppSettings モデル）
    - バックエンド GET/PUT /api/admin/settings エンドポイント
    - 管理画面の設定タブに PAID_MODE トグル UI
    - フロントエンドの PAID_MODE 管理が DB 動的取得に移行（環境変数依存を除去）
  </what-built>
  <how-to-verify>
    1. バックエンドコンテナを再ビルドしてマイグレーション実行:
       ```
       docker compose up --build backend -d
       docker compose exec backend alembic upgrade head
       ```
    2. `curl -H "X-API-Key: <INTERNAL_API_KEY>" http://localhost:8000/api/admin/settings` が `{"settings":[{"key":"PAID_MODE","value":"false",...}]}` を返すことを確認
    3. 管理画面（/admin）→ 設定タブ を開き、PAID_MODE トグルが「無効（ペイウォールOFF）」で表示されることを確認
    4. トグルを ON にしてから別ウィンドウで `/races/...` を開き、非プレミアムユーザーにペイウォールが表示されることを確認
    5. トグルを OFF に戻してペイウォールが無効になることを確認
    6. `grep -r "NEXT_PUBLIC_PAID_MODE" frontend/src/` が 0 件であることを確認
  </how-to-verify>
  <resume-signal>動作確認できたら "approved" と入力。問題があれば内容を説明してください。</resume-signal>
</task>

</tasks>

<verification>
1. `grep -r "NEXT_PUBLIC_PAID_MODE" frontend/src/` が 0 件であること
2. 管理画面の設定タブにトグルが表示され、ON/OFF が DB に反映されること
3. `npx tsc --noEmit` が新規エラーなしで通ること
4. バックエンドの `ruff check src/` が新規エラーなしで通ること
</verification>

<success_criteria>
- 管理者が設定タブの PAID_MODE トグルを操作すると、DB の値が更新される（ADMIN-07）
- フロントエンドが `NEXT_PUBLIC_PAID_MODE` 環境変数を参照しておらず、起動時にバックエンド API から取得する（ADMIN-08）
- バックエンド障害時はフェイルセーフで `paidMode=false`（ペイウォール無効）になる
- PaywallGate が `paywallEnabled` prop を受け取り、ビルド時の環境変数依存を持たない
</success_criteria>

<output>
完了後に `.planning/phases/17-設定タブ-paid-mode-db化/17-02-SUMMARY.md` を作成すること。
</output>
