---
phase: 16-データタブ
plan: "02"
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - frontend/src/app/api/admin/data-coverage/route.ts
  - frontend/src/app/admin/DataTab.tsx
  - frontend/src/app/admin/actions.ts
autonomous: true
requirements: [ADMIN-05, ADMIN-06]

must_haves:
  truths:
    - "管理画面のデータタブで年/月別レース件数グリッドが表示される"
    - "件数ゼロの月に「取得」ボタンが表示され、クリックするとその年以降の取得指示がキューに積まれる"
    - "取得ボタンクリック中は isPending で disabled になり二重送信を防ぐ"
    - "GET /api/admin/data-coverage（Next.js Route Handler）が admin セッション認証の上でバックエンドへ中継する"
  artifacts:
    - path: "frontend/src/app/api/admin/data-coverage/route.ts"
      provides: "admin セッション認証 + バックエンド中継の Next.js Route Handler"
      contains: "data-coverage"
    - path: "frontend/src/app/admin/DataTab.tsx"
      provides: "月別グリッド表示 + 取得ボタン + useEffect fetch"
      contains: "loadCoverage"
    - path: "frontend/src/app/admin/actions.ts"
      provides: "triggerFetchData Server Action"
      contains: "triggerFetchData"
  key_links:
    - from: "frontend/src/app/admin/DataTab.tsx"
      to: "/api/admin/data-coverage"
      via: "fetch('/api/admin/data-coverage') in loadCoverage()"
      pattern: "fetch.*api/admin/data-coverage"
    - from: "frontend/src/app/admin/DataTab.tsx"
      to: "triggerFetchData (actions.ts)"
      via: "handleFetch() → triggerFetchData(yearMonth)"
      pattern: "triggerFetchData"
    - from: "frontend/src/app/api/admin/data-coverage/route.ts"
      to: "BACKEND_URL/admin/data-coverage"
      via: "fetch with X-API-Key header"
      pattern: "X-API-Key"
---

<objective>
Next.js Route Handler でバックエンドの data-coverage API を中継し、DataTab.tsx に月別グリッド表示と取得指示機能を実装する。

Purpose: 管理者がブラウザからデータ取得状況を確認し、未取得月の取得をワンクリックで指示できるようにする。
Output: Route Handler（API鍵中継）+ DataTab.tsx（月別グリッド + 取得ボタン）+ actions.ts（triggerFetchData）
</objective>

<execution_context>
@/Users/ysuzuki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ysuzuki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-データタブ/16-RESEARCH.md
@.planning/phases/16-データタブ/16-01-SUMMARY.md

# 既存コードのインターフェース
</context>

<interfaces>
<!-- frontend/src/app/admin/actions.ts から抽出（既存パターン） -->
```typescript
"use server";
const BACKEND_URL = process.env.BACKEND_URL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api";
const API_KEY = process.env.INTERNAL_API_KEY ?? "";

// 既存 Server Action パターン（fetch + X-API-Key ヘッダー）
export async function updateUser(userId: number, patch: {...}): Promise<{ error?: string }> {
  const res = await fetch(`${BACKEND_URL}/admin/users/${userId}`, {
    method: "PATCH",
    headers: { "Content-Type": "application/json", "X-API-Key": API_KEY },
    body: JSON.stringify(patch),
  });
  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    return { error: (body as { detail?: string }).detail ?? "更新に失敗しました" };
  }
  revalidatePath("/admin");
  return {};
}
```

<!-- backend API レスポンス構造（16-01-PLAN.md で定義済み） -->
```typescript
// GET /api/admin/data-coverage レスポンス
type MonthCoverage = { year_month: string; race_count: number };  // year_month: "YYYYMM"
type YearCoverage  = { year: string; months: MonthCoverage[]; total: number };
type CoverageData  = { coverage: YearCoverage[]; total_races: number };

// POST /api/admin/fetch-data リクエスト
// body: { year_month: "YYYYMM" }
// レスポンス: { queued: true, action: "recent", from_year: number, year_month: string }
```

<!-- frontend/src/app/admin/AdminTabs.tsx — DataTab の使われ方 -->
```tsx
// AdminTabs.tsx が <DataTab /> をタブコンテンツとして render している
// DataTab は props なしで呼ばれる（自律的にデータ取得する設計）
import { DataTab } from "./DataTab";
```

<!-- Next.js Route Handler パターン（既存 auth パターン準拠） -->
```typescript
// frontend/src/app/api/auth/[...nextauth]/route.ts 等のパターン
import { auth } from "@/auth";
import { NextResponse } from "next/server";
// session?.user?.role === "admin" で管理者チェック
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Next.js Route Handler を作成（data-coverage API 中継）</name>
  <files>frontend/src/app/api/admin/data-coverage/route.ts</files>
  <action>
`frontend/src/app/api/admin/data-coverage/route.ts` を新規作成する。ディレクトリ `frontend/src/app/api/admin/data-coverage/` も作成が必要。

```typescript
import { auth } from "@/auth";
import { NextResponse } from "next/server";

const BACKEND_URL =
  process.env.BACKEND_URL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api";
const API_KEY = process.env.INTERNAL_API_KEY ?? "";

export async function GET() {
  const session = await auth();
  if (session?.user?.role !== "admin") {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 });
  }

  const res = await fetch(`${BACKEND_URL}/admin/data-coverage`, {
    headers: { "X-API-Key": API_KEY },
    cache: "no-store",
  });

  if (!res.ok) {
    return NextResponse.json({ error: "Backend error" }, { status: 502 });
  }

  return NextResponse.json(await res.json());
}
```

これにより:
- `DataTab.tsx` は `fetch('/api/admin/data-coverage')` を呼ぶだけでよく、INTERNAL_API_KEY がブラウザに露出しない
- 管理者セッションがない場合は 403 を返す（セキュリティ）
  </action>
  <verify>
    <automated>test -f /Users/ysuzuki/GitHub/kiseki/frontend/src/app/api/admin/data-coverage/route.ts && grep -l "X-API-Key" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/api/admin/data-coverage/route.ts && grep -l "role.*admin" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/api/admin/data-coverage/route.ts && echo "Route Handler OK"</automated>
  </verify>
  <done>
    - ファイルが存在する
    - auth() セッション検証と role === "admin" チェックが含まれる
    - X-API-Key を付けてバックエンドへ fetch する実装になっている
    - cache: "no-store" が設定されている
  </done>
</task>

<task type="auto">
  <name>Task 2: DataTab.tsx 実装 + actions.ts に triggerFetchData を追加</name>
  <files>frontend/src/app/admin/DataTab.tsx, frontend/src/app/admin/actions.ts</files>
  <action>
**① frontend/src/app/admin/DataTab.tsx を実装に書き換える:**

プレースホルダー（"データタブは準備中です"）を削除し、以下の実装に全置き換えする:

```tsx
"use client";

import { useEffect, useState, useTransition } from "react";
import { triggerFetchData } from "./actions";

type MonthCoverage = { year_month: string; race_count: number };
type YearCoverage = { year: string; months: MonthCoverage[]; total: number };
type CoverageData = { coverage: YearCoverage[]; total_races: number };

export function DataTab() {
  const [data, setData] = useState<CoverageData | null>(null);
  const [loading, setLoading] = useState(true);
  const [fetchingMonth, setFetchingMonth] = useState<string | null>(null);
  const [isPending, startTransition] = useTransition();

  async function loadCoverage() {
    setLoading(true);
    try {
      const res = await fetch("/api/admin/data-coverage");
      if (res.ok) setData(await res.json());
    } finally {
      setLoading(false);
    }
  }

  useEffect(() => {
    loadCoverage();
  }, []);

  function handleFetch(yearMonth: string) {
    const fromYear = yearMonth.slice(0, 4);
    if (!confirm(`${fromYear}年以降のデータを取得します（Windows Agent の recent モード）。よろしいですか？`)) return;
    setFetchingMonth(yearMonth);
    startTransition(async () => {
      const result = await triggerFetchData(yearMonth);
      if (result.error) {
        alert(`取得指示に失敗しました: ${result.error}`);
      }
      setFetchingMonth(null);
    });
  }

  if (loading) {
    return <div className="py-8 text-center text-gray-400 text-sm">読み込み中...</div>;
  }
  if (!data) {
    return <div className="py-8 text-center text-red-400 text-sm">取得状況の読み込みに失敗しました</div>;
  }

  return (
    <div className="space-y-6">
      <p className="text-sm text-gray-500">
        総レース数: <span className="font-medium text-[#0d1f35]">{data.total_races.toLocaleString()}</span> 件
      </p>
      <p className="text-xs text-gray-400">
        ※「取得」ボタンをクリックすると、その年以降のデータを Windows Agent の recent モードで再取得します。
      </p>
      {data.coverage.map((year) => (
        <div key={year.year}>
          <h3 className="font-medium text-sm text-[#0d1f35] mb-2">
            {year.year}年 （計 {year.total.toLocaleString()} 件）
          </h3>
          <div className="grid grid-cols-6 gap-1">
            {year.months.map((m) => {
              const month = m.year_month.slice(4);
              const isEmpty = m.race_count === 0;
              const isFetching = fetchingMonth === m.year_month;
              return (
                <div
                  key={m.year_month}
                  className={`rounded border p-2 text-xs text-center ${
                    isEmpty ? "border-red-200 bg-red-50" : "border-gray-200 bg-white"
                  }`}
                >
                  <div className="font-medium">{month}月</div>
                  <div className={isEmpty ? "text-red-500" : "text-gray-600"}>
                    {isEmpty ? "0" : m.race_count.toLocaleString()}件
                  </div>
                  {isEmpty && (
                    <button
                      onClick={() => handleFetch(m.year_month)}
                      disabled={isPending || fetchingMonth !== null}
                      className="mt-1 w-full text-xs bg-[#0d1f35] text-white rounded py-0.5 disabled:opacity-40 hover:bg-[#1a3560] transition-colors"
                    >
                      {isFetching ? "指示中..." : "取得"}
                    </button>
                  )}
                </div>
              );
            })}
          </div>
        </div>
      ))}
    </div>
  );
}
```

**② frontend/src/app/admin/actions.ts に triggerFetchData を追加:**

既存ファイルの末尾に追加する（`"use server"` ディレクティブと既存の定数は変更しない）:

```typescript
export async function triggerFetchData(yearMonth: string): Promise<{ error?: string }> {
  const res = await fetch(`${BACKEND_URL}/admin/fetch-data`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "X-API-Key": API_KEY,
    },
    body: JSON.stringify({ year_month: yearMonth }),
  });

  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    return { error: (body as { detail?: string }).detail ?? "取得指示に失敗しました" };
  }
  return {};
}
```

注意事項:
- `BACKEND_URL` と `API_KEY` は既に actions.ts の先頭で定義済みなので再定義不要
- `"use server"` ディレクティブは既にファイル先頭にあるので追加不要
  </action>
  <verify>
    <automated>grep -l "triggerFetchData" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/admin/actions.ts && grep -l "loadCoverage" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/admin/DataTab.tsx && grep -l "handleFetch" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/admin/DataTab.tsx && echo "Frontend OK"</automated>
  </verify>
  <done>
    - DataTab.tsx が useEffect + fetch('/api/admin/data-coverage') でデータ取得する Client Component になっている
    - 件数ゼロの月に「取得」ボタンが表示される実装になっている
    - isPending で取得ボタンが disabled になる実装がある
    - actions.ts に triggerFetchData が export されている
    - "データタブは準備中です" プレースホルダーが消えている
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript 構文チェック（build は不要、型チェックのみ）
cd /Users/ysuzuki/GitHub/kiseki/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error|warning" | head -20 || echo "Type check passed"

# Route Handler が存在するか確認
test -f /Users/ysuzuki/GitHub/kiseki/frontend/src/app/api/admin/data-coverage/route.ts && echo "Route Handler exists"

# triggerFetchData が actions.ts に存在するか確認
grep "triggerFetchData" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/admin/actions.ts

# DataTab.tsx が実装済みか確認（プレースホルダー文字列がないこと）
grep -c "準備中" /Users/ysuzuki/GitHub/kiseki/frontend/src/app/admin/DataTab.tsx && echo "ERROR: still placeholder" || echo "DataTab implemented"
```
</verification>

<success_criteria>
- `frontend/src/app/api/admin/data-coverage/route.ts` が存在し、admin セッション認証 + バックエンド中継を実装している
- `DataTab.tsx` が useEffect で `/api/admin/data-coverage` を fetch し、年/月グリッドを表示する Client Component として実装されている
- 件数ゼロの月に「取得」ボタンが表示され、クリックすると triggerFetchData(yearMonth) を呼び出す
- `actions.ts` に triggerFetchData が追加されており、POST /api/admin/fetch-data を呼ぶ Server Action として動作する
- TypeScript 型エラーがない（tsc --noEmit が通る）
- "データタブは準備中です" プレースホルダーが DataTab.tsx から消えている
</success_criteria>

<output>
After completion, create `.planning/phases/16-データタブ/16-02-SUMMARY.md`
</output>
