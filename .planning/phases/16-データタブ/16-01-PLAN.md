---
phase: 16-データタブ
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/api/users.py
  - backend/src/api/agent_router.py
  - windows-agent/jvlink_agent.py
autonomous: true
requirements: [ADMIN-05, ADMIN-06]

must_haves:
  truths:
    - "GET /api/admin/data-coverage が年/月別レース件数（ゼロ月含む）を JSON で返す"
    - "POST /api/admin/fetch-data が year_month を受け取り _command_queue に recent コマンドを積む"
    - "Windows Agent の run_command_loop() が action='recent' を受け取り run_recent() を実行する"
  artifacts:
    - path: "backend/src/api/users.py"
      provides: "GET /data-coverage エンドポイント + POST /fetch-data エンドポイント + Pydantic スキーマ"
      contains: "data-coverage"
    - path: "backend/src/api/agent_router.py"
      provides: "valid_actions に 'recent' を追加"
      contains: "recent"
    - path: "windows-agent/jvlink_agent.py"
      provides: "run_command_loop() 内の recent ハンドラー"
      contains: "recent"
  key_links:
    - from: "backend/src/api/users.py (trigger_fetch_data)"
      to: "backend/src/api/agent_router.py (_command_queue)"
      via: "_command_queue.append(entry) で直接操作"
      pattern: "_command_queue\\.append"
    - from: "windows-agent/jvlink_agent.py (run_command_loop)"
      to: "run_recent(jv, from_year)"
      via: "elif action == 'recent' ブランチ"
      pattern: "action == .recent."
---

<objective>
バックエンドに月別レースデータ取得状況APIと取得指示APIを追加し、Windows Agent の recent コマンドハンドラーを実装する。

Purpose: フロントエンド DataTab がデータ取得状況の表示と取得指示を行うための API 基盤を整備する。
Output: GET /api/admin/data-coverage（月別件数） + POST /api/admin/fetch-data（取得指示） + Windows Agent recent ハンドラー
</objective>

<execution_context>
@/Users/ysuzuki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ysuzuki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-データタブ/16-RESEARCH.md

# 既存コードのインターフェース（Executor はこれをベースに実装する）
</context>

<interfaces>
<!-- backend/src/api/users.py から抽出 -->
```python
# 既存の admin_router と依存性注入パターン
admin_router = APIRouter(prefix="/api/admin", tags=["admin"])
ApiKeyDep = Annotated[None, Depends(verify_api_key)]  # X-API-Key ヘッダー検証
DbDep = Annotated[AsyncSession, Depends(get_db)]

# 既存エンドポイント例（パターン参照用）
@admin_router.get("/users", response_model=list[UserResponse])
async def list_users(_: ApiKeyDep, db: DbDep) -> list[UserResponse]: ...

@admin_router.patch("/users/{user_id}", response_model=UserResponse)
async def update_user(user_id: int, patch: UserPatch, _: ApiKeyDep, db: DbDep): ...
```

<!-- backend/src/api/agent_router.py から抽出 -->
```python
# インメモリキュー（直接 import して append する）
_command_queue: deque[dict] = deque()

# 現在の valid_actions（"recent" が未追加）
valid_actions = {"setup", "daily", "retry", "stop"}
```

<!-- windows-agent/jvlink_agent.py から抽出（既存関数） -->
```python
# 既存の run_recent 関数（実装済み）
def run_recent(jv, from_year: int) -> None: ...

# 既存の report_status 関数
def report_status(status: str, mode: str = "", message: str = "") -> None: ...

# run_command_loop() の既存パターン（例）
# elif action == "daily":
#     report_status("running", mode="daily", ...)
#     run_daily(jv)
#     report_status("idle", ...)
```

<!-- backend/src/db/models.py から抽出 -->
```python
class Race(Base):
    __tablename__ = "races"
    __table_args__ = {"schema": "keiba"}
    id: Mapped[int]
    date: Mapped[str]  # String(8), "YYYYMMDD" 形式
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: バックエンドに data-coverage / fetch-data エンドポイントを追加</name>
  <files>backend/src/api/users.py</files>
  <action>
`backend/src/api/users.py` の末尾に以下を追加する。

**Pydantic スキーマ（ファイル上部の既存 BaseModel 群の後に追加）:**

```python
class MonthCoverage(BaseModel):
    """月別レースデータ取得状況。"""
    year_month: str   # "YYYYMM"
    race_count: int

class YearCoverage(BaseModel):
    """年別レースデータ取得状況（01〜12月全件含む）。"""
    year: str
    months: list[MonthCoverage]
    total: int

class DataCoverageResponse(BaseModel):
    """データ取得状況レスポンス。"""
    coverage: list[YearCoverage]
    total_races: int

class FetchDataRequest(BaseModel):
    """データ取得指示リクエスト。"""
    year_month: str  # "YYYYMM" 形式（例: "202301"）
```

**エンドポイント（既存エンドポイントの後に追加）:**

```python
@admin_router.get("/data-coverage", response_model=DataCoverageResponse)
async def get_data_coverage(_: ApiKeyDep, db: DbDep) -> DataCoverageResponse:
    """年/月別のレースデータ取得状況を返す。ゼロ件の月も含めて01〜12月全件を返す。"""
    from sqlalchemy import func, select
    stmt = (
        select(
            func.left(Race.date, 6).label("year_month"),
            func.count(Race.id).label("race_count"),
        )
        .group_by(func.left(Race.date, 6))
        .order_by(func.left(Race.date, 6))
    )
    result = await db.execute(stmt)
    rows = result.all()

    month_counts: dict[str, int] = {row.year_month: row.race_count for row in rows}

    if not month_counts:
        return DataCoverageResponse(coverage=[], total_races=0)

    years = sorted({ym[:4] for ym in month_counts})
    coverage = []
    for year in years:
        months = [
            MonthCoverage(
                year_month=f"{year}{m:02d}",
                race_count=month_counts.get(f"{year}{m:02d}", 0),
            )
            for m in range(1, 13)
        ]
        coverage.append(
            YearCoverage(year=year, months=months, total=sum(mo.race_count for mo in months))
        )

    return DataCoverageResponse(
        coverage=coverage,
        total_races=sum(month_counts.values()),
    )


@admin_router.post("/fetch-data")
async def trigger_fetch_data(body: FetchDataRequest, _: ApiKeyDep) -> dict:
    """指定年月のデータ取得をWindows Agentへ指示する。recentモードでコマンドキューに積む。"""
    from .agent_router import _command_queue
    from datetime import datetime

    if len(body.year_month) != 6 or not body.year_month.isdigit():
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail="year_month は YYYYMM 形式で指定してください",
        )
    from_year = int(body.year_month[:4])
    entry = {
        "action": "recent",
        "params": {"from_year": from_year, "year_month": body.year_month},
        "queued_at": datetime.now().isoformat(),
    }
    _command_queue.append(entry)
    return {"queued": True, "action": "recent", "from_year": from_year, "year_month": body.year_month}
```

**必要な import の確認:** ファイル上部に `from ..db.models import Race` が既にあるか確認し、なければ追加する。`func`, `select` は関数内 import でもよいが、既存 import 群にあれば上部に移動する。`status` (`from fastapi import ... status`) が既存 import にあるか確認し、なければ追加する。

注意: `datetime` は既存 import が `from datetime import datetime` であることを確認して関数内 import と重複しないよう調整する。
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki/backend && python -c "from src.api.users import admin_router; routes = [r.path for r in admin_router.routes]; assert '/api/admin/data-coverage' in routes, f'data-coverage missing: {routes}'; assert '/api/admin/fetch-data' in routes, f'fetch-data missing: {routes}'; print('OK:', routes)"</automated>
  </verify>
  <done>
    - GET /api/admin/data-coverage と POST /api/admin/fetch-data が admin_router に登録されている
    - data-coverage は year/month グリッドを返す DataCoverageResponse を返す
    - fetch-data は _command_queue に recent コマンドを積む
    - Python import エラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: agent_router.py に recent を追加し jvlink_agent.py にコマンドハンドラーを追加</name>
  <files>backend/src/api/agent_router.py, windows-agent/jvlink_agent.py</files>
  <action>
**① backend/src/api/agent_router.py:**

`valid_actions` の行を探して `"recent"` を追加する:

```python
# 変更前
valid_actions = {"setup", "daily", "retry", "stop"}
# 変更後
valid_actions = {"setup", "daily", "retry", "stop", "recent"}
```

ファイル先頭のモジュール docstring のコマンドフロー説明（`POST /api/agent/command` の action 一覧部分）に以下を追記する:
```
- recent: JVOpen(option=3)で指定年以降のデータを再取得
  params: {"from_year": 2023, "year_month": "202301"}
```

**② windows-agent/jvlink_agent.py:**

`run_command_loop()` 関数内の action ディスパッチ部分（既存の `elif action == "daily":` や `elif action == "setup":` などの並び）を探し、末尾（`else:` の直前）に以下を追加する:

```python
elif action == "recent":
    from_year = cmd.get("params", {}).get("from_year", 2023)
    year_month = cmd.get("params", {}).get("year_month", "")
    report_status(
        "running",
        mode="recent",
        message=f"Starting recent mode (from {from_year}, year_month={year_month})",
    )
    run_recent(jv, from_year=from_year)
    report_status("idle", message=f"Recent fetch completed (from_year={from_year})")
```

`run_recent` と `report_status` は既に同ファイルに存在するので import 不要。
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki/backend && python -c "from src.api.agent_router import _command_queue; print('agent_router OK')" && grep -n '"recent"' /Users/ysuzuki/GitHub/kiseki/backend/src/api/agent_router.py && grep -n "action == .recent." /Users/ysuzuki/GitHub/kiseki/windows-agent/jvlink_agent.py</automated>
  </verify>
  <done>
    - agent_router.py の valid_actions に "recent" が含まれる
    - jvlink_agent.py の run_command_loop() に elif action == "recent": ブランチが存在する
    - Python import エラーなし
  </done>
</task>

</tasks>

<verification>
```bash
# バックエンド全体の import チェック
cd /Users/ysuzuki/GitHub/kiseki/backend && python -c "from src.api.main import app; print('Backend import OK')"

# agent_router に recent が追加されているか確認
grep "recent" /Users/ysuzuki/GitHub/kiseki/backend/src/api/agent_router.py

# jvlink_agent に recent ハンドラーが存在するか確認
grep -A 6 "action == .recent." /Users/ysuzuki/GitHub/kiseki/windows-agent/jvlink_agent.py

# エンドポイント一覧確認
cd /Users/ysuzuki/GitHub/kiseki/backend && python -c "
from src.api.users import admin_router
for r in admin_router.routes:
    print(r.methods, r.path)
"
```
</verification>

<success_criteria>
- GET /api/admin/data-coverage が admin_router に登録されており、年/月別件数（ゼロ月含む全12月）を DataCoverageResponse 形式で返す実装が完成している
- POST /api/admin/fetch-data が admin_router に登録されており、_command_queue に action="recent" のエントリを追加する
- agent_router.py の valid_actions に "recent" が含まれる
- jvlink_agent.py の run_command_loop() に elif action == "recent": ハンドラーが存在し、run_recent(jv, from_year=from_year) を呼ぶ
- `python -c "from src.api.main import app"` がエラーなく通る
</success_criteria>

<output>
After completion, create `.planning/phases/16-データタブ/16-01-SUMMARY.md`
</output>
