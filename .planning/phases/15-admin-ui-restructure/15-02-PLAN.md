---
phase: 15-admin-ui-restructure
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - frontend/src/app/admin/page.tsx
  - frontend/src/app/admin/AdminTabs.tsx
  - frontend/src/app/admin/UsersTab.tsx
  - frontend/src/app/admin/CodesTab.tsx
  - frontend/src/app/admin/DataTab.tsx
  - frontend/src/app/admin/SettingsTab.tsx
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "管理画面に「ユーザー」「データ」「設定」の3タブが表示され、タブ切り替えで各セクションに移動できる"
    - "ユーザーテーブルの各行が1行に収まり、長いフィールドが折り返さない（whitespace-nowrap）"
    - "ユーザーテーブルが10件ページングで表示され、前後ページボタンで移動できる"
    - "ユーザーテーブルに予想家名（yoso_name）と公開設定（is_yoso_public）の列が表示される"
  artifacts:
    - path: "frontend/src/app/admin/AdminTabs.tsx"
      provides: "タブコンテナ（useState でアクティブタブ管理）"
      exports: ["AdminTabs"]
    - path: "frontend/src/app/admin/UsersTab.tsx"
      provides: "ユーザーテーブル（ページング + whitespace-nowrap + yoso列）"
      exports: ["UsersTab"]
    - path: "frontend/src/app/admin/CodesTab.tsx"
      provides: "招待コード管理（page.tsx から分離）"
      exports: ["CodesTab"]
    - path: "frontend/src/app/admin/DataTab.tsx"
      provides: "データタブプレースホルダー（Phase 16 実装予定）"
      exports: ["DataTab"]
    - path: "frontend/src/app/admin/SettingsTab.tsx"
      provides: "設定タブプレースホルダー（Phase 17 実装予定）"
      exports: ["SettingsTab"]
    - path: "frontend/src/app/admin/page.tsx"
      provides: "Server Component（認証 + データ取得 → AdminTabs へ渡す）"
  key_links:
    - from: "frontend/src/app/admin/page.tsx"
      to: "AdminTabs"
      via: "props (users, codes)"
      pattern: "<AdminTabs users=\\{users\\}"
    - from: "frontend/src/app/admin/UsersTab.tsx"
      to: "backend /api/admin/users"
      via: "props.users (Server Componentが取得済み)"
      pattern: "users\\.slice"
    - from: "frontend/src/app/admin/UsersTab.tsx"
      to: "actions.ts"
      via: "import { updateUser } from './actions'"
      pattern: "updateUser"
---

<objective>
管理画面を「ユーザー」「データ」「設定」の3タブ構成に再編し、ユーザーテーブルの表示品質を向上させる。

Purpose: ADMIN-01〜04 を満たし、Phase 16/17 の拡張基盤を整える。
Output: AdminTabs.tsx, UsersTab.tsx, CodesTab.tsx, DataTab.tsx, SettingsTab.tsx（新規）, page.tsx（修正）
</objective>

<execution_context>
@/Users/ysuzuki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ysuzuki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-ui-restructure/15-RESEARCH.md
@.planning/phases/15-admin-ui-restructure/15-01-SUMMARY.md
</context>

<interfaces>
<!-- 既存コードの契約。executor はこれを参照して実装する。 -->

From frontend/src/app/admin/actions.ts:
```typescript
// "use server" ファイル — Client Component から直接 import して呼べる
export async function updateUser(userId: number, patch: { role?: string; is_active?: boolean }): Promise<{ error?: string }>
export async function createInvitationCode(formData: FormData): Promise<{ error?: string }>
export async function toggleInvitationCode(codeId: number, isActive: boolean): Promise<{ error?: string }>
export async function grantUserAccess(userId: number, formData: FormData): Promise<{ error?: string }>
// 全関数内で revalidatePath("/admin") 呼び出し済み
```

現在の page.tsx の型定義（フロントエンド側）:
```typescript
type User = {
  id: number;
  email: string;
  name: string | null;
  image_url: string | null;
  role: string;
  is_active: boolean;
  is_premium: boolean;
  access_expires_at: string | null;
  created_at: string;
  last_login_at: string | null;
  // Plan 01 完了後に追加:
  // yoso_name: string | null;
  // is_yoso_public: boolean;
};

type InvitationCode = {
  id: number;
  code: string;
  grant_type: string;
  weeks_count: number | null;
  target_date: string | null;
  max_uses: number;
  use_count: number;
  is_active: boolean;
  note: string | null;
  created_at: string;
};
```

現在の page.tsx:
- Server Component（"use client" なし）
- `const [users, codes] = await Promise.all([fetchUsers(), fetchInvitationCodes()])` でデータ取得
- インライン Server Action（`action={async () => { "use server"; ... }}`）を多数使用 → Client Component 移行後は actions.ts 経由に統一必要
- formatDate() / grantTypeLabel() ヘルパー関数が page.tsx に定義済み
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 型定義と Client Component ファイル群を作成</name>
  <files>
    frontend/src/app/admin/AdminTabs.tsx,
    frontend/src/app/admin/UsersTab.tsx,
    frontend/src/app/admin/CodesTab.tsx,
    frontend/src/app/admin/DataTab.tsx,
    frontend/src/app/admin/SettingsTab.tsx
  </files>
  <action>
以下の5ファイルを新規作成する。

**AdminTabs.tsx** ("use client"):
- `Tab = "users" | "data" | "settings"` 型を定義
- `const TABS = [{id: "users", label: "ユーザー"}, {id: "data", label: "データ"}, {id: "settings", label: "設定"}]`
- props: `{ users: User[]; codes: InvitationCode[] }`
- `useState<Tab>("users")` でアクティブタブ管理
- タブナビ: `border-b border-gray-200` のナビバー、アクティブ時 `border-[#0d1f35] text-[#0d1f35]`、非アクティブ時 `border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300`
- コンテンツ: activeTab に応じて UsersTab / CodesTab / DataTab / SettingsTab を表示
- User と InvitationCode の型はこのファイルで export して他コンポーネントから import する
- User 型に `yoso_name: string | null` と `is_yoso_public: boolean` を含める（Plan 01 でバックエンド追加済み）

**UsersTab.tsx** ("use client"):
- props: `{ users: User[] }`
- `PAGE_SIZE = 10`、`useState(0)` でページ管理
- `pageUsers = users.slice(page * PAGE_SIZE, (page + 1) * PAGE_SIZE)`
- テーブル: `overflow-x-auto` ラッパー、全 `<td>` に `whitespace-nowrap` クラスを付与
- 列: ID / メールアドレス / 名前 / 予想家名 / 公開 / ロール / 有効 / プレミアム / 期限 / 最終ログイン / 操作
  - 予想家名列: `user.yoso_name ?? "—"` を表示
  - 公開設定列: `user.is_yoso_public ? "公開" : "非公開"` をバッジで表示（公開=緑、非公開=グレー）
- 操作列: `actions.ts` からの `updateUser` を直接 import して `<form action={updateUser.bind(null, user.id, patch)}>` 形式で呼ぶ（bind で引数を部分適用）
  - bind パターン: `updateUser.bind(null, user.id, { role: user.role === "admin" ? "member" : "admin" })` を form の action に渡す
- ページングコントロール: totalPages > 1 の場合のみ表示、「前へ」「次へ」ボタン、`{page * PAGE_SIZE + 1}–{Math.min((page + 1) * PAGE_SIZE, users.length)} / {users.length}件` 表示
- formatDate は UsersTab 内にローカル定義する

**CodesTab.tsx** ("use client"):
- props: `{ codes: InvitationCode[] }`
- 現在の page.tsx の招待コード管理セクション（新規作成フォーム + コード一覧テーブル）を全移動
- フォームの action: `createInvitationCode` を actions.ts から直接 import して `<form action={createInvitationCode}>` 形式（FormData を受け取る形）
- toggleInvitationCode も同様に actions.ts から import して `<form action={toggleInvitationCode.bind(null, code.id, !code.is_active)}>` 形式
- formatDate と grantTypeLabel をローカル定義する

**DataTab.tsx** ("use client"):
- props なし
- Phase 16 実装予定のため、`<div className="py-12 text-center text-gray-400 text-sm">データタブは準備中です</div>` のみ表示

**SettingsTab.tsx** ("use client"):
- props なし
- Phase 17 実装予定のため、`<div className="py-12 text-center text-gray-400 text-sm">設定タブは準備中です</div>` のみ表示
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki/frontend && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>5ファイルが作成され、TypeScript コンパイルエラーなし</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx をリファクタリングして AdminTabs に委譲</name>
  <files>frontend/src/app/admin/page.tsx</files>
  <action>
`page.tsx` を以下の構造に書き換える:

1. "use client" は付けない（Server Component のまま維持）
2. import: `AdminTabs` を `./AdminTabs` から import。`updateUser`, `createInvitationCode` 等のインライン Server Action import は削除
3. User / InvitationCode 型定義は AdminTabs.tsx に移したので page.tsx からは削除。`import type { User, InvitationCode } from "./AdminTabs"` で再利用
4. `fetchUsers()` の戻り値型を `User[]` に変更（yoso_name, is_yoso_public を含む型）
5. `formatDate` / `grantTypeLabel` ヘルパーは page.tsx から削除（各タブコンポーネントに移動済み）
6. JSX は以下の最小構成に:
```tsx
return (
  <div className="min-h-screen bg-[#f0f5fb]">
    <main className="p-6 max-w-6xl mx-auto">
      <AdminTabs users={users} codes={codes} />
    </main>
  </div>
);
```
7. インライン Server Action (`action={async () => { "use server"; ... }}`) を**全て削除**する。これらは CodesTab.tsx / UsersTab.tsx の Client Component 内で actions.ts 経由に置き換え済み。

**注意**: `cache: "no-store"` の fetch はそのまま維持。`auth()` チェックもそのまま維持。
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki/frontend && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm build がエラーなしで完了し、/admin ルートが正常にビルドされる</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 管理画面タブ再構成の動作確認</name>
  <action>ローカル開発サーバーで /admin を開き、3タブ切り替え・ユーザーテーブルのページング・whitespace-nowrap・yoso列・招待コード操作が全て正常に動作することを確認する。</action>
  <verify>手動確認（下記 how-to-verify 参照）</verify>
  <done>全ての確認項目がパスし "approved" が入力される</done>
  <what-built>
管理画面タブ再構成の完成確認。
- AdminTabs コンポーネントによる「ユーザー」「データ」「設定」3タブ表示
- ユーザーテーブルの whitespace-nowrap + 10件ページング
- yoso_name / is_yoso_public 列の追加
- 招待コード管理が CodesTab に移動
  </what-built>
  <how-to-verify>
1. ローカル開発サーバーを起動して `/admin` を開く（管理者アカウントでログイン）
2. 「ユーザー」「データ」「設定」の3タブが上部に表示されていることを確認
3. 各タブをクリックして切り替えられることを確認
4. 「ユーザー」タブで:
   - テーブル行が折り返さず1行に収まることを確認
   - 10件以上ユーザーがいる場合、ページングコントロールが表示されることを確認（「前へ」「次へ」ボタン）
   - 「予想家名」「公開」列が表示されていることを確認
   - ロール切り替え・有効化/無効化ボタンが動作することを確認
5. 「ユーザー」タブ内（またはAdminTabs内のサブセクション）で招待コード管理が表示されることを確認
6. 招待コードの新規作成・有効化/無効化が動作することを確認
  </how-to-verify>
  <resume-signal>問題なければ "approved" と入力。問題があれば具体的な内容を記載してください。</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` がエラーなし
- TypeScript strict モードでコンパイルエラーなし
- `/admin` ページに3タブが表示される
- ユーザーテーブルの全 td に whitespace-nowrap が適用されている
- ページングが正常動作する（10件/ページ、前後ナビ）
- yoso_name / is_yoso_public 列が表示される
- 既存の招待コード操作（作成・有効化/無効化）が引き続き動作する
</verification>

<success_criteria>
- ADMIN-01: 「ユーザー」「データ」「設定」タブが表示され、切り替え可能
- ADMIN-02: ユーザーテーブル全 td に whitespace-nowrap、テーブルラッパーに overflow-x-auto
- ADMIN-03: 10件ページング（PAGE_SIZE=10）、前後ページナビ表示
- ADMIN-04: yoso_name（予想家名）と is_yoso_public（公開設定）列がユーザーテーブルに追加
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-ui-restructure/15-02-SUMMARY.md`
</output>
