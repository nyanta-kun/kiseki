---
phase: 15-admin-ui-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/api/users.py
autonomous: true
requirements:
  - ADMIN-04

must_haves:
  truths:
    - "GET /api/admin/users レスポンスの各ユーザーオブジェクトに yoso_name と is_yoso_public フィールドが含まれる"
    - "yoso_name は文字列またはnull、is_yoso_public は真偽値として正しく返る"
  artifacts:
    - path: "backend/src/api/users.py"
      provides: "UserResponse スキーマ拡張"
      contains: "yoso_name"
  key_links:
    - from: "UserResponse"
      to: "User ORM モデル"
      via: "_make_user_response()"
      pattern: "yoso_name=user\\.yoso_name"
---

<objective>
バックエンドの UserResponse Pydantic モデルに yoso_name と is_yoso_public を追加し、管理画面フロントエンドが予想家情報を表示できる API 基盤を整える。

Purpose: ADMIN-04 の前提条件。フロントエンド Plan 02 が yoso_name / is_yoso_public を型安全に扱えるようにする。
Output: backend/src/api/users.py — UserResponse と _make_user_response() に2フィールド追加
</objective>

<execution_context>
@/Users/ysuzuki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ysuzuki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-ui-restructure/15-RESEARCH.md
</context>

<interfaces>
<!-- 変更対象の既存コード。executor はこれを直接参照する。 -->

From backend/src/api/users.py (L115-158):
```python
class UserResponse(BaseModel):
    """ユーザーレスポンス"""
    id: int
    email: str
    name: str | None
    image_url: str | None
    role: str
    is_active: bool
    is_premium: bool
    can_input_index: bool
    access_expires_at: datetime | None
    created_at: datetime
    last_login_at: datetime | None

    model_config = {"from_attributes": False}

async def _make_user_response(user: User, db: AsyncSession) -> UserResponse:
    is_premium, access_expires_at = await get_user_premium_status(user.id, db)
    return UserResponse(
        id=user.id,
        email=user.email,
        name=user.name,
        image_url=user.image_url,
        role=user.role,
        is_active=user.is_active,
        is_premium=is_premium,
        can_input_index=user.can_input_index,
        access_expires_at=access_expires_at,
        created_at=user.created_at,
        last_login_at=user.last_login_at,
    )
```

User ORM モデル（backend/src/db/models.py）には yoso_name: str | None と is_yoso_public: bool が既に存在することをリサーチで確認済み。
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UserResponse に yoso_name / is_yoso_public を追加</name>
  <files>backend/src/api/users.py</files>
  <action>
UserResponse クラスの last_login_at フィールドの直後に以下2フィールドを追加する:
```python
yoso_name: str | None        # 予想家名
is_yoso_public: bool         # 予想公開フラグ
```

_make_user_response() 関数の return UserResponse(...) の last_login_at=user.last_login_at の後に:
```python
yoso_name=user.yoso_name,
is_yoso_public=user.is_yoso_public,
```
を追加する。

注意: model_config = {"from_attributes": False} はそのまま維持する（ORM直接マッピング不使用のため手動セット方式を維持）。
  </action>
  <verify>
    <automated>cd /Users/ysuzuki/GitHub/kiseki && python -c "from backend.src.api.users import UserResponse; r = UserResponse(id=1, email='test@test.com', name=None, image_url=None, role='member', is_active=True, is_premium=False, can_input_index=False, access_expires_at=None, created_at=__import__('datetime').datetime.now(), last_login_at=None, yoso_name=None, is_yoso_public=False); print('yoso_name:', r.yoso_name, 'is_yoso_public:', r.is_yoso_public)"</automated>
  </verify>
  <done>UserResponse インスタンスに yoso_name と is_yoso_public フィールドが存在し、None/bool の値を正常に保持できる</done>
</task>

</tasks>

<verification>
- `UserResponse` に yoso_name (str | None) と is_yoso_public (bool) が追加されている
- `_make_user_response()` が user.yoso_name と user.is_yoso_public を参照している
- 既存フィールドが変更されていない（Ruff lint エラーなし）
- `cd backend && uv run ruff check src/api/users.py` がエラーなし
</verification>

<success_criteria>
- GET /api/admin/users レスポンスの各ユーザーに yoso_name と is_yoso_public が含まれる
- 既存ユーザー管理機能（updateUser, grantUserAccess など）が引き続き動作する
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-ui-restructure/15-01-SUMMARY.md`
</output>
