# 地方競馬 予想ロジック 再整理（2026-08 〜）

競輪で行っている予想ロジック見直しと同じ手順を地方競馬（chihou）にも適用する。
本ドキュメントを**今回の作業の唯一の作業台帳**とし、フェーズが進むたびに追記する。

- 前提: **スクレイピング（取得）データは資産として維持する。生成済みデータ
  （`calculated_indices` / 学習済みモデル / バックフィル結果）は破棄・再構築しうる。**
- 先行ドキュメント: `docs/chihou_logic_review_2026_08_02.md`（前回の検討リスト。
  Phase 0〜2 の判定はそちらが正）
- プロトコル: `backend/src/chihou_protocol.py`（TRAIN_END / VAL / TEST_START の定義）

---

## 0. フェーズ計画

| # | フェーズ | 状態 | 内容 |
|---|---|---|---|
| **P0** | **取得データ監査** | ✅ **完了（1〜5章）** | 期間別の取得可否・欠測・不整合の確定 |
| **P1** | **欠測の回収** | 🔄 **進行中（8章）** | 原因特定と importer ガードは完了。回収の実行は承認待ち |
| P2 | 生成データの棚卸しと再構築 | 未着手 | `calculated_indices` 13世代の整理、サブ指数の再算出方針 |
| P3 | train/serve 整合の是正 | 未着手 | 市場特徴のライブ無効化問題（5.4 節）の解消 |
| P4 | 特徴量・目的関数の再検討 | 未着手 | 未使用列の活用、ばんえい・複勝の扱い |
| P5 | 推奨カテゴリの再設計 | 未着手 | sweet_spot / place_bet の honest 再評価 |

---

## 1. データ源の全体像

「スクレイピング」と一括りにされがちだが、chihou には**性質の違う3系統**がある。
壊れ方も回収方法も別なので、まず分けて考える。

| 系統 | 取得元 | 格納先 | 取得方式 | 実行場所 |
|---|---|---|---|---|
| **A. 本体データ** | UmaConn SDK（NVDTLab.dll） | `chihou.races` / `race_entries` / `race_results` / `race_payouts` / `horses` | COM（スクレイピングではない） | Windows VM `umaconn_agent.py` |
| **B. オッズ時系列** | UmaConn SDK（速報） | `chihou.odds_history` | COM ポーリング | 同上（realtime モード） |
| **C. 外部指数** | netkeiba / 吉馬 の**Webスクレイピング** | `sekito.netkeiba` / `sekito.kichiuma` | HTTP スクレイプ | **sekito リポジトリ**（`backend/scripts/bin/scrape/*`）・VPS |

- **C は kiseki リポジトリの管轄外**。壊れているときの修理は `~/GitHub/sekito` 側で行う。
- `sekito.anagusa`（穴ぐさ）は**中央専用**で、地方場のレコードは 0 件。地方の特徴量には
  一切入っていない（CLAUDE.md の記述どおり）。
- 取得の成否は `sekito.data_fetch_status`（date × course × race × data_type）に記録される。
  **C 系統の障害診断はまずこのテーブルを見ること**（4.4 節）。

### モデルが実際に使っている外部データは 2 列だけ

44特徴のうち外部指数由来は 5 本（`kc_sp_z` / `nk_idx_z` / `kc_rank_n` / `nk_rank_n` /
`ext_missing`）で、その入力は

- `sekito.kichiuma.sp_score`
- `sekito.netkeiba.idx_ave`（**`is_time_index = true` の行に限定**）

の 2 列のみ。`sp_trust` / `sp_adjust` / `sp_max` / `sueashi` / `senko`、netkeiba の
`idx_max` / `idx_distance` / `idx_course` / `p_rank` / `sire` などは**取得済みだが未使用**。
学習側 `train_chihou_prod_lgb.add_external_features` と serve 側
`chihou_calculator._fetch_external_raw` は同条件で、parity は取れている。

---

## 2. 監査結果サマリ（期間 × 使用可否）

🟢 使用可 / 🟡 条件付き / 🔴 使用不可

| データ | 2023 | 2024上 | 2024下 | 2025上 | 2025下 | 2026上 | 2026-06〜 |
|---|---|---|---|---|---|---|---|
| `races` / `race_entries` | 🔴 皆無 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 | 🟢 |
| `race_results` | 🔴 | 🟡 欠損日あり | 🟡 欠損日多 | 🟡 | 🟡 | 🟡 | 🟢 |
| `race_payouts`（払戻） | 🔴 | 🔴 | 🔴 ほぼ皆無 | 🔴 | 🔴 | 🟢 2026-01〜 | 🟢 |
| `race_results.place_odds` | 🔴 | 🔴 0% | 🔴 0% | 🔴 0% | 🔴 0% | 🟡 28%→100% | 🟢 |
| `odds_history`（時系列） | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 | 🟢 2026-04-07〜 | 🟢 |
| `kichiuma.sp_score` | 🟡 H1のみ | 🟢 | 🟡 **2024-07 皆無** | 🟢 | 🟢 | 🟢 | 🟢 |
| `netkeiba.idx_ave`（タイム指数） | 🔴 | 🔴 未提供 | 🔴 未提供 | 🔴 未提供 | 🟢 2025-06〜 | 🟢 〜2026-05 | 🔴 **崩壊** |
| `chihou.pedigrees`（血統） | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 **全期間 0 件** |

### 結論として「揃っている」窓

| 用途 | 使ってよい期間 | 制約 |
|---|---|---|
| 基本特徴（速度・騎手・調教師・コーナー等）の学習 | **2024-01-01 〜 現在** | 結果欠損 477レースを除く（**回収不能・8.5e**） |
| 外部指数 5特徴を含む学習 | **kichiuma のみなら 2024-01〜**（2024-07 除く）<br>**netkeiba も含めるなら 2025-06 〜 2026-05 の 12か月のみ** | netkeiba は前後とも欠測 |
| 単勝 ROI / EV 検証 | 2024-01 〜 現在 | `race_results.win_odds` は全期間 100% |
| **複勝 ROI 検証** | **2026-04 以降のみ**（実質 2026-05〜） | それ以前は `place_odds` が存在しない |
| 3連単等の払戻検証 | **2026-01 以降のみ** | `race_payouts` が 2026 年からの運用 |
| 発走前オッズ（時系列）を使う検証 | **2026-04-07 以降のみ** | 68.5M 行・9.9GB |

> 🔴 **最重要**: 「複勝」を扱う検証（`place_bet` / `upset_place` / `place_ev_index`）で
> 使える実データは **2026-04 以降の約 4 か月しかない**。しかも既存の複勝バックテストには
> **`place_odds` の欠損パターンに起因する重大な上方バイアスがある**（3.5 節で実証）。
> CLAUDE.md の「place_bet 複勝ROI 0.972（2024-10〜2026-07 walk-forward）」は
> **無効として扱うこと**（6章 課題 #1）。

---

## 3. 本体データ（A系統）の監査

### 3.1 カバレッジ

| 項目 | 実測 |
|---|---|
| レース総数 | 40,030（うち 2024-01-01 以降 39,983） |
| 期間 | **2024-01-01 〜 2026-08-14**（連続。欠落月なし） |
| 競馬場 | 15（NAR 14場 + `course='83'` = ばんえい帯広） |
| 出走馬エントリ | 409,397 |
| 確定結果 | 402,758 |
| 馬マスタ | 26,160 |
| 重複レース `(date, course, race_number)` | **0** ✅ |
| `umaconn_race_id` NULL | **0** ✅ |

- **2021-10-05 に孤立レコードが 47 レース存在する**（門別5・盛岡10・大井12・金沢11・笠松9、
  entries 478 / results 478）。内部的には完結しているが期間外の試験投入と思われる。
  日付フィルタ（`>= 20240101`）で常に除外されるため実害はないが、削除して構わない。
- **2023 年のデータは 1 件も無い**。学習開始が実効 2024-01-01 なのは
  CLAUDE.md にある「`calculated_indices version>=9` が 2024 年からしかない」ためだけでなく、
  **そもそも本体データが存在しない**ため。2023 年へ遡るなら UmaConn の
  `--mode fetch-results` 等でレース本体から取り直す必要がある
  （外部指数は 2023 H1 しか無いので、遡っても外部指数付きになるのは半年分だけ）。

### 3.2 結果の取得抜け — **482 レース / 49 日**

> 📌 **8.1 で再分類済み。** このうち **346 レースは「開催中止」で取得抜けではない**。
> 真に回収すべきは **425 レース**（内訳と根拠は 8.1）。以下は分類前の生の観測。

`race_entries` はあるのに `race_results` が 1 行も無いレース（未来分・ばんえい除く）。
**日 × 場 まるごと 12 レース単位で欠ける**パターンが支配的で、
「その日その場の結果取得ジョブが走らなかった」ことを示す。

| 規模 | 日付 |
|---|---|
| 全場丸ごと（4〜5場） | **2024-07-24, 2024-07-25, 2024-09-03, 2024-09-28, 2024-09-29, 2024-09-30, 2024-10-22** |
| 1場丸ごと（12R） | 2024-08-12 盛岡, 2024-08-16 大井, 2024-08-29 佐賀, 2024-12-16 水沢, 2025-04-26(ばんえい), 2025-12-15(ばんえい) |
| 部分（1〜14R） | 2024-01-24, 2024-02-05, 2024-02-22, 2024-03-10, 2024-06-03, 2024-06-18, 2024-07-09, 2024-08-04, 2024-08-14, 2024-08-19, 2024-09-16, 2024-12-29, 2025-03-17, 2025-03-19, 2025-04-02, 2025-04-11, 2025-05-24, 2025-06-25, 2025-07-01, 2025-07-10, 2025-07-30, 2025-08-18, 2025-08-30, 2025-09-01, 2025-09-11, 2025-12-14, 2025-12-26, 2026-04-04, 2026-04-20, 2026-05-21, 2026-06-25, 2026-06-26, 2026-07-12, 2026-07-23, 2026-07-24, 2026-07-28 |

- 集中しているのは **2024-07 〜 2024-10**（この4か月で 300 レース超）。
- 2025 年以降は 1 日あたり数レースの散発に減っており、
  `kiseki-UmaConn-FetchResults`（5分おき）導入後は改善している。
- 回収手段: `umaconn_agent.py --mode fetch-results --fetch-date YYYYMMDD`。
  **UmaConn 側の保持期間内かどうかは未確認**。1 日試して可否を確定させること（P1）。

### 3.3 head_count と entries の不一致（**正常**）

3,939 レース（約10%）で `entries` が `head_count` を **1〜4 頭上回る**。逆方向は 0 件。
これは**出走取消・発走除外馬が entries に残る**ためで、仕様どおり。
本番 `chihou_recommender.rank_by_hn` は出走予定馬全体で順位を確定するので、
**バックテスト側も LEFT JOIN で母集団を揃えること**（CLAUDE.md の生存者バイアス監査で確定済み）。

### 3.4 列の充足率

`chihou.races`（2024-01 以降・ばんえい除く）

| 列 | 充足 | 備考 |
|---|---|---|
| `post_time` / `prize_1st..3rd` / `lap_times` / `race_type_code` | **100%** | 未使用（活用余地あり） |
| `condition` / `weather` | 97〜99% | ばんえいのみ `condition` 24%（水分率が別体系） |
| `finishers_count` | 97〜99% | |
| `grade` | 24% | 重賞のみ |
| `first_3f` / `last_3f_race` | **21〜29%** | **場依存**（下記） |

`last_3f_race`（レース単位の上がり3F）は**南関4場でしか取れていない**:
浦和 97.9% / 船橋 99.3% / 大井 98.1% / 川崎 97.3% / 園田 15.1% / **その他 9場は 0%**。
一方 `race_results.last_3f`（馬単位）は**全場 100%** 取れているので、
レース単位の値は馬単位から導出可能。レース単位列に依存する処理を作らないこと。

`chihou.race_results`（2024-01 以降・ばんえい除く・完走馬）

| 列 | NULL 率 | 判定 |
|---|---|---|
| `win_odds` / `win_popularity` / `finish_time` / `last_3f` / `running_style` / `horse_weight` | 0.0〜0.1% | 🟢 |
| `passing_1..4`（コーナー通過順） | **26〜28%** | 🟡 **場によって激しく偏る** |
| `margin`（着差） | **100%** | 🔴 **全期間・全場で空。使用不可** |
| `place_odds` | 2024/2025 は 100% | 🔴 → 2026-05 以降 🟢 |

**`passing_*` の場別 NULL 率**（コーナー特徴 `c_early_n` / `c_late_gain_n` / `c_makuri_n` の入力）:

| 場 | NULL率 | | 場 | NULL率 |
|---|---|---|---|---|
| 盛岡 | **95.6%** | | 園田 | 15.0% |
| 門別 | **80.9%** | | 浦和 | 15.5% |
| 大井 | **61.4%** | | 名古屋 | 7.8% |
| 船橋 | **56.7%** | | 佐賀 | 4.1% |
| 川崎 | 20.5% | | 笠松 | 3.7% |
| 水沢 | 20.4% | | 金沢 | 2.9% |
| 姫路 | 18.1% | | 高知 | 1.1% |

> ⚠️ **Phase6 のコーナー特徴 9本は「場によって効いたり効かなかったりする」特徴である。**
> 盛岡・門別・大井・船橋では実質ほぼ欠損値（中立値埋め）で動いている。
> A/B で全体平均が改善していても、**場別に見ると南関・門別・盛岡では寄与ゼロ**のはず。
> P4 で場別に効果を分解して確認すること。

### 3.5 🔴 複勝バックテストの上方バイアス（実証済み）

`place_odds` の欠損は**ランダムではなく着順と強く相関している**。
HR 払戻（`race_payouts`）由来の補完は**1〜3着馬にしか値を与えない**ためである。

`place_odds` 充足率（ばんえい除く・完走馬）:

| 期間 | 1-3着 | 4着以下 |
|---|---|---|
| 2024-01 〜 2025-12 | 0.4% | 0.0% |
| **2026-01 〜 2026-03** | **98.3%** | **0.0%** |
| 2026-04 〜 | 99.8% | 93.3% |

一方、複勝 ROI を計算している 3 スクリプトはいずれも
**`place_odds` が NULL の行を黙って落としてから ROI を出している**:

- `scripts/chihou_rebuild_walkforward.py:309` — `valid = sub[sub["place_odds"].notna()]`
- `scripts/backtest_chihou_sweetspot.py:80` — 同上
- `scripts/aggregate_chihou_recent.py:176` — 同上

**2026-01〜03 の期間では、この絞り込みが「4着以下の馬を全て捨てて 1〜3着馬だけ残す」
操作と等価になる。** すなわち母集団 `valid` の的中率が定義上ほぼ 100% になり、
`ROI = Σ(的中馬の複勝オッズ) / len(valid)` は**複勝オッズの平均値そのもの（2〜5倍）**に
なる。2024-10〜2025-12 の行はほぼ全て捨てられ、残るのはこの偏った 3 か月と
クリーンな 2026-04 以降だけ。

> **したがって「place_bet 複勝ROI 0.972」「複勝ROI 1.046」等、
> `place_odds` を使って算出された既存の数値はすべて信頼できない。**
> P5 で **2026-04 以降のみ**を母集団として再計算すること
> （それでも 4 か月・n は大幅に減る）。
> 併せて 3 スクリプトに「`place_odds` 欠損率が着順帯で偏っていたら中断する」
> ガードを入れること。落として黙って進むのが事故の直接原因である。

**バイアスの大きさ（実測）**: 2026-01〜03 の完走馬全体（ばんえい除く）で
旧コードと同じ計算をすると:

| 指標 | 値 |
|---|---|
| `place_odds.notna()` 後の母集団 | 8,579 行 |
| そのうち 1〜3着 | **8,579 行（100.0%）** |
| naive 複勝ROI | **2.440** |
| レース単位で全馬揃っているレース | **0 / 2,910** |

的中率が定義上 100%、ROI は複勝オッズの平均そのもの。**この期間の複勝 ROI は
数値としての意味を持たない。**

**対応（本ブランチで実施済み）**: `src/services/chihou_place_odds_guard.py` を新設し、
**レース単位で全出走馬の複勝オッズが揃っているレースだけ**を ROI の母集団にする。
期間で切るのではなく充足そのものを見るので、データが増えれば自動的に対象が広がる。
`chihou_rebuild_walkforward.py` / `backtest_chihou_sweetspot.py` /
`aggregate_chihou_recent.py` の 3 本に適用し、監査結果（着順帯別の充足率と
落としたレース数）を必ず出力するようにした。

⚠️ **払戻を 2024 年まで遡ると、この壊れ方が 2024〜2025 にも広がる**
（HR は 1〜3着しか持たないため同じ形の欠損が作られる）。
**バックフィルの前に本ガードを入れること**が必須。

`chihou.odds_history`（2026-04-07 〜）から発走前最終スナップを引けば全馬の複勝オッズが
取れるので、**2026-04 以降については `place_odds` の NULL を埋めきることが可能**
（`backfill_chihou_place_odds.py` が既にその実装）。それ以前は**恒久的に回復不能**。

### 3.6 `chihou.horses` は薄い

26,160 頭。`name` / `sex` / `birthday` は 100% だが、
**`coat_color` / `owner` / `breeder` は全件 0%**。
`chihou.pedigrees` は **0 件**（テーブルはあるが空）＝ **地方に血統特徴は一切存在しない**。
`chihou_pedigree_importer.py` は実装されているが一度も実効していない。

### 3.7 ばんえい帯広（`course='83'`）の扱い

- 4,536 レース（全体の 11%）。`surface='90'` / `distance=200` で判別可能。
- `chihou_calculator.calculate_and_save` は `BANEI_COURSE_CODE` でスキップ、
  学習クエリも `r.course != '83'` で除外 → **完全に対象外**。
- `course_name` が `'83'` のまま未解決なのは、`sekito.racecourse` の帯広（`NOBH`）の
  `netkeiba_id` が **`65`** で、UmaConn の場コード **`83`** と体系が違うため。
  ばんえいを扱う気がないなら実害はないが、**マッピング欠落であることは記録しておく**
  （扱う場合は 200m 専用の別モデルが必要。現行 44特徴は流用不可）。

---

## 4. 外部指数（C系統・スクレイピング）の監査

### 4.1 kichiuma — 概ね健全

| 期間 | 状態 |
|---|---|
| 2023-01 〜 2023-06 | 取得あり（本体データが無いので使えない） |
| 2023-07 〜 2023-12 | 皆無 |
| 2024-01 〜 2026-08 | 🟢 供給率 **93〜98%**・全14場 |
| **2024-07** | 🔴 **1 行も無い（完全欠測・1か月まるごと）** |
| 2024-03 / 2024-06 | 🟡 21日 / 26日分のみ（75% / 82%） |
| 2025-10 / 2026-04 / 2026-05 | 🟡 85〜95% |

`sp_score` / `sueashi` / `senko` の内部充足率は一貫して 93〜98% で、劣化トレンドは無い。

> CLAUDE.md の「kichiuma は 95%→76% に劣化中で要監視」という記述は
> **現時点の実測と一致しない**（2026-06/07 はいずれも 100%）。当時の一時的な落ち込みが
> 回復したものと見られる。記述を更新すること。

### 4.2 netkeiba — 二重の問題

**(a) タイム指数はそもそも 2025-06 からしか無い**

モデルが使う `nk_idx` は `is_time_index = true` の行に限定されるが、
`is_time_index` が立った行が出るのは **2025-05（部分・620行）→ 2025-06（本格・12,232行）** から。
**2024-01 〜 2025-04 は `is_time_index` が 1 行も無い＝ nk_idx は全件 NULL。**

さらに元テーブル自体も **2024-02 〜 2024-07 の 6 か月が完全欠測**。

→ **`nk_idx_z` / `nk_rank_n` は学習期間の大半で「欠損時の既定値」しか入っていない。**
（`nk_idx_z`=0.0 / `nk_rank_n`=0.5 の定数）

**(b) 2026-06-08 にスクレイパーが停止した**

| 月 | 行数 | 取得日数 | 場数 |
|---|---|---|---|
| 2026-04 | 10,878 | 29 | 12 |
| 2026-05 | 10,839 | 29 | 13 |
| **2026-06** | **3,300** | **10** | 10 |
| **2026-07** | **691** | **4** | 5 |
| **2026-08** | **369** | **2** | 3 |

取得できている日を並べると停止の形がはっきり出る:

```
2026-06-01 .. 06-05, 06-07, 06-08   ← ここまで日次で動いていた
2026-06-15, 06-22, 06-29,
2026-07-06, 07-13, 07-20, 07-27,
2026-08-03, 08-10                    ← 以降は「月曜だけ・2場だけ」
```

**日次ジョブが 2026-06-08 に死に、週次（月曜）の別ジョブだけが生き残っている。**

### 4.3 sekito 側で同時に死んでいる他のスクレイパー

`sekito.data_fetch_status` を見ると、**netkeiba だけでなく 3 系統が別々に落ちている**。

| data_type | 2026-04 | 2026-05 | 2026-06 | 2026-07 | 2026-08 |
|---|---|---|---|---|---|
| `kichiuma` | 1,080 ✅ | 1,050 ✅ | 1,208 ✅ | 1,256 ✅ | 443 ✅ |
| `netkeiba_time_index` | 1,041 | 1,038 | **318** | **76** | **38** |
| `netkeiba_blood` | 1,048 | **60** | **76** | **76** | **38** |
| `netkeiba_data_analysis` | 1,048 | **59** | **73** | **73** | **38** |
| `horse_weight` | 295 | 65 | **0（全て not_available）** | **0** | **0** |
| `results` | 1,137 | **41** | **0** | **0** | **0** |

- **`fetch_failed` はほとんど無く、そもそも「取りに行っていない」**。
  → 障害は取得処理ではなく **enqueue / スケジューラ側**。sekito の
  `backend/scripts/scheduler-control.js` と `bin/scrape/netkeiba*` を疑う。
- **崩壊時期が data_type ごとに違う**（blood/data_analysis は 2026-05、
  time_index は 2026-06、results/horse_weight は 2026-05〜06）。単一原因ではない可能性。
- **`horse_weight` と `results` の死は chihou には影響しない**
  （chihou は UmaConn 由来で `race_entries.horse_weight` 充足 92〜95%、
  `race_results` も UmaConn 経由）。**sekito（中央）側の問題**。
- 復旧後の遡り取得手段は `backend/scripts/bin/scrape/backfill-all-data` /
  `refetch-pending-data` が存在する。**netkeiba 側に過去日の指数が残っているかは要確認**
  （タイム指数は開催後しばらくで消える可能性がある）。

### 4.4 外部指数の場別カバレッジ（健全期 2025-06〜2026-05）

全 14 場で netkeiba 89〜100% / kichiuma 92〜100%。**特定の場だけ落ちている構図は無い**。
（最低: 門別 netkeiba 89.3% / 盛岡 kichiuma 92.4%）

### 4.5 anagusa は地方に存在しない

`sekito.anagusa` の地方場（`course_code LIKE 'N%'`）レコードは **0 件**。
JRA 側の `has_anagusa` / `SIGNAL_ANAGUSA_ELITE` 相当のものは地方には無い。

---

## 5. 生成済みデータ（再構築対象）の現状

### 5.1 `calculated_indices` — 13 世代が全部残っている

3,339,509 行 / **855MB**。version 1〜13 が同居している。

| version | 行数 | レース数 | 期間 | 位置づけ |
|---|---|---|---|---|
| 1〜9 | 各 17〜35万 | 各 1.6〜3.4万 | 2024-01 〜 2026-05 | **死蔵**（v9 のみサブ指数取得元として学習クエリが参照） |
| 10 | 349,968 | 33,811 | 2024-01 〜 2026-07-02 | 旧本番 |
| 11 / 12 | 3,176 / 10,929 | 305 / 1,079 | 2026-07 のみ | 短命 |
| **13** | **358,845** | **34,541** | **2024-01 〜 2026-08-13** | **現本番** |

- v13 のバックフィルは実施済み（CLAUDE.md の「v10 で止まっている」は解消済み）。
- **v13 のカバレッジは 97〜99% で、欠けている分は 3.2 節の結果欠損日と一致する**
  （例: 2024-09 は 1,013 / 1,189 = 85%）。結果を回収すれば v13 も埋まる。
- **v1〜9・11・12 は削除候補**。ただし学習クエリ
  （`train_chihou_market_lgb.BASE_QUERY`）が `version >= 9` の DISTINCT ON で
  **サブ指数（speed/last3f/jockey/rotation/last_margin）を v9 から取っている**ため、
  **v9 を消すと学習が動かなくなる**。整理する場合はサブ指数の供給元を先に v13 へ寄せること。
- `last_margin_index` の NULL 率は 2024 年 11.0% / 2025 年 3.9% / 2026 年 3.7%。
  それ以外のサブ指数は全期間 0%。

### 5.2 `odds_history` は 10GB ある

68,455,423 行 / 9,943MB。2026-04-07 04:43 〜。`bet_type` は **`win` / `place` の2種のみ**。
1 レースあたり約 1,400 スナップショット（1日 48R で約 14 万行）。
このペースだと**年間およそ 30GB 増える**。P2 で保持方針（間引き・パーティション・
発走前 N 分のみ残す等）を決めること。

### 5.3 `race_payouts` は 2026 年からの運用

2026-01 以降は 99〜100% 充足（8種: win/place/bracket/quinella/exacta/wide/trio/trifecta）。
2025 年以前は 2024-07 の 12 レースと 2024-10 の 96 レースを除いて**皆無**。

### 5.4 🔴 市場特徴がライブでは無効化されている（train/serve 不整合）

**これは監査中に見つかった最大の実装上の問題**であり、P3 で扱う。

`ChihouIndexCalculator.calculate_and_save(race_id, odds_map=None)` は
`odds_map` が None のとき `_fetch_win_odds()` で **`chihou.race_results.win_odds`**
（＝レース確定後にしか入らない値）を読む。

そして **コードベース全体で `odds_map` を渡している呼び出しは 1 つも無い**
（`src/api/chihou_import_router.py::_run_chihou_calculate` / `import_router.py` /
`scripts/chihou_backfill_indices.py` すべて省略）。

⚠️ **タイムスタンプの TZ が列によって違う**（読み違えの罠。実際に一度誤読した）:

| 列 | TZ | 根拠 |
|---|---|---|
| `chihou.race_results.created_at` | **JST** | 大井1R post_time 15:30 → created_at 15:43（10レース全て発走+13〜25分） |
| `chihou.calculated_indices.calculated_at` | **UTC** | 12:30 = VPS cron `30 21`（21:30 JST）と一致 |

VPS crontab（実見・サーバは JST）:

```
30 21 * * *  chihou_calculate_trigger.sh            # 21:30 JST — 当日分
 0 22 * * *  chihou_calculate_trigger.sh tomorrow   # 22:00 JST — 翌日分
```

つまり **同じレースの v13 行が 1 日に 3 回書かれ、最後の 1 回だけが DB に残る**
（cron 2 本に加えて、realtime からのイベント駆動トリガーが 1 日 1 回ある）:

| # | 実行 | 契機 | 対象 | 実行時点の `race_results` | 市場特徴 |
|---|---|---|---|---|---|
| 1 | **前夜 22:00 JST** | cron `0 22 * * *` (tomorrow) | 翌日のレース | **0 件（未実施）** | 🔴 **中立値** |
| 2 | **当日 朝〜昼 JST** | realtime の蓄積差分取込後に1日1回<br>(`umaconn_agent.py` L1370・`_calc_triggered_today`) | その日のレース | ほぼ 0 件<br>（2026-08-13 11:28 実測: **45中2**） | 🔴 **ほぼ中立値** |
| 3 | **当日 21:30 JST** | cron `30 21 * * *` | その日のレース | **全部確定**<br>（2026-08-12 実測: 57/57） | 🟢 確定オッズ入り |

**②が「日中にユーザーが見る値」**で、市場特徴は依然として中立のまま。
③がそれを確定オッズ入りで上書きし、**DB に残るのは③だけ**。

→ **当日ユーザーに提示されているのは①②の「オッズなし」版**。
その状態では `odds_rank_n` = 0.5 固定・`is_heavy_fav` = 0 固定・`is_dark_horse` = 0 固定、
`speed_mkt_gap` / `kc_mkt_gap` は `0.5 − speed_rank_n` / `0.5 − kc_rank_n` に退化して
オッズ非依存の量になる（＝市場5特徴が実質死んでいる）。

→ **その値は当日 21:30（③）に確定オッズ入りで上書きされ、DB にはそちらだけが残る。**
`scripts/inference_chihou_v13.py` によるバックフィルも `race_results.win_odds`
（確定オッズ）を使う。したがって:

> **DB に残っている `composite_index` は「確定オッズ入り」版であり、
> 当日ユーザーに提示された「オッズなし」版とは別物。**
> API（`chihou_races_router`）は DB の値をそのまま返すため、
> **過去分を使った再現検証は本番の再現になっていない。**

CLAUDE.md の「v12(44特徴) が v10 に対して top1勝率 +6.6pt、その差の実体は市場（オッズ）特徴」
という結論は、**確定オッズを与えた条件での比較**であり、**ライブの改善量を意味しない**。

**検証の足場（2026-08-13 に確保）**: ②の値は③に上書きされて消えるため、
**8/13 11:32 JST 時点の v13 を CSV へ退避した**（`v13_preodds_20260813.csv` / 450行 / 45R）。
退避時点で結果があったのは 45R 中 2R のみ＝実質「オッズなし版」。
今夜 21:30 の③で同じ行が確定オッズ版へ置き換わるので、
**同一レース・同一モデルで「オッズあり/なし」を直接比較できる**。
`scripts/chihou_compare_preodds.py` で実行する（8.8 節）。

**参考数値の扱いに注意**: 2026-08-12 の v13 指数と確定オッズ順位の rank 相関 **0.988**
（n=559）は、上表のとおり**確定オッズ入りで上書きされた後の値**を見たものなので、
「オッズ無しでも市場を再現できている」証拠には**ならない**。
**当日提示版（オッズなし）の品質は現時点で一度も測られていない。**
P3 で「同一レースを odds あり / なしで算出して差分を取る」直接比較を行うこと。

---

## 6. 検出した課題一覧（優先度順）

| # | 課題 | 影響 | 対応フェーズ |
|---|---|---|---|
| **1** | **複勝バックテストに構造的な上方バイアス**（3.5節・実証済み）。`place_odds` の欠損が着順と相関しているのに NULL 行を黙って落としている。既存の複勝 ROI は全て無効 | 🔴 致命 | P5 |
| **2** | **市場5特徴がライブで無効**（5.4節）。train/serve 不整合 | 🔴 致命 | P3 |
| **3** | netkeiba スクレイパーが 2026-06-08 から停止。blood/data_analysis は 2026-05 から | 🔴 高 | P1（sekito リポジトリ） |
| **4** | `nk_idx` は 2025-06 より前が全件 NULL。外部指数2本が学習期間の大半で定数 | 🟡 中 | P1 / P4 |
| **5** | 結果取得抜け 482レース / 49日（2024-07〜10 に集中） | 🟡 中 | P1 |
| **6** | kichiuma 2024-07 が 1か月まるごと欠測 | 🟡 中 | P1 |
| **7** | `passing_*` が場によって最大 95.6% 欠損 → コーナー特徴9本が場依存 | 🟡 中 | P4 |
| **8** | `chihou.pedigrees` が 0 件（血統特徴なし） | 🟡 中 | P4 |
| **9** | `race_results.margin` が全期間 100% NULL | 🟢 低 | P2（列削除 or 補完） |
| **10** | `calculated_indices` に 13 世代 855MB が滞留。v9 が学習の依存先 | 🟢 低 | P2 |
| **11** | `odds_history` 10GB・年 +30GB ペース。保持方針なし | 🟢 低 | P2 |
| **12** | ばんえい（`course='83'`）が `sekito.racecourse` と紐づかない（65 vs 83） | 🟢 低 | P2 |
| **13** | 2021-10-05 の孤立 47 レース | 🟢 低 | P2（削除） |
| **14** | `chihou_calculate_trigger.sh` のコメントが「13:00 UTC = 22:00 JST」等と書いているが VPS は **JST 運用**。TZ 記述が実態と食い違う（`calculated_at` は UTC で保存されるため二重に紛らわしい） | 🟢 低 | P3 |

### 次に確定させること（P0 の残タスク）

1. ~~**課題#1 の裏取り**~~ → **実施済み。3.5 節のとおりバイアスを実証した**（3 スクリプトとも
   `place_odds.notna()` で黙って除外していた）。既存の複勝 ROI 数値は破棄する。
2. **課題#5 の回収可否**: `umaconn_agent.py --mode fetch-results --fetch-date 20240724` を
   1 回試し、UmaConn の保持期間内かを確定させる。
3. **課題#3 の切り分け**: sekito 側で `netkeiba_time_index` の enqueue が止まった原因を
   特定し、過去日の遡り取得が可能かを確認する。

---

## 7. 再現用クエリ

本章の数値はすべて VPS PostgreSQL `hrdb` への直接クエリで得た。再実行手順:

```bash
cd /Users/ysuzuki/GitHub/kiseki-wt/chihou/upset-model
set -a && . ./.env && set +a
PGPASSWORD="$DB_PASSWORD" psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME"
```

主要クエリ:

```sql
-- 月次カバレッジ
SELECT substr(date,1,6) ym, count(*) races, count(DISTINCT date) days,
       count(DISTINCT course) venues
FROM chihou.races GROUP BY 1 ORDER BY 1;

-- 結果の取得抜け（未来・ばんえい除く）
SELECT r.date, count(*) missing, count(DISTINCT r.course) venues
FROM chihou.races r
LEFT JOIN (SELECT DISTINCT race_id FROM chihou.race_results) s ON s.race_id = r.id
WHERE s.race_id IS NULL AND r.date >= '20240101'
  AND r.date < to_char(now(),'YYYYMMDD')
GROUP BY 1 ORDER BY 1;

-- 外部指数の供給率（出走馬単位・月次）
WITH base AS (
  SELECT r.date, re.horse_number, rc.code cc, r.race_number
  FROM chihou.races r
  JOIN chihou.race_entries re ON re.race_id = r.id
  JOIN sekito.racecourse rc ON rc.netkeiba_id = r.course
  WHERE r.date >= '20240101' AND r.course <> '83'
)
SELECT substr(b.date,1,6) ym, count(*) entries,
  round(100.0*count(*) FILTER (WHERE nk.is_time_index)/count(*),1) nk_time_pct,
  round(100.0*count(kc.horse_no)/count(*),1) kc_pct
FROM base b
LEFT JOIN sekito.netkeiba nk ON nk.course_code=b.cc
  AND nk.date=to_date(b.date,'YYYYMMDD')
  AND nk.race_no=b.race_number AND nk.horse_no=b.horse_number
LEFT JOIN sekito.kichiuma kc ON kc.course_code=b.cc
  AND kc.date=to_date(b.date,'YYYYMMDD')
  AND kc.race_no=b.race_number AND kc.horse_no=b.horse_number
GROUP BY 1 ORDER BY 1;

-- スクレイパーの死活（sekito 側）
SELECT to_char(date,'YYYY-MM') ym, data_type, fetch_status, count(*)
FROM sekito.data_fetch_status
WHERE course_code LIKE 'N%' AND date >= '2026-04-01'
GROUP BY 1,2,3 ORDER BY 1,2,3;

-- 指数の算出時刻 vs レース日
SELECT r.date, min(ci.calculated_at) t0, max(ci.calculated_at) t1, count(*) rows
FROM chihou.calculated_indices ci JOIN chihou.races r ON r.id = ci.race_id
WHERE ci.version = 13 AND r.date >= to_char(now()-interval '14 days','YYYYMMDD')
GROUP BY 1 ORDER BY 1;
```

既存の定期チェックスクリプト: `backend/scripts/chihou_data_health_check.py`
（直近 N 日 vs baseline の比率で外部指数供給率・オッズ充足・指数算出漏れを監視）。
**本監査で見つかった netkeiba の段階的崩壊（日次→週次）は、比率ベースのこの検査では
検知できていなかった**。P1 で「取得日数」ベースの検査を追加すること。

---

## 9. P3 の進め方（市場特徴のライブ無効化）

課題 #2（5.4 節）。**残る最大の未解決項目**で、毎日の提示品質に直接効く。

### 9.1 何が分かっていないか

DB に残るのは③（当日21:30・確定オッズ入り）だけなので、
**日中ユーザーに提示されている①②（オッズなし版）の品質は一度も測られていない**。
CLAUDE.md の「v12 の +6.6pt は市場特徴が実体」も③の条件での比較であり、
ライブの改善量ではない。

### 9.2 対照実験（コード変更なしで測れる）

同一レース・同一モデル・同一サブ指数で、市場特徴の入力だけが違う 2 版が
1 日のうちに順に DB へ書かれる。②を消える前に退避すれば直接比較できる。

```bash
# 当日 21:30 JST より前（②が入っている間）に退避
python scripts/chihou_compare_preodds.py snapshot --date YYYYMMDD --out pre_YYYYMMDD.csv

# 翌日以降（③に上書きされた後）に比較
python scripts/chihou_compare_preodds.py compare --snapshot pre_YYYYMMDD.csv
```

出力: |Δcomposite| 分布 / レース内 Spearman / 指数1位の一致率 /
**両版の指数1位馬の勝率・複勝率・単勝ROI**。
退避時点で既に結果が出ていたレースは自動で除外する（対照にならないため）。

- 退避済み: **2026-08-13**（450行 / 45R・退避時に結果済みは 2R のみ）
- ⚠️ **1 日では標本が小さい**。最低でも 1〜2 週間ぶん貯めてから判断すること

### 9.3 結果次第の分岐

| 観測 | 意味 | 対応 |
|---|---|---|
| 2版でほぼ差が無い | 市場5特徴は実質効いていない | 特徴から外して学習し直す（モデルが単純になる） |
| ②が明確に劣る | ライブが損をしている | **serve 時に `odds_map` を渡す**（`odds_history` の発走前最終スナップ）。2026-04-07 以降しか無い点に注意 |
| ②が同等以上 | 確定オッズがリークとして効いていた疑い | 過去分析の前提を見直す |

### 9.4 併せて直すもの

- `calculate_and_save` の `odds_map` 引数は**どこからも渡されていない死んだ引数**。
  使うか消すか決める
- `calculated_at` は UTC・`created_at` は JST という**列ごとのTZ不一致**（課題#14）。
  実際に一度誤読したので、どちらかへ揃えるかコメントで明示する

---

## 10. 目標の再設定と、それに対する最大の障害（2026-08-13）

### 10.1 目標

> レース内で**不人気の激走馬**を探す指数を作り、**1レースに1〜2頭**ピックアップする。
> 人気馬でも3着以内に入れなさそうなら**無理に推奨しない**。入着可能な場合のみ推奨する。

評価軸は **A: 複勝的中率（3着以内に入る率）を主軸**、B: 複勝ROI は参考値。
理由は 2 章のとおり、`finish_position` は 2024-01 以降の全期間あるが
`place_odds` は 2026-04 以降しか無く、ROI を主軸にすると検証窓が 4 か月に潰れるため。

### 10.2 これは既に本番に載っている（`chihou_is_place_pick`）

`backend/docs/chihou_darkhorse_feasibility_2026_08_05.md` で確立済み:

> 発走前**6番人気以下** ∧ 指数**3位以内** ∧ 市場上位3頭シェア**<0.63** ∧ **8頭立て以上**
> → 複勝圏率 **51.5%**（母集団 11.79% の **4.36倍**・p=1.0e-22）

同文書は「単勝はエッジなし」「複勝ROIの主張は確定オッズによる look-ahead で崩れた」と
訂正済みで、**生き残っているのは的中率の条件だけ**。今回の目標と完全に一致する。

### 10.3 🔴 51.5% は look-ahead で、到達不能な数字だった

**検証に使われた walk-forward 指数は `ALL_FEATURES`（市場特徴5本）で学習・予測しており、
その入力は `race_results.win_odds` / `win_popularity` ＝ 確定オッズ**
（`chihou_darkhorse_wf_build.py` → `add_market_features`）。
一方 **条件側（人気順位・シェア）は発走前オッズ**から作っている。

この**入力の食い違いが look-ahead を生む**。同一スクリプトで3通りの指数を作って確かめた
（`--no-market` / `--prerace-lead 5` を新設）。

**指数1位馬の勝率（walk-forward・重なる2四半期）**

| 四半期 | 確定オッズ | 発走5分前オッズ | 市場なし（＝本番） |
|---|---|---|---|
| 2026Q2 | 45.8% | **44.1%** | 39.9% |
| 2026-07 | 42.1% | **41.6%** | 36.7% |

素の順位精度で見れば、確定オッズと発走前オッズの差は 0.4〜1.7pt しかない。
「オッズを見せること」自体の価値は本物で、見せないと 5〜6pt 落ちる。

**ところが穴馬条件に適用すると話が逆転する**（discovery・発走前6番人気以下・母集団 11.8%）:

| 指数 | 全レース × 指数3位内 | シェア<0.63 × 指数3位内 |
|---|---|---|
| 確定オッズ | 43.2% (n=192) | **50.7%** (×4.28, n=71) |
| **発走5分前オッズ** | **— (n=2)** | **— (n=0)** |
| 市場なし（本番） | 26.1% (n=720) | **28.3%** (×2.39, n=212) |

**発走前オッズを入れると条件が空になる**。指数が市場と同じものを見れば、
市場が嫌っている馬を上位3位に置かなくなるからで、これは当然の挙動。

### 10.3.1 では確定オッズ版が選んでいた 186 頭は何だったのか

`wf_market.csv` の該当馬を発走5分前オッズと突き合わせた（2026-04-07〜06-30）:

| | 選ばれた 186頭 | 母集団 15,415頭 |
|---|---|---|
| 発走5分前オッズ 中央値 | **17.1倍** | 42.3倍 |
| 確定オッズ 中央値 | **7.2倍** | 64.1倍 |
| pre/final 比 中央値 | **2.30**（人気化） | 0.67（人気落ち） |
| 3着内率 | 42.5% | 12.0% |

**確定オッズでの人気順位: 1番人気7 / 2番人気45 / 3番人気110 / 4番人気22 / 5位以下2**
→ **186頭中162頭（87%）が最終的に1〜3番人気**。

つまりこの条件は「発走5分前は6番人気以下だが、締切までに2〜3番人気まで買われた馬」を
拾っていた。**締切後にしか分からない情報**であり、賭け時点では再現できない。

### 10.4 結論: 本番の作りは正しく、期待値だけが間違っていた

- **本番＝「オッズを見ない指数」+「発走前オッズによる絞り込み」は、この目的に対して
  アーキテクチャとして正しい。** 指数に市場を見せると穴馬を推せなくなる
- **直すべきは実装ではなく期待値**。この条件の honest な実力は
  **複勝圏率 約28%（母集団 11.8% の ×2.4）** であって 51.5% ではない
- 先行文書（`chihou_darkhorse_feasibility_2026_08_05.md`）は**条件側の look-ahead は
  訂正したが、指数側の look-ahead は見落としていた**。51.5% の記述は要訂正
- ⚠️ **5.4 節の「市場特徴がライブで無効」は、この目的においては欠陥ではない。**
  ただし「指数1位馬の勝率」のような一般的な順位精度では依然 5〜6pt 損しており、
  用途によって評価が割れる。**穴馬用と本命用で指数を分ける**設計が筋

### 10.5 次の一手（改訂）

1. ~~serve 時に odds_map を渡す~~ → **穴馬用途では逆効果**。やらない
2. **28% を出発点として「1レース1〜2頭」の閾値を設計する**。
   現状の条件は年間 921本（レースの約2%）で、「1〜2頭ピックアップ」には選択的すぎる。
   指数3位内 → 5位内などで頻度と精度のトレードオフを取る
   （市場なし指数・discovery: シェア<0.63 × 指数5位内 = 27.3%・n=604・年2,623本）
3. **「入着可能な時のみ推奨」の棄権ルール**を設計する。
   棄権したレースで実際に何が起きたかを測れるようにする
4. 新しい一度きり評価窓を切る（**2026-07 HOLDOUT は開封済み**）


---

## 11. 推奨ルールの設計（レース単位・2026-08-13）

`scripts/chihou_pick_design.py`。**市場を見ない walk-forward 指数**（`--no-market`）＋
**発走5分前オッズ**による絞り込みで、DISCOVERY（2026-04-07〜06-30 / 2,894R）を評価した。

母集団: 発走前6番人気以下の複勝圏率 **11.86%**。
**6番人気以下が1頭でも複勝圏に入るレースは 52.8%** ＝ 半分のレースには「拾える穴」がある。

### 11.1 指数順位 × 採用頭数

（6番人気以下・8頭以上・シェア<0.63。倍率は同じ人気帯の母集団比）

| ルール | 推奨 | 網羅 | 頭/R | 複勝率 | 倍率 | R的中 |
|---|---|---|---|---|---|---|
| 指数3位内 × 最大1頭 | 184 | 6.4% | 1.00 | 27.2% | 2.27 | 27.2% |
| **指数3位内 × 最大2頭** | 211 | 6.4% | 1.15 | **29.4%** | **2.46** | 32.1% |
| 指数5位内 × 最大1頭 | 405 | 14.0% | 1.00 | 24.9% | 2.09 | 24.9% |
| **指数5位内 × 最大2頭** | 568 | **14.0%** | 1.40 | 28.2% | 2.36 | **36.8%** |
| 指数6位内 × 最大2頭 | 761 | 16.1% | 1.63 | 25.9% | 2.16 | 39.3% |

**「最大2頭」は一貫して「最大1頭」より複勝率が高い**（27.2→29.4 / 24.9→28.2）。
2頭目の質が良いのではなく、**2頭が条件を満たすレース自体が当たりやすい**
（開いていて指数が複数頭を推せるレース）。頭数そのものがレースの選別に効いている。

### 11.2 「人気薄」の線引き — 的中率と倍率のトレードオフ

| 人気帯 | 推奨 | 網羅 | 複勝率 | 倍率 | R的中 |
|---|---|---|---|---|---|
| 4番人気以下 | 519 | 13.5% | **35.3%** | 2.06 | 42.6% |
| 5番人気以下 | 341 | 9.5% | 30.5% | 2.13 | 35.3% |
| 6番人気以下 | 211 | 6.4% | 29.4% | 2.46 | 32.1% |
| 7番人気以下 | 124 | 3.9% | 26.6% | **2.72** | 28.6% |

深追いするほど**絶対の的中率は下がるが倍率（市場が見落としている度合い）は上がる**。
「当たりやすさ」を採るなら4番人気以下、「穴らしさ」を採るなら7番人気以下。

### 11.3 🟢 棄権は機能している

**推奨を出したレースと出さなかったレースで、実際に人気薄が来た率**:

| ルール | 推奨あり | 棄権 |
|---|---|---|
| 指数3位内・シェア<0.63 | **80.4%** (184R) | 51.0% (2,710R) |
| 指数5位内・シェア<0.7 | 67.3% (834R) | 47.0% (2,060R) |
| 指数5位内・シェアなし | 59.1% (2,062R) | 37.4% (832R) |

**推奨を出したレースは、実際に人気薄が複勝圏に来た割合が 80.4%**（全体 52.8%）。
棄権側は 51.0%。**「出す/出さない」の判断そのものが情報を持っている**ことが確認できた。
目標の「入着可能な場合のみ推奨」は成立している。

### 11.4 推奨する運用点

> **発走前6番人気以下 ∧ 指数5位以内 ∧ シェア<0.63 ∧ 8頭立て以上 → 指数上位2頭まで**

| 指標 | 値 |
|---|---|
| 網羅率 | 14.0%（レースの約1/7で推奨が出る） |
| 1レースあたり | 1.40頭 |
| 複勝率 | 28.2%（母集団比 2.36倍） |
| レース単位の的中 | 36.8%（推奨のどれかが複勝圏） |
| 発生頻度 | 年 約2,400本 / 約1,700レース |

現行本番（指数3位内）より **網羅率が2.2倍**になり、複勝率の低下は 29.4→28.2% と小さい。
「1レースに1〜2頭」という目標には現行より近い。

### 11.5 ⚠️ この数字の限界

- **母集団が約4か月・2,894レースしかない**。`odds_history` が 2026-04-07 開始のため
  構造的にこれ以上遡れない（2章）
- **DISCOVERY 上での探索**であり、約30セルを比較している。
  隣接するセル同士の差（例 指数3位内 29.4% vs 5位内 28.2%）は**有意ではない**
- **HOLDOUT(2026-07) は先行検証で開封済み**。ここで選んだ運用点の確認には使えない
- 複勝ROI は 0.63〜0.96 で**すべて 1.0 未満**。これは的中率の設計であって
  収支の設計ではない（10.1 の評価軸A）

### 11.6 次

1. **前向き記録を始める**（2026-08〜）。11.4 の運用点を凍結し、
   毎日の推奨と結果を記録する。これが唯一の汚染されていない確認窓になる
2. 3か月ぶん貯まったら一度きりの評価をする
3. 並行して、**穴馬用と本命用で指数を分ける**設計を検討する（10.4）

---

## 12. 穴馬用と本命用で指数を分けるべきか（2026-08-13）

### 12.1 やったこと

「穴馬専用に学習した指数」が「汎用の市場なし指数」より穴馬を当てられるかを試した。
`train_binary_control` に `sample_weight` を追加し、`chihou_darkhorse_wf_build.py` に
`--upset-weight / --upset-from` を新設して 3 通りを walk-forward で作った。

- **plain**: 市場なし・重みなし（現行相当）
- **upset3**: 市場なし・確定4番人気以下を **x3**（対象 70.7%）
- **upset6**: 市場なし・確定6番人気以下を **x5**（対象 51.1%）

重み付けの基準は確定人気順位だが、**人気は特徴量に入れない**ので serve 時に人気を
知る必要はなく look-ahead にならない（学習集合の層別であって入力ではない）。

### 12.2 結果 — 有意な改善は無い

DISCOVERY・6番人気以下・シェア<0.63・8頭以上・最大2頭の複勝率:

| ルール | plain | upset3 | upset6 | n |
|---|---|---|---|---|
| 指数2位内 | 25.7% | **31.4%** | **30.5%** | ~110 |
| 指数3位内 | **29.4%** | 28.2% | 26.8% | ~220 |
| 指数4位内 | **27.7%** | 26.9% | 27.0% | ~390 |
| 指数5位内 | **28.2%** | 26.4% | 26.9% | ~570 |
| 指数6位内 | **25.9%** | 24.4% | 25.0% | ~770 |

- **指数2位内だけ両変種が +4.8〜5.7pt 改善**するが、n≈110 で SE≈4.3pt。単独では 1.2σ 程度
- **それ以外の全セルでは重み付けが僅かに悪化**する。符号が一貫しない
- 2変種が一致しているのは独立な証拠ではない（同じデータ・似たモデル）

### 12.3 より重要な構造的制約

穴馬の評価に使える窓は `odds_history` の都合で **2026-04-07 以降・2,894レースだけ**。
セルの n は 100〜600 で、**5〜8pt 未満の差は原理的に解像できない**。

> **いま穴馬モデルのチューニングは検証不能である。**
> どんな学習上の工夫をしても「効いたかどうか」を判定できない。

### 12.4 結論: 分けるのは「学習」ではなく「配信」

| 用途 | 指数 | 市場を見るか | 状態 |
|---|---|---|---|
| **穴馬** | 市場なしモデル + 発走前オッズで絞り込み | **見ない**（見せると条件が空になる・10.3） | ✅ 現行が正しい |
| **本命** | 市場ありモデル | **見る**（見せないと順位精度 5〜6pt 損・10.3） | ❌ 未実装（serve時にオッズを渡していない） |

- **穴馬専用モデルは作らない**。効果が測れないうちに複雑さだけ増える
- **分離は配信側で行う**。同じ学習系から2つの出力を出し、
  穴馬用は `odds_map` なし、本命用は `odds_map` あり で算出する
- 本命用にオッズを渡すには **発走直前の再算出経路**が要る（9.4）。
  毎分動いている `chihou_odds_decision_trigger.sh` に相乗りするのが自然

### 12.5 保留にした仮説（データが貯まったら再検証）

> **穴馬重み付け × 指数2位内** は plain 比 +5pt 前後。
> 事前登録して、前向きデータが 1 年ぶん貯まってから一度きりで検証する。
> いま採用してはいけない（多重比較の中の 1 セルにすぎない）。

---

## 13. 🔴 本番モデルは「市場込みで学習・市場なしで配信」されている（2026-08-14）

### 13.1 見落としていた区別

10〜12章では「市場を見る/見ない」を2値で扱っていたが、実際には**3通り**ある。

| | 学習 | 配信 | 実体 |
|---|---|---|---|
| ① | 市場込み(44) | 市場込み | 検証で使っていた条件（確定オッズ＝look-ahead） |
| ② | **市場込み(44)** | **市場なし** | **本番これ**。`odds_map` が渡されないため |
| ③ | 市場なし(39) | 市場なし | walk-forward の `--no-market` |

**②は「モデルが頼りにしている5本の特徴が、配信時には常に定数」という状態**で、
③（最初から使わずに学習）より構造的に悪い。10.3 の比較は①と③だけを見ており、
②を測っていなかった。

`--serve-no-market`（ALL_FEATURES で学習し、予測時だけ中立値へ潰す）を新設して測った。
中立値は `chihou_calculator._build_lgb_features` L609-644 と同一:

    odds_rank_n = 0.5 / speed_mkt_gap = clip(0.5 - speed_rank_n)
    kc_mkt_gap = clip(0.5 - kc_rank_n) / is_heavy_fav = 0
    is_dark_horse = 1 if 0.5 >= 6/head_count → **12頭立て以上は全馬 1**

### 13.2 結果 — 本番は約9pt 損している

**指数1位馬の勝率（walk-forward・全9四半期）**

| 四半期 | ① 市場込み配信 | ② **本番** | ③ 市場なし学習 | ②→③ |
|---|---|---|---|---|
| 2024Q3 | 41.5% | **25.1%** | 34.0% | +8.9 |
| 2024Q4 | 45.5% | **25.0%** | 38.3% | +13.3 |
| 2025Q1 | 43.7% | **23.7%** | 37.3% | +13.6 |
| 2025Q2 | 42.7% | **28.4%** | 35.3% | +6.9 |
| 2025Q3 | 43.1% | **28.0%** | 36.5% | +8.5 |
| 2025Q4 | 46.0% | **31.6%** | 40.1% | +8.5 |
| 2026Q1 | 47.0% | **32.6%** | 39.9% | +7.3 |
| 2026Q2 | 45.8% | **31.6%** | 39.9% | +8.3 |
| 2026-07 | 42.1% | **30.1%** | 36.7% | +6.6 |

**全四半期で ③ > ②。平均 約9pt。** 複勝率でも 8〜10pt の差がある。

**独立した裏付け**: 2026-08-13 の実測（`chihou_compare_preodds.py`・43レース）で、
本番のオッズなし版の指数1位馬は 勝率 16.7% / 複勝率 61.9% だった。
n が小さく直接比較はできないが、②が低いという方向と一致する。

### 13.3 なぜ直せるか

**配信時にオッズを渡さないなら、市場特徴込みで学習する意味が無い。**
穴馬用途では渡すべきでないことが 10.3 で確定しているので、
**本番モデルを `PROD_FEATURES`(39) で学習し直すだけ**で ③ の水準になる。

- 検証は walk-forward 全期間（9四半期）で可能。穴馬の4か月制約を受けない
- モデル構成・特徴量の意味論は変えない。**外すのは常に定数の5本だけ**
- 効果は全レース・全推奨カテゴリの土台に及ぶ

### 13.4 手順（未実施）

1. `train_chihou_market_lgb.py` の本番学習を `PROD_FEATURES` に切り替える
2. `chihou_calculator._build_lgb_features` から市場5特徴を外す（train/serve 整合）
3. `CHIHOU_COMPOSITE_VERSION` を上げる
4. デプロイ **→ その後** 全期間バックフィル（順序を逆にすると新旧混在になる）

⚠️ 12.4 で「本命用は市場ありモデルを配信する」と書いたが、**その経路を作るより先に
②→③ を直す方が費用対効果が高い**。②は今この瞬間も毎レース効いている。

---

## 14. v14 デプロイとバックフィルの結果（2026-08-14）

### 14.1 実施内容

- PR #137 マージ・デプロイ完了（本番の `CHIHOU_COMPOSITE_VERSION` = 14 / モデル同梱を確認）
- `scripts/inference_chihou_v14.py --start 20240101 --end 20260814`
  → **358,819 行 / 34,536 レース を 76 秒で upsert**
- カバレッジは v13 と同水準（月次 97〜98%。不足はばんえい・head_count NULL・頭数<6）

### 14.2 ⚠️ composite のスケールが縮み、tier 分布が動いた

| | レース内幅 平均 | sd |
|---|---|---|
| v13 | 28.50 | 4.42 |
| **v14** | **23.47** | **5.57** |

`confidence.calculate_race_confidence` は composite のスケールに依存するため
tier 分布が変わった（2026-07 / 1,217レース）:

| tier | v13 割合 | v14 割合 |
|---|---|---|
| S | 25.0% | 15.8% |
| A | 45.4% | 20.0% |
| B | 23.6% | 23.1% |
| **C** | **5.9%** | **41.1%** |

### 14.3 ただし tier の「質」は上がっている

**1位馬の勝率（同期間）**:

| tier | v13 | v14 |
|---|---|---|
| S | 56.2% | 56.8% |
| A | 43.1% | **50.8%** |
| B | 30.7% | 34.2% |
| C | 26.4% | 25.9% |

**v14 の方が分離が良い。** v13 は「A」に 45.4% のレースを入れて勝率 43.1% だったが、
v14 は A を 20.0% に絞って 50.8%。**v13 が過剰に自信を持っていた**と読める。

較正定数を比例縮小（GAP 12→9.6 / DISP 16→12.8）すると割合は v13 寄りに戻るが、
**A の勝率が 50.8% → 39.8% に劣化する**。分布を合わせるために質を落とすのは本末転倒なので
**定数は現行（GAP=12.0 / DISP=16.0）のまま**とした。

### 14.4 🟡 未対応: `chihou_buy_signal` の閾値

`recommend_rank` は表示だけでなく `chihou_buy_signal()`（buy / caution / pass）を左右する。
S+A の割合が **70.4% → 35.8%** に減ったため、
**「buy」「caution」と表示されるレースが半減し、「pass」が増える**。

tier の質が上がった結果なので誤りではないが、閾値（S/A で buy、B/C で caution 等）は
v13 の分布を前提に決められている。**買い目の出方が変わるので、閾値を見直すか
現状を許容するかはプロダクト判断**として残す。

### 14.4b 🔴 バックフィルだけでは当日・翌日のレースが埋まらない（実際に踏んだ）

バックフィル直後の実測:

```
20260813  45レース  v14 45
20260814  34レース  v14  0   ← 当日
20260815  20レース  v14  0   ← 翌日
```

**API は `version = CHIHOU_COMPOSITE_VERSION` の行しか読まない**ため、
この状態では**当日のレースの指数が画面に一切出ない**。

原因は `inference_chihou_v14.py` の抽出条件 `AND r.head_count >= 6`。
`head_count` はレース後にしか入らないので（8.x 節と同じ罠）、
**未実施のレースは構造的にバックフィルの対象外**になる。

通常運用では cron の `calculate` が当日分を書くので問題にならないが、
**版を上げた直後だけは「前夜の cron は旧版で計算済み・バックフィルは未実施分を拾わない」
という穴が空く**。今回はデプロイが 06:23 JST で、前夜 22:00 の cron は v13 だった。

**対処（実施済み）**: 本番の calculate を直接叩いて埋めた。

```bash
ssh sekito "API_KEY=\$(grep '^CHANGE_NOTIFY_API_KEY=' ~/GitHub/kiseki/.env | cut -d= -f2-)
  curl -s -X POST 'http://127.0.0.1:8003/api/import/chihou/calculate?date=YYYYMMDD' \
       -H \"X-API-Key: \$API_KEY\""
```

> ⚠️ **版を上げるときの手順に「当日・翌日の calculate を叩く」を必ず含めること。**
> デプロイ → バックフィル → **当日/翌日の calculate** の3段。

### 14.5 教訓

CLAUDE.md が「tier 較正はスケールに比例させること」と警告していたが、
**比例させても分布は戻らなかった**（分布の"形"が変わったため）。
スケール変更を伴うモデル更新では、**割合ではなく tier ごとの実勝率**で
較正の妥当性を判断すること。

---

## 15. 引き継ぎ（2026-08-14 時点）

### 15.1 本番で動いているもの

| | 状態 |
|---|---|
| 総合指数 **v14**（市場特徴削除・39特徴） | ✅ デプロイ済み・全期間バックフィル済み（358,819行/34,536レース） |
| 注目馬 `chihou_is_place_pick`（指数5位内・最大2頭・`registered_count` 代替） | ✅ 稼働中 |
| 複勝バックテストの母集団ガード | ✅ |
| 外部指数の死活監視（供給元別 + 取得日数） | ✅ |
| sekito netkeiba 地方タイム指数 日次化（id=63 `30 8 * * *`） | ✅ 初回 2026-08-14 08:30 |
| UmaConn 夜間バックフィル（23:50 起動 / 08:30 停止） | ✅ 稼働中 |
| 注目馬の前向き記録（発走6分前スナップショット → 翌日確定） | 🔜 実装済み・**デプロイ待ち**（16章） |

### 15.2 退避してあるデータ

`backend/data/chihou_preodds/`（`.gitignore` 対象・git には入らない）

| ファイル | 中身 | 再取得 |
|---|---|---|
| `v13_preodds_20260813.csv` | 8/13 の**オッズなし版**指数（450行/45R） | ❌ **不可**（DB は 21:30 に上書き済み） |
| `v13_preodds_20260814.csv` | 8/14 の同上（340行/34R） | ❌ **不可** |
| `wf_nomarket.csv` | 市場なし学習の walk-forward 予測 | ⭕ 約12分 |
| `wf_market.csv` | 市場込み学習・市場込み配信 | ⭕ 約12分 |
| `wf_servenomkt.csv` | 市場込み学習・市場なし配信（旧本番の再現） | ⭕ 約12分 |
| `wf_prerace5.csv` | 発走5分前オッズを市場特徴に使った版 | ⭕ 約12分 |
| `wf_upset3.csv` / `wf_upset6.csv` | 穴馬重み付き（12章） | ⭕ 約12分 |

⚠️ `v13_preodds_*.csv` は**その日のうちにしか取れない**。
`scripts/chihou_compare_preodds.py snapshot --date YYYYMMDD --out ...` を
**当日 21:30 JST より前**に走らせること。これを毎日続けるのが「前向き記録」の第一歩。

### 15.3 次にやること（優先順）

1. ~~**前向き記録の仕組み**~~ → **16章で実装済み**（デプロイ待ち）
2. **P5 の残り4カテゴリ**
   `sweet_spot` は walk-forward で該当0件・エッジなしと判定済みなのに**今も配信中**。
   `place_bet` の複勝ROI は母集団ガード導入後に再計算していない。
3. **P4 特徴量** → **17章で再調査**。`passing_*` は実際には97.9%以上充足しており
   欠測ではなく未活用。`chihou.pedigrees` 0件は UmaConn がデータを配信していないためで、
   血統は `keiba.pedigrees` 経由で 91.3% 取れる（17.5）。
4. **P2 の残り**
   `calculated_indices` v1〜v12 削除（約3M行・760MB。付け替えは済んでいる）/
   `odds_history` 9.9GB の保持方針。

### 15.4 判断済みで蒸し返さないもの

- **穴馬指数に市場（オッズ）を見せてはいけない** — 見せると「人気薄×指数上位」が空になる（10.3）
- **穴馬専用モデルは作らない** — 2年 n=22,000 で検定して効果なしと確定（17.3。12.5 の保留仮説は棄却済み）
- **tier 較正定数は据え置き** — 比例縮小すると A の質が落ちる。buy/caution が減るのは許容（ユーザー判断・14.4）
- **過去の欠損は回収不能** — UmaConn に成績確定ファイルが残っていない（8.5e）
- **51.5% は look-ahead** — honest な値は 28.2%（×2.36）（10.3）

---

## 16. 前向き記録の仕組み（2026-08-14 実装・デプロイ待ち）

15.3 の最優先タスク。**凍結した運用点（11.4）を確認できる唯一の窓を作る。**

### 16.1 なぜ後付けでは作れないか（再掲）

- 運用点は DISCOVERY（2026-04〜06）の探索で選ばれ、HOLDOUT(2026-07) は開封済み（11.5）
  → **汚染されていない確認窓は「これから起きるレース」しか無い**
- `chihou.calculated_indices` の現行 version 行は当日 21:30 JST の再算出で上書きされ、
  そのとき市場特徴の入力が確定オッズに変わる（5.4）
  → **日中ユーザーに提示された指数は DB に残らない**

したがって「発走前に撮る」以外に手が無い。

### 16.2 作ったもの

| | |
|---|---|
| テーブル | `chihou.place_pick_races`（レース単位）/ `chihou.place_picks`（出走馬単位） |
| マイグレーション | `alembic/versions/202608141900_chihou_add_place_pick_log.py` |
| 本体 | `backend/src/services/chihou_place_pick_log.py` |
| API | `POST /api/chihou/place-picks/snapshot` / `POST /api/chihou/place-picks/settle?date=` |
| cron | `scripts/chihou_pick_snapshot_trigger.sh`（毎分）/ `scripts/chihou_pick_settle_trigger.sh`（日次） |
| 集計 | `backend/scripts/chihou_pick_log_report.py --start --end` |
| 検査 | `backend/tests/test_chihou_place_pick_log.py` |

**2段構成**:

1. **発走 6 分前**（`SNAPSHOT_LEAD_MINUTES`）に、そのレースの**全出走馬**の
   指数・発走前オッズ・人気順位・判定結果を保存する
2. **翌日 23:30 JST**（+直近7日を毎回なめる）に確定結果を同じ行へ書き戻す

### 16.3 設計上、意図してそうしていること

- 🔴 **発走時刻を過ぎたら撮らない。** 撮り逃したレースは記録から欠ける。
  締切間際の資金移動が混ざれば記録自体が look-ahead になり（10.3.1）、
  **欠けているより悪い**。`is_snapshot_due()` が発走後を必ず弾き、テストで固定している
- **リードを 6 分にしたのは 10.3 の検証が発走5分前オッズで行われているから。**
  毎分 cron なので実際に撮れるのは 1〜6 分前。5分間隔の cron では窓を跨げないレースが出る
- **推奨が出なかったレースも記録する**（`skip_reason` 付き）。
  「出す/出さない」の判断自体が情報を持つ（11.3: 推奨あり 80.4% / 棄権 51.0%）ため、
  棄権側が無いと評価が片肺になる
- **推奨馬だけでなく全出走馬を記録する。** 指数が上書きされる以上、
  「指数2位内にしていたら」（12.5 の保留仮説）のような別案の事後評価は、
  全馬ぶんの指数がここに残っていなければ二度とできない。
  レポートの「反実仮想」表はこの列を使う
- **判定は本番と同じ関数**（`chihou_is_place_pick` / `chihou_select_place_picks`）を呼ぶ。
  条件を記録側に書き写すと、本番の閾値を変えたとき記録だけ古い条件で残る。
  閾値は `rule_version`（`pop>=6,idx<=5,share<0.63,head>=8,max2`）として毎行に埋める
  ので、変更しても世代が自動で分かれる
- **オッズ SQL は表示と共有**（`src/services/chihou_odds_query.py` へ移した）。
  片方だけ別のクエリを使うと「画面に出ていた顔ぶれ」と「記録した顔ぶれ」がずれる

### 16.4 読み取り専用ドライラン（2026-08-13 のデータ）

本番 DB に対して書き込みなしで判定だけ流した結果:

```
園田5R 1240 share=0.578 head=12 picks=[(11番, 7番人気, 指数4位, 14.2倍)]
大井2R 1612 share=0.622 head=9  picks=[(8番,  7番人気, 指数5位, 18.4倍)]
大井5R 1750 share=0.508 head=15 picks=[(7番, 8番人気, 指数1位, 16.9倍), (6番, 7番人気, 指数3位, 13.4倍)]
大井6R 1822 share=0.598 head=12 picks=[(1番, 9番人気, 指数4位, 25.6倍), (5番, 7番人気, 指数5位, 18.7倍)]

45R / 推奨あり 4R (8.9%) / 6頭   内訳 closed_race 32 / small_field 8 / no_odds 1
```

⚠️ この 8.9% を 11.4 の網羅率 14.0% と比べてはいけない。8/13 の行は既に 21:30 の
再算出で**確定オッズ版の指数**に置き換わっており、`head_count` もレース後の値。
**live で撮った数字ではない**。SQL と判定経路が通ることの確認以上の意味は無い。

オッズの到着は間に合う（8/13 実測: **T-6分までに単勝オッズがあるレース 44/45**、
平均 9.7 頭ぶん）。残り1レースは `no_odds` として記録される。

### 16.4b 初回稼働で見つけた既存バグ — 複勝オッズが一度も取れていなかった

初回スナップショット（2026-08-14 17:44 / 大井5R）で**全馬の `pre_place_odds` が空**だった。
原因は共有していたオッズ SQL の書き方:

```sql
CROSS JOIN (VALUES ('win', 'place')) AS bt(bet_type)   -- ❌ 2列の1行
CROSS JOIN (VALUES ('win'), ('place')) AS bt(bet_type) -- ⭕ 1列の2行
```

`AS bt(bet_type)` が名前を付けるのは**先頭列だけ**。複勝側は `column2` として捨てられ、
**`bet_type='win'` しか結合されていなかった**。これは前向き記録の不具合ではなく、
`GET /api/chihou/races/featured-place` の `place_odds` が**ずっと NULL だった**ということ。
エラーにならず「複勝オッズが未取得の日」に見えるため気付けなかった。

`latest_odds_sql(["win", "place"])` で必ず組み立てる形に変え（テンプレートの直接 format を禁止）、
`tests/test_chihou_place_pick_log.py::TestLatestOddsSql` で固定した。
実測で win 14 / place 14 が返ることを確認済み。

### 16.5 デプロイ手順（実施済み・2026-08-14）

main への push で CI の Blue-Green デプロイが走り、その中で
`alembic upgrade head` まで自動実行される（`scripts/deploy-bluegreen.sh` L130）。
人手で要るのは **cron 登録だけ**。

```bash
# 1〜2. migration + 再起動は CI が実施（確認: alembic current → 202608141900_chihou (head)）

# 3. cron 2本を追加（⚠️ VPS の TZ は JST。crontab も JST で書く）
* * * * *   /home/ysuzuki/GitHub/kiseki/scripts/chihou_pick_snapshot_trigger.sh
30 23 * * * /home/ysuzuki/GitHub/kiseki/scripts/chihou_pick_settle_trigger.sh

# 4. 翌日、記録されているか確認
cd backend && .venv/bin/python scripts/chihou_pick_log_report.py --start YYYYMMDD --end YYYYMMDD
```

**稼働確認（2026-08-14）**: 17:44 に初回スナップショット（大井5R・発走5分前・14頭）。
`top3_share` 0.598 で開いたレースだが、指数上位5頭が全員5番人気以内だったため
`no_candidate` で棄権。**意図どおりの挙動**（市場と指数が一致するレースでは推奨しない）。

⚠️ **記録が始まるまで、この運用点の実力は依然として「28.2%（DISCOVERY 上の推定）」のまま。**
数百件貯まるまで結論を出さないこと（11.4 の頻度で年 約1,700レース＝月 約140レース）。

---

## 17. 穴馬選出の伸びしろを測り直す（2026-08-16）

「人気決着になりそうなレース以外で、人気下位の好走馬を的確に選ぶ」を目標に、
**どこが詰まっているのか**を 2 年ぶんの walk-forward 予測
（`data/chihou_preodds/wf_nomarket.csv` = 市場なし学習＝本番と同条件・
271,336頭 / 25,744レース / 2024-07〜2026-07）で測り直した。

### 17.1 🔴 評価の作法を改める — 穴馬の A/B は 4 か月窓に縛られない

12.3 は「穴馬の評価に使える窓は `odds_history` の都合で 2026-04-07 以降・
2,894レースだけ」「いま穴馬モデルのチューニングは検証不能」と書いた。**これは言い過ぎだった。**

発走前人気が要るのは **本番の絞り込み条件を再現するとき**だけで、
**モデル A/B の層別には確定人気を使ってよい**（人気は特徴量に入れないので
look-ahead にならない。層別は入力ではなくラベル側の操作）。
これで窓は **2年・人気薄 142,946頭** に広がる。以下はすべてこの土俵で測った。

⚠️ ただし**絶対値は本番と一致しない**（発走前人気と確定人気で母集団が入れ替わる）。
比較には使えるが、「複勝率◯%」を運用の期待値として読まないこと。

### 17.2 精度はすでに頭打ち、詰まっているのは**再現率**

確定6番人気以下（基準複勝圏率 **10.4%**）を指数順位で切る:

| 指数順位 | n | 複勝圏率 | 母集団比 |
|---|---|---|---|
| 1位 | 491 | 20.8% | ×2.00 |
| 2位 | 1,570 | 22.3% | ×2.15 |
| 3位 | 3,378 | 21.9% | ×2.11 |
| 4位 | 6,368 | 17.9% | ×1.73 |
| 5位 | 10,627 | 16.9% | ×1.62 |

**指数1位も3位も 21〜22% で差が無い**。人気薄の帯で指数に分解能がない。

一方、取りこぼしが大きい:

> **実際に複勝圏へ来た人気薄 14,842頭のうち、指数5位以内に置けていたのは 27.8%**
> （3位以内なら 8.0%、6位以内で 43.5%）。
> レース単位でも、人気薄が来た 12,626レース中 **31.9%** しか拾えていない。

**荒れたレースの約7割は指数の視野に入っていない。** 閾値をどう動かしても届かない。
→ **打ち手は「重み付け」でも「閾値」でもなく「情報」**。

### 17.3 決着: 穴馬重み付け（12.5 の保留仮説）は**効果なし**

12.5 は「穴馬重み付け × 指数2位内が plain 比 +5pt。前向きデータが1年貯まってから
一度きりで検証する」と事前登録していた。17.1 の土俵で**いま検定できる**（n は 30倍）。

| 指数k位内 | plain | upset3 | upset6 |
|---|---|---|---|
| 2位内 | 21.93% | +0.76pt (0.6σ) | +0.78pt (0.6σ) |
| 3位内 | 21.93% | −0.56pt (−0.7σ) | −0.80pt (−1.0σ) |
| 5位内 | 18.39% | +0.11pt (0.3σ) | +0.24pt (0.7σ) |
| 6位内 | 17.12% | −0.05pt | −0.05pt |

**全セル ±1σ 以内・符号も不一貫。** 4か月・n≈110 で見えた「+5pt」はノイズだった。
再現率も 27.8% → 28.6% / 29.2% とほぼ動かない。
**仮説は棄却。1年待つ必要はない。**

### 17.4 否定: オッズの動き（資金の流れ）は水準との交絡

10.3.1 の「締切までに買われた穴馬は走る」を、**賭け時点で観測できる範囲**
（T-90分 → T-6分の単勝オッズ比）で試した（2026-06〜08・人気薄 13,692頭）。

| 動き | n | 複勝圏率 |
|---|---|---|
| ≤0.70（大きく買われた） | 1,290 | 17.5% |
| 0.95-1.05（動かず） | 1,245 | 13.7% |
| ≥1.50（見放された） | 6,948 | 10.6% |

一見効くが、**買われた馬はそもそもオッズが低い**（中央値 26.7倍 vs 52.5倍）。
発走6分前のオッズ水準を固定すると:

| 水準 | 買われた − 見放された |
|---|---|
| 10-15倍 | −3.8pt (−1.2σ) |
| 15-25倍 | −0.3pt (−0.1σ) |
| 25-40倍 | +2.2pt (+1.1σ) |
| 40-70倍 | +0.1pt (+0.1σ) |
| 70倍- | +0.1pt (+0.1σ) |

**全帯で消える。符号も不一貫。** 交絡だった。オッズの時系列特徴は作らない。

### 17.5 🟢 血統は使える — ただし UmaConn からではない

`chihou.pedigrees` は **0 件**（`chihou.horses` 26,185頭に対して）。
取り込み経路（UmaConn `BLOD` → `/api/import/chihou/bloodlines` →
`ChihouPedigreeImporter`）は**全部実装済み**だが、`data/chihou_completed/` には
`RACE_completed.txt` しか無く一度も走った形跡が無かった。BLOD が `run_setup` の中に
しか無かったため（setup は初回のみ・RACE 全期間再DL を伴うので気軽に回せない）。

`--mode blod` を新設して実機で確かめた結果:

| option | 結果 |
|---|---|
| 3（セットアップ） | `rc=-1` 該当データなし |
| 1（差分・from 2005 / from 2026-08） | `rc=-1` 該当データなし |
| 2（今週） | `rc=-116` fromtime 範囲外 |

→ **UmaConn は BLOD を配信していない。** 取り込み側の不具合ではなくデータ源が空。

**しかし血統は既に手元にあった。** `keiba.pedigrees`（中央の血統マスタ・sire 96.1%充足）と
**血統登録番号**で直接つながる:

    chihou.horses.umaconn_code  ↔  keiba.horses.jravan_code   （同一体系・どちらも100%充足）

| | |
|---|---|
| `chihou.horses` が中央マスタと一致 | 20,489 / 26,185（78.2%） |
| **出走馬ベースのカバー率（2024-01以降）** | **91.5%** |

🔴 **当初は `name + birthday` で突合しており、これは壊れたキーだった**（17.9 で訂正）。

⚠️ 台帳 12章の「JRA戦歴のクロス活用は市場に織り込み済みで不採用」と混同しないこと。
あれは**戦歴**（過去の着順・タイム）の話で、**血統は別の情報**。しかも戦歴の
クロス活用が効く母集団は 57.7% だったが、血統は**登録さえあれば取れる**ので 91.3%。

**out-of-time で有意**（父の 2024-25 実績で4分割 → 2026 の人気薄複勝率を見る。
80頭以上走らせた 214種牡馬・検証 36,047頭）:

| 父の過去実績 | n | 2026 人気薄複勝率 |
|---|---|---|
| 下位25% | 8,532 | 10.04% |
| 25-50% | 8,161 | 9.79% |
| 50-75% | 8,304 | 10.40% |
| **上位25%** | 8,047 | **11.48%** |

**上位25% − 下位25% = +1.44pt（±0.48・3.0σ）。**
効果量は ×1.14 と控えめだが、**モデルが一度も見ていない情報**であり、
距離・馬場・クラスとの交互作用を入れればここからさらに伸びる余地がある
（種牡馬の 影響 は「距離替わり・馬場替わりでの一変」に出るのが典型）。

### 17.6 次の一手

1. **血統特徴を v15 に入れる**（🔴 最優先）
   `keiba.pedigrees` 経由で sire / sire_line を引き、
   **point-in-time**（そのレース以前の産駒成績のみ）で集計した特徴を作る。
   距離帯・馬場状態・クラスとの交互作用を含める。評価は 17.1 の土俵で 2年 walk-forward
2. **展開の特徴化**
   `passing_*` は **97.9%以上充足**（CLAUDE.md の「盛岡95.6%欠損」は現状と不一致・要修正）、
   `running_style` は 100%。**欠測ではなく未活用**。枠 × 脚質 × 想定ペースは
   人気薄の好走機序（展開利）に直結する
3. **レース選別の学習化**
   いまの荒れ判定は `top3_share < 0.63` の手置き1本。ただし荒れ率は頭数でほぼ決まる
   （8頭 35.2% → 12頭 57.1% → 14頭 64.0%・2024-01以降 25,744レース）ため、
   頭数を超える上積みがあるかは未知。1・2 の後

### 17.7 🔴 血統は不採用 — 単変量の有意性はモデルへの増分ではなかった

17.5 の発見（父の過去実績が out-of-time で +1.44pt / 3.0σ）を受けて実装し、
**walk-forward 2年で A/B した結果、改善はゼロだった。**

実装: `scripts/chihou_pedigree_features.py`（5特徴・point-in-time）
+ `chihou_darkhorse_wf_build.py --pedigree`。
検査: `tests/test_chihou_pedigree_features.py`（同日混入・重複・行順を固定）。

| 指標 | plain | 血統あり | 差 |
|---|---|---|---|
| 指数1位馬 勝率 | 37.29% | 37.39% | +0.10pt |
| 人気薄 × 指数2位内 | 21.93% | 21.96% | +0.03pt (0.0σ) |
| 人気薄 × 指数3位内 | 21.93% | 21.46% | −0.47pt (−0.6σ) |
| 人気薄 × 指数5位内 | 18.39% | 18.45% | +0.06pt (+0.2σ) |
| **再現率（来た人気薄を5位内に）** | **27.80%** | **27.76%** | **−0.04pt** |
| レース単位で荒れを拾えた率 | 31.94% | 31.89% | −0.05pt |

機序から言えば「馬自身の戦績が薄いほど血統が効く」はずだが、そこでも動かない:

| キャリア | plain | 血統あり | 差 |
|---|---|---|---|
| 0-2走 | 17.21% | 17.35% | +0.13pt (0.2σ) |
| 3-9走 | 18.57% | 18.48% | −0.09pt |
| 2-3歳 | 18.45% | 18.57% | +0.11pt (0.2σ) |

**なぜ消えたか**: 単変量の +1.44pt は本物だが効果量は ×1.14 と小さく、
**馬自身の戦績（`fetch_hist` 由来の特徴）が既に同じ情報を持っている**。
種牡馬の質は産駒の戦績に現れるので、戦績を見ているモデルには増分がない。

⚠️ **教訓: 単変量の有意性を「モデルへの増分」と読み替えてはいけない。**
今回はそれで特徴を5本まるごと実装した。次からは **既存特徴に対する残差**で
当たりを付けること（out-of-time の単変量は必要条件であって十分条件ではない）。

⚠️ **netkeiba から血統を取る案は保留。** 取れていない 8.5% を埋めても、
いま持っている 91.3% が増分ゼロである以上、結論は変わらない見込み。
血統を再訪するなら**産駒成績の集計ではなく、母系の深さ・インブリードなど
戦績と共線でない情報**を狙うこと。

### 17.8 現在地

| 打ち手 | 結果 |
|---|---|
| 穴馬重み付け（12.5） | ❌ 効果なし（17.3） |
| オッズの動き | ❌ 水準との交絡（17.4） |
| 血統（産駒成績集計） | ❌ 増分ゼロ（17.7） |
| 展開（枠 × 脚質 × ペース） | 🔜 **未着手**。`passing_*` 97.9%充足・`running_style` 100%充足で**欠測ではなく未活用** |
| レース選別の学習化 | 🔜 未着手。荒れ率は頭数でほぼ決まる（8頭35% → 14頭64%）ので上積みは未知 |

**再現率 27.8% は動いていない。** 情報を足す方向は正しいが、
「既存特徴と共線でない情報」でなければ意味がない、というのが 17.7 の学び。
残る候補のうち**展開だけが「既に手元にあって、かつ戦績と別軸」**である。

### 17.9 ⚠️ 訂正: 血統の突合キーは壊れていた（ユーザー指摘・2026-08-16）

17.5〜17.7 で「`name + birthday` で突合」「同名同生年の別馬が実在する（`ジャズダンス` など
4頭ずつ）」と書いたが、**どちらも誤りだった**。

**実際に起きていたこと**:

- `horses.birthday` は **chihou / keiba とも 100% が空文字**（実測 26,185 / 112,628 行）。
  `kh.birthday = ch.birthday` は `'' = ''` で常に真になり、**実質は名前だけの一致**だった
- 「同名同生年の別馬」ではなく、**馬名が世代をまたいで再利用されているだけ**。
  名前一致では地方馬 20,692 頭のうち 520 頭で候補が2頭以上になる
- 重複を「name で GROUP BY」して数えたのも誤り。生年月日の違う別馬まで
  同じ名前として合算していた（`ジャズダンス 4` の正体）

**正しいキーは血統登録番号**。`chihou.horses.umaconn_code` と
`keiba.horses.jravan_code` は**同一体系で、どちらも 100% 充足**している。
名前を経由する必要はまったく無かった。

**A/B の結論への影響はない**:

| | |
|---|---|
| 登録番号キーで取れる馬 | 20,484 |
| 旧・名前突合で取れた馬（父が一意に決まるものだけ採用） | 20,254 |
| 両方で取れた馬 | 20,090 |
| **父が食い違った馬** | **0（100% 一致）** |

当時の「父が一意に決まる馬だけ採用する」フィルタが誤りを弾いていたため、
結果として同じ血統が入っていた。**たまたま助かっただけ**で、キーとしては壊れている。
登録番号キーで再計算しても 17.7 の結論（血統は増分ゼロ）は変わらない。

⚠️ **教訓**: 「`name + birthday` で突合」と書いたとき、**birthday が入っているかを
一度も見ていなかった**。結合キーは「一致件数がそれらしいか」ではなく
**列が実際に埋まっているか**を先に確かめること。
（同型の失敗: `head_count` がレース後にしか入らない・14.4b / 0B11 の馬体重が
一度も取り込まれていなかった・CLAUDE.md）

### 17.10 母系（兄弟姉妹の戦績）も増分ゼロ（2026-08-16）

17.7 で産駒成績の集計が消えたのは「種牡馬の質が馬自身の戦績に現れていて共線だから」
と解釈した。ならば**馬自身の記録から復元できない情報**なら残るはずである。
兄弟姉妹の戦績はその条件を満たす（母単位の累計から自分の累計を引く）。

- 母が判明する地方馬 20,543 頭のうち、**兄弟姉妹がいるのは 92.7%**（平均 4.53頭/母）
- 特徴4本（兄弟の複勝率 / 勝率 / 人気薄時の複勝率 / 出走数）を point-in-time で作成
- 🔴 **自分自身の戦績は必ず除く**。引き忘れると馬自身の戦績を別名で渡すことになる
  （`tests/test_chihou_pedigree_features.py::TestMaternalSelfExclusion` で固定）

| 指数k位内 | plain | 母系あり | 差 |
|---|---|---|---|
| 2位内 | 21.93% | 20.99% | −0.94pt (−0.7σ) |
| 3位内 | 21.93% | 21.52% | −0.41pt (−0.5σ) |
| 5位内 | 18.39% | 18.50% | +0.11pt (+0.3σ) |
| **再現率** | **27.80%** | **27.91%** | **+0.11pt (+0.2σ)** |

キャリアが薄いほど効くはずだが、そこでも動かない（0-2走 +0.15pt / 0.2σ）。

**血統由来の情報は、独立な2系統（父の産駒成績・兄弟姉妹の戦績）がいずれも増分ゼロ。**
既存39特徴が「馬の地力の事前分布」を既に十分持っている、と読むのが自然。

### 17.11 残るのはインブリード／クロス — ただし3代では届かない

**手元のデータは3代（14頭）が上限**:

| 情報源 | 深さ | 状態 |
|---|---|---|
| `keiba.pedigrees` | **3頭のみ**（父・母・母父） | 現状 |
| JV-Link `SK` / `UM` レコード | **3代＝14頭**（繁殖登録番号） | 🔴 **パーサーは14頭すべて返しているのに DB が3頭ぶんしか受け皿を持たず捨てている** |
| `keiba.breeding_horses` | コード→名前の辞書のみ・**親リンクなし** | 再帰不可 |
| netkeiba 馬ページ | **5代＝62頭** | 未取得 |

インブリードは 3×4 / 4×4 / 5×5 が主で、**3代では 3×3 以内しか判定できない**。
つまり自前パイプラインを直しても届く範囲は限定的で、
**本格的にやるなら netkeiba の5代血統表が要る**。

⚠️ ただし 17.7 と 17.10 の2連敗を踏まえると、**血統情報そのものの増分に期待しすぎないこと**。
インブリードは「地力の事前分布」ではなく構造的特徴なので性質は違うが、
投資判断は「取得コスト × 2連敗の事前確率」で行うべき。

---

*作成: 2026-08-13 / ブランチ `feat/chihou-upset-model`*

---

## 8. P1: 欠測の回収（2026-08-13〜）

**方針: データは 2024-01-01 以降で揃える。**

### 8.1 「結果欠損 482レース」の再分類 — うち 346 は取得抜けではなかった

P0 では 482 レースをまとめて「取得抜け」と数えたが、`races.condition` / `weather` の
充足パターンと Windows 側の取得済みファイル一覧
（`C:\kiseki\windows-agent\data\chihou_completed\RACE_completed.txt`）を突き合わせると
**3 種類に分かれる**。

| 区分 | 場日 | レース | ばんえい除く | 実体 | 回収 |
|---|---|---|---|---|---|
| **A1 その日まるごと欠損** | 7 | 313 | **289** | 蓄積系の成績確定ファイルを**一度も取得していない** | ✅ 可能 |
| **A2 場まるごと + 馬場天候 NULL** | 27 | 81 | 57 | **開催中止**（データではなくレースが無い） | ❌ 不要 |
| **C 場内の一部レースのみ欠損** | 37 | 153 | **136** | 取得抜け | ✅ 可能（要調査） |

→ **真に回収すべきは 482 ではなく 425 レース**（A1 289 + C 136）。

**A2 = 開催中止 の根拠**: 同じ日に他場は正常に結果が入っているのに、当該場だけ
**全レースの `condition` と `weather` がともに NULL**。実例と外形的裏付け:

| 日付 | 場 | R | 状況 |
|---|---|---|---|
| 2024-08-12 | 盛岡 | 12 | 台風5号が東北へ接近・上陸した日 |
| 2024-08-16 | 大井 | 12 | 台風7号が関東直撃した日 |
| 2024-08-29 | 佐賀 | 12 | 台風10号が九州へ上陸した日 |
| 2024-12-16 | 水沢 | 12 | 岩手・冬期 |

**A1 = ファイル未取得 の根拠**: 該当 7 日（2024-07-24 / 07-25 / 09-03 / 09-28 / 09-29 /
09-30 / 10-22）は **その日の全場・全レースが欠損**しており、かつ completed 一覧に
**発走前の `RANV/SENV`（レース日の 2 日前作成）しか無く、成績確定後のファイルが 1 つも無い**。
他の日は成績確定ファイル（レース日当日作成の `RA/SE/HR/O1..O6/WF` 計 11〜14 本）が揃っている。

### 8.1b ⚠️ 8.1 の分類は誤りだった — 独立系統で検証した結果（2026-08-14）

8.1 では「場まるごと欠損 + `condition`/`weather` が全 NULL」を**開催中止**と判定し
346レースをそう分類した。**これは誤り。** その特徴は「レース後のデータが一度も
届かなかった」ことしか意味せず、**中止と取得漏れの両方で同じ形になる**。

独立した検証系統として **`sekito.payout`**（2023-01〜2025-06・当時スクレイプされた
NAR の払戻）を使った。chihou と別経路・別タイミングで取得されている。

**注意**: `sekito.payout` 自体に穴がある。chihou に結果がある（＝実施が確実な）
1,699 場日のうち払戻を持つのは **91.2%**。したがって「払戻ゼロ」だけでは中止と
言えない。**その日に他場の払戻を持っているか**（＝sekito が生きていたか）を
条件に付けて判定した。

| 判定 | 場日 | レース | 根拠 |
|---|---|---|---|
| **A 実施された（chihou の取得漏れ）** | 31 | **242** | sekito が払戻を持っている |
| **B 中止** | 6 | **67** | sekito は同日に他場を取得しているのに当該場だけゼロ |
| **C 判定不能** | 10 | **101** | sekito もその日は全場ゼロ（sekito 側の停止） |

**→ 欠損の過半（242/410）は中止ではなく取得漏れ。中止と確認できたのは 67 レースだけ。**

**B 中止（確定）**: 2024-01-24 笠松9 / 2024-08-12 盛岡12 / 2024-08-16 大井12 /
2024-08-29 佐賀12 / 2024-12-16 水沢12 / 2025-03-19 浦和10
（台風5号・7号・10号、岩手の冬期と整合）

**C 判定不能の主体は 2024-07-24 / 07-25**（各4場48レース・計96レース）。
`sekito.payout` は **2024-07-22〜28 の1週間が丸ごとゼロ**で、sekito 側が
停止していた期間にあたるため証拠にならない。
一方この2日は **門別(北海道)・名古屋(愛知)・園田(兵庫)・川崎(神奈川)** が
同時に欠けており、**地理的に無関係な4場が2日連続で同時中止する事態は考えにくい**。
状況証拠としては取得漏れ寄りだが、確定はしていない。

⚠️ **2025-07 以降の欠損（約67レース）は `sekito.payout` の範囲外**（2025-06-28 まで）
なので、この方法では判定できない。

### 8.2 根本原因: 地方の蓄積系取り込みが定期実行されていない

Windows のタスクスケジューラに登録されている UmaConn 系タスクは 3 つだけ:

| タスク | 内容 |
|---|---|
| `kiseki-UmaConn-Realtime` | 9:00 起動・速報系 0B12 を30秒ポーリング |
| `kiseki-UmaConn-FetchResults` | 5分おき 10:00-22:30・速報系 0B12 |
| `kiseki-UmaConn-Watchdog` | 上記2つの監視 |

**`--mode daily` / `--mode recent`（蓄積系 `NVOpen`）を定期実行するタスクが存在しない。**
`RACE_completed.txt` の最終更新も **2026-05-14** で止まっており、以降は手動実行すら無い。

したがって地方の結果は**速報系だけで入っている**。速報系は「その時点で確定している分」しか
返さないため、**取りこぼすと二度と埋まらない**。これが A1・C の直接原因であり、
`race_payouts` が 2026-01 以降にしか無いこと（HR を送る経路が速報系にしか無い）とも整合する。

> 補足: 2026-05-21 以降の C（門別・佐賀・園田・大井・名古屋・金沢の各1〜5R）は
> completed 一覧に**そもそも該当日のファイルが無い**。蓄積系が止まっている期間そのもの。

### 8.3 🔴 前提バグ: 蓄積系の再取得が破壊的だった（**修正済み**）

`chihou_race_importer._bulk_upsert_races` の UPSERT は
`set_={col: stmt.excluded[col] for col in update_cols}` で**全列を無条件上書き**していた。

発走前 RA（出走馬名表・出馬表）は `condition` / `weather` / `first_3f` / `last_3f_race` /
`lap_times` / `finishers_count` が空で届く。蓄積系には同一レースについて**発走前ファイルと
成績確定ファイルの両方**が存在し、処理順は SDK 任せなので、**発走前ファイルが後に来ると
確定値が NULL に戻る**。JRA 側 `race_importer.py` には 2026-08-02 に同じガード
（`_POST_RACE_ONLY_COLS` + `COALESCE`）が入っているが、**chihou には無かった**。

つまり **8.4 の回収を今のコードで実行すると、正常なレースのデータを壊しながら進む**。

**対応（本ブランチで実施済み）**:
- `backend/src/importers/chihou_race_importer.py` に `_POST_RACE_ONLY_COLS` を新設し、
  当該6列を `COALESCE(excluded.x, chihou.races.x)` に変更
- `backend/tests/test_chihou_race_importer.py` に
  `TestPostRaceOnlyColumnGuard` を追加（compile 後の SQL を検査。ガードを外すと落ちる）
- テスト: 10 passed

> ⚠️ **この修正を VPS にデプロイしてから回収を実行すること。** 取り込み先は
> VPS FastAPI (`/api/chihou/import/races`) なので、Windows 側だけ動かしても意味がない。

### 8.4 回収手順（承認待ち）

| # | 対象 | 手段 | 備考 |
|---|---|---|---|
| 1 | A1 289レース + 2026-05-21以降の C | `umaconn_agent.py --mode recent --from-year 2024` | completed 済みファイルは自動スキップ。**未取得の成績確定ファイルだけ**を拾う |
| 2 | `race_payouts` 2024-01〜2025-12 | 同上 + `HRNV*` を completed 一覧から除外 | HR ファイルは completed 済みのため今のままでは skip される。**除外すれば2024年まで払戻が埋まる** |
| 3 | C のうち 2026-04-20 以前（〜約120R） | 該当日の `RANV/SENV` を completed 一覧から除外して再取得 | ファイルは取得済みなのに欠損している理由が未特定。1日で検証してから広げる |
| 4 | 蓄積系の定期化 | `kiseki-UmaConn-Daily` タスクを新設（例: 毎朝 7:00 `--mode daily`） | **これを入れないと同じ欠損が再発する** |

**副次的な利益（#2）**: `race_payouts` が 2024-01 まで遡れば、
3連単・ワイド等の払戻を使った検証期間が **7か月 → 31か月** に伸びる。
ただし **`place_odds` の 4着以下は復元されない**（HR は1〜3着のみ）ので、
3.5 節の複勝バイアス問題は解消しない。複勝は 2026-04 以降のまま。

### 8.5 netkeiba スクレイパー停止の原因特定（**sekito 側**）

`sekito.scripts_schedules` を実見した結果、**「壊れた」のではなく「地方を毎日取る
スケジュールが存在しない」**ことが判明した。

| id | script | cron | 有効 | 備考 |
|---|---|---|---|---|
| 63 | `bin/scrape/netkeiba-index` | `30 8 * * 6,0,1` | ✅ | **土日月のみ**。これが唯一のタイム指数取得経路 |
| 56 | `bin/scrape/netkeiba --type nar --target horse_weight` | `30 21 * * *` | ✅ | 21:30 は開催終了後で netkeiba に馬体重が無い → **毎日 `not_available` を量産** |
| 31 | `bin/scrape/netkeiba --type nar --target paddock horse_weight` | `*/10 10-21 * * *` | ❌ 無効 | |
| 52 | `bin/scrape/netkeiba --type jra ...` | `*/5 9-17 * * 6,0,1` | ❌ 無効 | |
| 8 | `bin/run/nar-races` | `20 0 * * *` | ❌ 無効 | `sync-nar-from-umaconn` へ移行済 |
| 22 / 92 | `bin/scrape/kichiuma` | `30 0` / `30 6` **毎日** | ✅ | **2本あるので kichiuma は健全** |

- 観測された「地方 netkeiba が月曜だけ」は id=63 の `6,0,1`（土日月）そのもの。
  土日は JRA 側を取るため、地方が残るのが月曜になる。
- `netkeiba_ip_restricted*` フラグは**全て false**（IP 制限による停止ではない）。

**「取得対象レースが無い」わけではない**（切り分け済み）:

- `sekito.races` の地方レースは**毎日フル充足**（2026-07: 1,256レース/31日/12場）。
  スクレイパーが列挙する母集団は健全。
- 同じ母集団を使う `bin/scrape/kichiuma` は毎日 100% 取れている（id=22 `30 0` +
  id=92 `30 6` の**2本立て**）。
- → **列挙も供給元も生きていて、単に「地方のタイム指数を毎日叩くスケジュールが無い」だけ。**

**いつ失われたか**: `scripts_schedules` の `created_at` / `updated_at` と remarks より、
2026-06-07 に `sync-nar-from-umaconn`（id=88/91、remarks に「地方netkeiba/kichiumaスクレイパの
対象レース供給(2026-06 復旧)」）が追加されている。地方 netkeiba のデータが途切れるのは
**その翌日 2026-06-08**。この復旧作業でスケジュール構成を組み替えた際に、
日次のインデックス取得ジョブが**削除された**とみられる
（`scripts_schedules` は行削除されるため履歴が残らない）。
- **修正案**: 地方向けに日次スケジュールを1本追加する
  （例: `bin/scrape/netkeiba-index --type nar` を毎日朝）。
  `bin/scrape/netkeiba` は `--type {all,jra,nar}` と `--target time_index ...` を持つ。
- ついでに id=56 は実行時刻が不適切（21:30）。地方の馬体重が要るなら発走前の時間帯へ。
  要らないなら止めるべき（毎日 1,200件超の `not_available` を書いている）。

> ⚠️ **sekito は別リポジトリ・別デプロイ**。ここでの変更は kiseki の worktree では行わない。
> また **過去日のタイム指数が netkeiba 側に残っているかは未確認**。
> 2026-06〜08 の遡り取得可否は 1 日試してから判断すること。

### 8.5b 回収経路の実機検証（2026-08-13）— **蓄積系しか無く、全期間再DLを伴う**

**① 速報系（0B12）は過去日を返さない — 確定**

```
=== FETCH-RESULTS MODE: 20240724 の成績を取得 ===
レースキー: 48 件
成績データなし（レース未確定）
```

レースキーは DB から 48 件引けるのに成績が 0 件。
`--mode fetch-results --fetch-date` は**過去日の回収に使えない**。

**② 蓄積系（NVOpen / RACE dataspec）は 2024 年から返す — ただし全ファイル再DL**

```
NVOpen 呼び出し開始: dataspec=RACE, from=20240101000000, option=3
NVOpen 戻り値: rc=0, 読込ファイル数=12283, DL数=12283, 最終TS=20260812223130
[completed] 処理済みファイル: 11106 件（スキップ対象）
```

- **rc=0 で 2024-01-01 以降が取れることは確定**（回収は原理的に可能）
- しかし **DL数 = 読込ファイル数 = 12,283**。UmaConn 側のキャッシュに無く、
  **2.5年分を丸ごと再ダウンロードする**動きになる
- `completed` 11,106 件は**読み込み後のスキップ**にしか効かず、**ダウンロードは省略されない**
- 実測: **37分間 NVRead が -3（ダウンロード待機）のまま、取得 0 件・ファイル未開始**。
  ディスク書き込みも観測されず、SDK が全ファイルを揃えてから返す挙動とみられる

**③ 実行は夜間のオフピークに限る**

`from_time` に上限は無く「その日だけ」を狙って取ることはできないため、
この全期間DLは避けられない。一方 UmaConn COM は realtime / FetchResults と共有で、
**レース日の日中に長時間占有すると当日のオッズ・結果収集を壊す**
（2026-08-04 に fetch-results 4本の競合で `NVSetServiceKey` 60秒タイムアウトが発生した前例）。

- 空き窓は **realtime 終了（約23:20）〜 翌 9:00 の約9時間**のみ
- ファイルは到着ごとに `mark_file_completed` されるので、**途中で止めても進捗は残る**。
  一晩で終わらなければ複数夜に分けてよい
- 2026-08-13 07:13 に開始した回は 07:50 に `taskkill /F` で停止した
  （9:00 の realtime 起動前に本番を守るため）

### 8.5c 夜間バックフィルのタスク化（**実施済み・2026-08-13**）

全期間DLは避けられず日中に走らせられないため、夜間窓で自動実行する形にした。

| タスク | 時刻 | 内容 |
|---|---|---|
| `kiseki-UmaConn-Backfill` | 毎日 **23:50** | `run_umaconn_backfill.vbs` → `--mode recent --from-year 2024` |
| `kiseki-UmaConn-Backfill-Stop` | 毎日 **08:30** | `run_umaconn_backfill_stop.vbs` → 9:00 の realtime 起動前に確実に落とす |

**安全側の作り**:

- launcher は **realtime が動いていれば起動しない**（COM の奪い合いを構造的に防ぐ）
- 多重起動しない。前夜から継続中ならそのまま走らせる
- 停止は `Terminate`(=TerminateProcess)。`DLL_PROCESS_DETACH` を走らせないので
  NVDTLab.dll(FastMM) のリークダイアログを出さずに落とせる
- ファイルは到着ごとに `mark_file_completed` されるため**途中停止でも進捗が残り**、
  複数夜に分割して完走できる
- `ExecutionTimeLimit` 10時間・`MultipleInstances IgnoreNew`
- ログ: `C:\kiseki\windows-agent\backfill.log`

**実機検証済み（2026-08-13）**:

```
9:10:22 stop: no backfill process running        ← 停止タスク: 対象なしでも正常終了(rc=0)
9:10:54 skip: realtime is still running          ← 起動タスク: realtime 稼働中は起動しない
recent procs: 0
```

登録: `powershell -ExecutionPolicy Bypass -File C:\kiseki\windows-agent\register_backfill_task.ps1`

**初回実行は 2026-08-13 23:50**。翌朝以降、`backfill.log` と
`chihou.race_payouts` / 欠損レース数の推移で進捗を確認すること。

### 8.5d sekito スケジュールの修正（**実施済み・2026-08-13**）

VPS（`ssh sekito` = 160.251.234.83）から直接適用した。
admin API は `uid` ベース認証（`sekito.users` 照合）で、他人の uid を持ち出して
認証するのは筋が悪いため **DB 更新 → `sekito-backend-1` 再起動**の経路を採った
（スケジューラは起動時にしか `scripts_schedules` を読まない）。

| id | script | 変更 |
|---|---|---|
| **63** | `bin/scrape/netkeiba-index` | cron `30 8 * * 6,0,1` → **`30 8 * * *`**（毎日） |
| **56** | `bin/scrape/netkeiba --type nar --target horse_weight` | **無効化**（`is_enabled = false`） |

**id=56 を止めた根拠**: 21:30 は開催終了後で netkeiba に馬体重が無く、2026-06 以降は
**毎日 1,200 件超の `not_available` を書くだけ**になっていた。書き込み先の
`sekito.races.is_horse_weight` は**リポジトリ全体で書き込みのみ・どこからも読まれていない**
（`routes` / `frontend` に参照ゼロ）。NAR の馬体重が必要になったら**発走前の時間帯**で
設計し直すこと。手動実行時のログでも `馬体重データなし` が全レースで出ており、
時間帯の問題であることが裏付けられた。

再起動後のログで反映を確認:

```
Loaded schedules ... {"id":63,"cron":"30 8 * * *","script":"bin/scrape/netkeiba-index","enabled":true}
Skipping disabled schedule {"scheduleId":56,"scriptName":"bin/scrape/netkeiba"}
```

**次回の自動実行は 2026-08-14 08:30**（当日 08:30 は過ぎていたため 8/13 分は手動で補完）。

⚠️ **コンテナ内で手動実行するときは `/opt/venv/bin/python3` を使うこと。**
`sh -lc` のログインシェルは PATH をリセットして `/usr/bin/python3` を掴み、
`ModuleNotFoundError: No module named 'pandas'` で落ちる（pandas は `/opt/venv` にある）。

```bash
ssh sekito "docker exec -d sekito-backend-1 sh -c 'cd /app && \
  /opt/venv/bin/python3 scripts/bin/scrape/netkeiba-index --type nar > /tmp/nk.log 2>&1'"
```

### 8.5e 🔴 回収の結果 — **過去の欠損は回収不能だった**（2026-08-14）

夜間バックフィル初回（2026-08-13 23:50 〜 08-14 01:05・約75分）の結果:

```
NVOpen 完了: 823,124 件取得 from RACE
RECENT 完了: 279 ファイル / 46,263 件 DB 反映 / 11,064 スキップ
```

completed 一覧は 11,106 → **12,325 ファイル**（+1,219）に増えたが、
**A1（その日まるごと欠損）の各日は依然として発走前ファイルしか無い**:

```
20240724 -> RANV2024072420240722230003.nvd, SENV2024072420240722230003.nvd
20240725 -> RANV2024072520240723230011.nvd, SENV2024072520240723230011.nvd
20240903 -> RANV2024090320240901230425.nvd, SENV2024090320240901230425.nvd
```

（いずれも作成日がレース日の 2 日前＝出馬表。成績確定後のファイルが存在しない）

**UmaConn 側にもう成績確定ファイルが残っていない。** 当時取り逃した分は
蓄積系を回しても戻らない。

| 指標 | 実行前 | 実行後 | 差 |
|---|---|---|---|
| 結果欠損レース（ばんえい除く） | 482 | **477** | −5（2026-07-12 のみ） |
| `race_payouts` 2024年 | 1,236行 | 1,267行 | +31 |
| `race_payouts` 2025年 | **0** | **0** | 変化なし |
| `races.condition` 充足 | 36,102 | 36,147 | +45 |
| `race_entries.horse_weight` 充足 | 374,619 | 375,067 | +448 |

**結論**:

- **A1 289レース / C 136レース は恒久的に失われた**。2024-07〜10 の欠損は残る
- **`race_payouts` の 2024〜2025 遡りも不可能**（HR ファイルが返らない）。
  3連単等の払戻検証は **2026-01 以降のまま**
- 埋まったのは蓄積系が止まっていた 2026-05-14 以降の一部だけ
- ⚠️ **これは「取り逃したら終わり」を意味する。** 夜間バックフィルの本当の価値は
  過去の回収ではなく、**今後の取りこぼしを翌日までに埋めること**にある。
  タスクは維持する（速報系だけでは同じ穴が開き続けるため）

### 8.6 死活監視の穴を塞いだ（**実施済み**）

netkeiba の縮退を `chihou_data_health_check.py` が**一度も検知していなかった**理由は 2 つ。

1. **供給元を OR で合算していた** — `kichiuma OR netkeiba` を 1 本の供給率にしていたため、
   kichiuma が健全なら netkeiba が全滅しても 95% を維持する
2. **判定が baseline 比のみ** — 段階的に崩れると baseline も一緒に下がるので比率が 1.0 に
   張り付き、**崩れきった後は永久に OK を返す**

本番実測がそれを示している:

```
[OK  ] 外部指数供給率 netkeiba(time_idx): 直近  6.5% (baseline 5.6%, 比率117.5%)
[WARN] 外部指数 取得日数 netkeiba(time_idx): 1/8 日 (12.5%  閾値 80%)
```

**対応（本ブランチで実施済み）**:
- 供給率を**供給元ごとに独立判定**。netkeiba は `is_time_index = true` に限定
  （モデルの入力条件と揃える。生の行数では使えないデータを数えてしまう）
- `check_external_index_active_days` を新設。**開催日のうちデータがある日の割合**を見るので、
  比率ベースが取りこぼす「日次 → 週1」の縮退を捕まえる（閾値 80%）
- スタブカーソルによる判定ロジックのテスト 8 件を追加

### 8.7 P1 の進捗

**完了**

- [x] 「結果欠損 482レース」の再分類 → 実回収対象は **425レース**（346 は開催中止）
- [x] 根本原因の特定（**地方の蓄積系取り込みが定期実行されていない**）
- [x] importer の破壊的 UPSERT を修正 + テスト（PR #128・CI 通過）
- [x] netkeiba 停止原因の特定（**sekito のスケジュール欠落**。IP制限でも列挙失敗でもない）
- [x] 死活監視の穴を塞ぐ（供給元別判定 + 取得日数チェック）

**ブロック中 — 本セッションからは実行できなかった**

以下は本番へのミューテーションで、いずれもツール実行がゲートされたため未実施。

| 作業 | 必要な操作 |
|---|---|
| PR #128 のマージ・デプロイ | `gh pr merge 128 --merge` → galloplab デプロイ |
| ~~sekito スケジュール修正~~ | ✅ **実施済み**（8.5d） |
| ~~id=56 の整理~~ | ✅ **実施済み**（8.5d・無効化） |

**デプロイ後に実行する（Plan A）**

- [x] 回収経路の実機検証（8.5b）→ **速報系は不可・蓄積系は可だが全期間再DLが必要**
- [x] 蓄積系の夜間バックフィルをタスク化（8.5c・初回 2026-08-13 23:50）
- [ ] 翌朝以降、`backfill.log` と欠損レース数・払戻件数の推移で進捗を確認
- [ ] `race_payouts` 2024-01〜 のバックフィル（`HRNV*` を completed 一覧から除外）
- [ ] C のうち 2026-04-20 以前（約120R）の欠損理由を特定
- [ ] `kiseki-UmaConn-Daily` タスク新設（**これが無いと同じ欠損が再発する**）

**P1 の残り**

- [ ] kichiuma 2024-07（1か月欠測）の遡り取得可否を確認
- [ ] netkeiba 2026-06〜08 のタイム指数が netkeiba 側に残っているか 1 日試して確認
