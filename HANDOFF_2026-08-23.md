# 引き継ぎ 2026-08-23（JRA）

このセッションは「**現在の指数で1日の開催の回収率100%超を達成できるか**」から始まり、
検証の過程で見つかった**データ基盤の障害を4件つぶす**ところまで進んだ。

---

## 1. 本題の現在地 — 買い方の検討

### 結論: 現在の指数では届かない。ただし券種を替えると距離が5分の1になる

検証レポート（全部の数字と根拠）: https://claude.ai/code/artifact/cb272d5c-2493-414c-8750-03594b496765

**最初にやるべきだったのは walk-forward だった。** DB の `calculated_indices` は
version=27 が 2023-05〜2026-06 まで**全部 in-sample**（`calculated_at` が 2026-08-02 以降、
学習終端 2026-06-30）。ここから測った ROI・的中率は全部上振れしている。

`anagusa_top3_walkforward.py` の `fit_vintage` を再利用して 2024Q1〜2026Q3 の11四半期を
各期の直前までで学習し直したところ（**全11 fold で約3分**）、8,349レース/284開催日の
honest な母集団が得られた。結果:

| | 複勝ROI | 95%CI |
|---|---|---|
| 指数1位 [walk-forward honest] | **0.831** | 0.815–0.847 |
| 指数1位 [DB v27 in-sample] | 0.906 | 0.889–0.922 |
| 1番人気 [市場・指数不使用] | 0.853 | 0.839–0.867 |

同一レースで対応をとると **指数の増分は −0.022 [−0.040, −0.003]（有意にマイナス）**。
in-sample では +0.053 と有意にプラスに見えていた。**リークが符号を反転させていた。**

### やって効かなかったこと（同じ穴を掘らないこと）

| 施策 | 結果 |
|---|---|
| レース厳選（1次元総当たり・探索/確認で分割） | **1.0を超えるセルはゼロ**。最良は「単勝1.5倍以下」だが指数を使わない市場構造(0.958)と同値 |
| 複勝EVで絞る | 較正は正しいのに **EVが高いほどROIが下がる**（EV≥1.20で0.773）。EVは「モデルが市場より強気な馬」を拾う装置で、上乗せが小さい以上その中身は推定誤差 |
| サブ指数の圧縮を解く（直近6走×23項目=179特徴を生データで） | 市場への上乗せは **+0.0016** で17列のサブ指数と**同値**。訓練を4倍にしても動かず |
| 券種名や特徴量の小手先の追加 | 桁が違う（必要なのは +0.06 AUC） |

### 効いたこと

| 施策 | 効果 |
|---|---|
| **オッズを特徴量に入れる**（現行34特徴にオッズ・人気は不在だった） | 複勝ROI 0.831 → 0.866 |
| **券種を三連複に替える** | 市場への上乗せが 複勝+0.010 → **三連複6点+0.066 [+0.033,+0.099]**、三連単3点+0.155。着順確率の積で増幅される |
| **2着・3着を直接モデル化**（3ヘッド＋Henery混合 λ=0.82, μ2=0.35, μ3=0.5） | 三連複3点 0.868 → **0.895** [0.851, 0.941] |

⚠️ 素直な条件付け（`P(j 2着|i 1着) ∝ p2_j`）は**悪化する**（6点 0.849→0.834）。Henery と混ぜること。
⚠️ パラメータ選定は**3分割必須**（2024調整→2025検証→2026確認）。2分割だと μ が確認期間に転移しなかった。

### 目標までの距離（同じ物差し w で比較）

「w」= 市場の勝率に真の着順スコアを混ぜる量。**ROI 1.0 に届くのに必要な w**:

| 券種 | 通算ROI>1 | 日次中央値>1 |
|---|---|---|
| **三連複12点** | **0.028** | **0.030** |
| **三連複6点** | **0.031** | **0.033** |
| 三連単6点 | 0.050 | 0.071 |
| **複勝1点** | **到達せず**（w=0.13 で 0.994） | 到達せず |

🔴 **当初「日次ROI中央値>1 なら複勝一択」と報告したが誤り。** 払戻を一律スケールして
測ったのが原因で、実際のモデル改善は配当ではなく**的中率**を上げる形で効き、日次分散を下げる。
**点数を増やすと日次の要求が通算に近づく**（三連複1点は 0.027 vs 0.053 と乖離、6点で 0.031 vs 0.033）。

**着地点: 三連複6点を全レースで買う。** 1点100円 × 約32レース = **1日19,200円**で
`.env` の予算枠（1日30,000/1R 5,000/1点1,000）に収まる。現在地は w≈0.009 相当。

---

## 2. 今日つぶした障害（全てマージ・デプロイ済み）

| PR | 内容 |
|---|---|
| **#265** | エキゾチックオッズのパースが **11バイトずれ**ていた。`EXOTIC_HEADER_SIZE` 51→40。票数合計11バイトはヘッダーでなく**レコード末尾**。馬番が出走頭数以内の行が三連単1.4%/三連複3.0%しかなかった。壊れた 12,269,943 行は削除済み |
| **#266** | `JVOpen(option=3/4)` が**不可視のセットアップダイアログ**で永久ブロック。`jvlink_dialog_guard` がタイトル前方一致で取りこぼしていた。**5.8時間 → 5秒台**。あわせて JVSkip の戻り値誤用など実バグ4件 |
| **#267** | `jvlink_historical` の UM 取得が `--from-date` を無視して26年分を舐め、3,600秒でタイムアウトしていた。**3,601秒 → 185秒** |
| **#268** | エージェント健康診断 `scripts/jra_agent_health.py`。`run_historical.vbs` に直近90日を渡す |
| **#269** | 推奨ページの tier を数値化（`tier_score`） |
| **#270** | ワイドの券種名を `wide` に統一。637,628行を改名済み |

### 実測された改善

| | 修正前 | 修正後 |
|---|---|---|
| 三連複オッズの馬番妥当性 | 3.0% | **100.00%**（`registered_count` 基準） |
| `JVOpen(BLDN, option=4)` | 21,600秒でタイムアウト・**完走ゼロ** | **約12分で完走**（589,269件をDBへ） |
| `breeding_horses.sire_breeding_code` | **0 / 314,247** | 161,604 / 314,585 |
| 直近7日の血統欠落 | 出走の11.5%（8/23は54/468頭） | **0.0%** |
| ワイドの `race_payouts` との join | **0件**（券種名不一致で空振り） | 10,378件・払戻の99.3%が格納値(最低オッズ)以上 |

---

## 3. 次にやること（優先順）

1. **`odds_backfill.py --option 4` で2024年まで確定オッズを再取得**
   option=1 の保持窓は**ちょうど1年**で、現在 honest母集団 9,264レースのうち **3,450（37.2%）しか
   確定オッズが無い**（2025-08-23 以降のみ）。option=4 が復活したので残り約5,300レースを埋められる。
   **これが埋まれば9,264レース全体で三連複の期待値検証ができる。**
   手順は `windows-agent/RUN_ODDS_BACKFILL.md`。JV-Link が混むので**夜間推奨**。
2. **三連系オッズの前向き蓄積を活かした期待値選別**
   現在の選別は「確率上位K点」で期待値を使えていない（`race_payouts` は的中組合せの払戻しか持たない）。
   確定オッズが入った今、EV ベースの選別が検証できる。
3. **listwise ランキングを学習目標そのものにする**
   今回は二値ヘッド3本を後から合成した。「順位づけに効いた」のが傍証。
4. **地方競馬で同じ walk-forward を当てる**
   障害競走だけ市場への上乗せが **+0.0133（全体の6倍）**だった。非効率な市場ほど手持ちデータが効く。
   `chihou` スキーマは既にあるので数時間で測れる。
5. 棚卸し: `kiseki-RunNEXT5`（一度も実行されていない残骸）/ `kiseki-Chokyo-Setup`
   （`--setup` という**存在しない引数**で登録・トリガー失効済み）

---

## 4. 踏んだ罠（次も踏む）

- **DB の `calculated_indices` から ROI・的中率を測ってはいけない。** 全期間 in-sample。
  判定は必ず walk-forward で、**市場のみモデルをベースラインに並走**させて
  「市場の上にどれだけ乗ったか」だけを見る。
- **`calculated_at` は UTC naive**、DB の `now()` は JST。9時間ずれる。
- **スケジュールタスクの rc は全機能で無意味。** 全 VBS が非同期起動で即 return する。
  6時間ハングして強制終了された晩も rc=0 だった。→ `jra_agent_health.py` で**データ側**から見る。
- **VM 上の PowerShell は文字化けする。** 先頭に
  `$OutputEncoding=[Console]::OutputEncoding=[Text.Encoding]::UTF8;` を付ける。
  日本語パターンを `Select-String` に渡すと SSH 経由で壊れるので ASCII で絞る。
- **ファイル転送は `scp`。** UNC パスは ssh 越しの PowerShell だとエスケープが壊れる。
  Windows のスクリプト起動は **RunAdhoc タスク経由**（SSH 直接起動は JVOpen が無限ブロック）。
- **`kiseki-JVLink-BldnFull-Stop`（毎日08:00）**は問答無用で殺す。日中に手動で流すなら08:00通過後。
- **バックエンドの `--backend` デプロイはソース変更を反映しない。** レジストリイメージなので
  main へ push → CI の "Build & Push Backend Image" 完了を待つ。確認は
  `docker exec galloplab-backend-1 grep ... /app/src/...` でコンテナ内を直接見る。
- **固定長レコードのオフセットは引き算で求めず、仕様書の絶対位置を写す。**
  テストのフィクスチャを実装と同じ仮定で作ると、両方まとめて間違える（今回まさにそれ）。

---

## 5. 参照

- 検証レポート: https://claude.ai/code/artifact/cb272d5c-2493-414c-8750-03594b496765
- 分析スクリプト: `/private/tmp/claude-501/.../scratchpad/roi100/`（walk-forward 生成・評価一式。
  再現するなら `wf_gen.py` → `wf7_finish.py` → `wf9_final.py`）
- 健康診断: `.venv/bin/python scripts/jra_agent_health.py`（終了コード 0/1/2）
- VM 手順: `windows-agent/RUN_ODDS_BACKFILL.md`
- 詳細な技術メモは Claude の memory に14件記録済み（`jra_walkforward_index_negative_*` /
  `jra_exotic_edge_amplification_*` / `jra_exotic_daily_criterion_corrected_*` /
  `jra_jvopen_option4_broken_*` / `jra_agent_monitoring_gap_*` など）

## 6. 未マージ

- **#264 `feat/keirin-tf-core-redesign`** — 競輪の別作業。このセッションでは触っていない。
