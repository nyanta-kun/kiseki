# 引き継ぎ 2026-08-27 — 型ラボ（レースの型で商品を張り直す検証台）

> 📇 引き継ぎメモの索引は [`HANDOFF.md`](HANDOFF.md)。**2026-08-27 時点のスナップショット**であって、
> 現在の運用ルールではない。正本は `CLAUDE.md`（競輪の推奨は
> `keirin/docs/RECOMMENDATION.md`）。食い違ったら正本が正しい。
> 「次にやること」は書いた時点の未着手リストなので、着手前に済んでいないか確認すること。


このセッションで作ったもの・動いているもの・残っていることの一覧。
**設計と全実測の正本は `keirin/docs/type_lab/SUMMARY.md`**（+ `type_a.md`〜`type_f.md`）、
運用手順は `keirin/docs/type_lab/RUNBOOK.md`。

---

## 1. 何を作ったか

既存商品（7C/7S/7M1/7T1…）の**全面置き換えを想定した設計**を、現行のデータ・画面と
**混ぜずに**検証するための台。7車レースを6型に分け、型ごとに買い方を変える。

| 層 | 量 | 支配するもの |
|---|---|---|
| ① 軸の堅さ | `axis_sum`（境界 1.44） | 的中率 66%↔39% |
| ② 相手の開き | 全体3,4番手 − 5〜7番手 の p3差 | 3着の出どころ |
| ③〜⑥ | ライン構成 / 先頭の自力 / 開催日目 / 並びの妥当性 → 荒れ度 | 配当 4.0↔15.7倍 |

型 = 堅い(A 荒れ度≤−1 / B =0 / C ≥1) × 混戦(D / E / F)。

### 出荷した8プラン（1レース最大2つ）

| plan | 型 | 買い方 | ペーパー実測（表示的中 / 払戻中央） |
|---|---|---|---|
| `A_hit` | A 鉄板 | 三連単 1着=軸1・2着=軸2 固定 → 3着3車 | 29.69% / 18,955円 |
| `A_pay` | A | 三連単 1着=軸1固定・2着2車 → 3着流し | 19.13% / 28,320円 |
| `B_hit` | B 堅い・中 | 三連単 確率上位を想定平均払戻3万円の床まで | 27.85% / 28,955円 |
| `C_hit` | C 崩れ筋 | 三連単 予測20倍+ から確率上位12点 | 23.22% / 32,200円 |
| `D_hit` | D 混戦・軸あり | 三連複 軸2車＋相手4点（最人気の相手を外す） | 26.21% / 26,500円 |
| `E_hit` | E 混戦・中 | 三連単 予測30倍+ から確率上位14点 | 18.39% / 38,060円 |
| `F_hit` | F 大混戦 | 三連単 軸2車＋相手2車の6順列すべて（12点） | 24.15% / 23,875円 |
| `F_pay` | F | 三連単 1着=軸1固定・2着2車 → 3着流し | 8.47% / 58,885円 |

🔴 `_hit` と `_pay` は**同じ型に対する狙いの違い**（当たる回数 ↔ 払戻の大きさ）。
ROI はどれも 70〜80% で控除率の壁の周辺＝**収支の差ではない**。
型A では 2倍以上の的中件数が 1.62件/日 で**同じ**まま、払戻中央が1.5倍・10万円超が3.4倍になる。

---

## 2. 実装（すべて main にマージ済み・#324〜#336）

| 場所 | 中身 |
|---|---|
| `keirin/src/type_lab.py` | **型判定・買い目・配分の唯一の正本**。paper も live もここを呼ぶ |
| `keirin.type_lab_picks` | 検証専用テーブル（Alembic `202608270930_keirin`・適用済み） |
| `keirin/scripts/build_type_lab_picks.py` | 生成。`--mode paper\|live` / `--models board\|vintage` |
| `keirin/scripts/settle_type_lab_picks.py` | 採点（**確定オッズ**で） |
| `keirin/scripts/type_lab_daily.sh` | 日次（当日生成＋前日・当日採点）・VPS cron `15 7 * * *` |
| `keirin/scripts/type_lab_settle.sh` | **15分ごと**の前日+当日採点・VPS cron `5,20,35,50 8-23,0 * * *`（2026-08-27 夜に毎時→15分へ。/keirin の `intraday_results_wt.sh` と歩調を合わせる） |
| `backend/src/api/keirin_type_lab_router.py` | `GET /api/keirin/type-lab` |
| `frontend/src/app/keirin/type-lab/page.tsx` | 画面（`/keirin` ヘッダー右端の 🧪型ラボ から） |

### 隔離
書き込むのは `type_lab_picks` **だけ**。`picks_history` / `netkeirin_submissions` /
入稿経路は「現行との比較」で読む以外に触らない。
`test_type_lab.py::test_scripts_never_touch_existing_product_tables` が AST で固定。

---

## 3. いまのデータ

    paper  16,634行  2026-01-01〜08-11（採点 15,881）
    live       94行  2026-08-27（採点は毎時進む）

- paper の 01-01〜08-04 は**四半期 walk-forward**（`/tmp/race_type_board.npz`）由来
- 08-05 以降は **月次 vintage**（`lgbm_wt_{eval,win}_m2608`）由来。
  ⚠️ **境目をまたいで良し悪しを読まないこと**（再学習の刻みが違う）

## 4. ✅ 完了（2026-08-27 13:58 / 別セッションで回収）

引き継ぎ時点で走っていた `2026-08-05〜08-26` の paper 生成が完走し、採点も流し終えた。

    paper  19,380行  2026-01-01〜08-26（採点 17,992 / 保留 10 = 同着・失格で未確定）
    live       94行  2026-08-27〜（毎時 cron で進行）

**ペーパーが 08-26 まで伸びたので、08-27 から始まる実地とちょうど地続きになった。**

vintage 窓（08-05〜08-26・2,111行）の実測。四半期WF窓と表示的中は 1〜3pt 以内で揃う
（⚠️ 1プラン 101〜404 行しかないので、この差は読まないこと）:

| plan | n | 表示的中 | 払戻中央 | （参考）WF窓の表示的中 |
|---|---|---|---|---|
| A_hit | 282 | 29.79% | 23,725 | 29.69% |
| A_pay | 282 | 17.02% | 32,360 | 19.09% |
| B_hit | 202 | 25.74% | 24,255 | 27.85% |
| C_hit | 281 | 24.56% | 34,100 | 23.22% |
| D_hit | 101 | 29.70% | 26,655 | 26.21% |
| E_hit | 155 | 20.65% | 39,850 | 18.39% |
| F_hit | 404 | 22.52% | 28,490 | 24.15% |
| F_pay | 404 |  7.18% | 62,160 |  8.47% |

ROI は窓全体で 78.4%（WF窓 76.1%）＝どちらも控除率の壁の周辺で、**収支の差ではない**。

⚠️ **実行中のジョブがある間は worktree を消さないこと。** このセッションで
`git worktree remove --force` を実行中ジョブのディレクトリに掛けてしまい、
生成が丸ごと落ちた（保存は最後の一括なので何も残らない）。

## 5. 残っていること

1. **1週間の実地検証**（8/27 開始）。1週間たったら
   **実地とペーパーで数字が揃うか**（本番モデル ↔ vintage の差）を見る
2. 全面置き換えへ進むときの判断（`SUMMARY.md` §6）
   - 1レース1商品の割り当て（型ラボは1レース最大2プラン・既存との重複整理）
   - 三連単経路の明示ゲート（`Σ(1/予測オッズ) ≤ 0.6`。既存の平均払戻ゲートは三連単に効かない）
   - 入稿経路（12点は1点1行が要る。`formation_bet_7t1` では表現できない）
   - 売上（pt/R）の測り直し。想定払戻3〜4万円の三連単には前例が無い
3. ヘッダーの「型ラボ」リンクは**検証期間だけの4つ目**。終わったら消すか
   常設の1つと入れ替える（`/keirin/page.tsx` にコメントあり）

---

## 6. このセッションで踏んだ落とし穴（同じ失敗を繰り返さないため）

- **NpzFile を添字アクセスのたびに引くと 30MB を毎回展開する**。ループ前に dict へ展開する
- **`race_date` の型がテーブルごとに違う**。`type_lab_picks`=DATE / `picks_history`=VARCHAR /
  `netkeirin_submissions`=日付列なし。asyncpg は厳格で、取り違えると即 500
- **`hit` は PG では boolean**。1/0 を渡すと DatatypeMismatch（SQLite では通る）
- **`src.features_wt` は無い**。正しくは `src.preprocessing.feature_wt`
- **ダークモードで本文色がほぼ白**（`--foreground: #e8f0fa`）。`dark:` を書かないと
  白い面に白い文字になる。**色指定の無い数値は継承で消える**
- **VPS の keirin バッチは `.venv/bin/python3`**。素の python3 では import で落ちる
- 🔴 **新しい画面には導線を付ける**。`/keirin/review` で 2026-08-11 に踏んだのと同じ失敗を再演した
- 🔴 **外に出るものはデプロイ先で実際に叩いて 200 を確認してから報告する**。
  このセッションでは #326〜#329 と4回続けて「出してから直す」になった
